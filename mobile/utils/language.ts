/**
 * Spoken-language names, from `shared/movies/language`.
 *
 * A wrapper rather than a copy, so the website and the app name a film's
 * language the same way. Kept at this path because the app's call sites already
 * import it from here.
 */
export { formatLanguageCode } from "shared/movies/language";
