/**
 * Shared sizing constants for the filter controls.
 *
 * Moved to `shared/filters/filter-control-metrics.ts` so the website shares the exact same logic;
 * re-exported here to keep existing `@/components/filters/filter-control-metrics` imports working.
 */
export * from "shared/filters/filter-control-metrics";
