/**
 * Cinema-preset queries and cache keys.
 *
 * Moved to `shared/filters/cinema-presets.ts` so the website shares the exact same logic;
 * re-exported here to keep existing `@/components/filters/cinema-presets` imports working.
 */
export * from "shared/filters/cinema-presets";
