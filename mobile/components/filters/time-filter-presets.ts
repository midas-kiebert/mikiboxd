/**
 * The premade time-of-day ranges.
 *
 * Moved to `shared/filters/time-filter-presets.ts` so the website shares the exact same logic;
 * re-exported here to keep existing `@/components/filters/time-filter-presets` imports working.
 */
export * from "shared/filters/time-filter-presets";
