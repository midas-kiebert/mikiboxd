/**
 * The saved filter-preset model: apply, compare, save and order.
 *
 * Moved to `shared/filters/saved-presets.ts` so the website shares the exact same logic;
 * re-exported here to keep existing `@/components/filters/saved-presets` imports working.
 */
export * from "shared/filters/saved-presets";
