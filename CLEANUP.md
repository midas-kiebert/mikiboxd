# Codebase Cleanup Checklist

Track progress through the full cleanup: understand, document, refactor, and test each module.
Check a box once you've read the file, understand what it does, and it has proper documentation.

Legend:
- [ ] = not started
- [~] = in progress
- [x] = documented, reviewed, cleaned up

---

## Backend — Core (`backend/app/core/`)

- [x] `config.py` — App settings (Pydantic Settings, env vars, DB/SMTP/auth config)
- [x] `db.py` — Engine creation, connection pool, `init_db` seeding
- [x] `security.py` — JWT creation, password hashing/verification, password reset tokens (moved from `utils.py`)
- [x] `enums.py` — App-wide enums (GoingStatus, TimeOfDay, etc.)
- [x] `client_version.py` — Dotted-integer version parsing/comparison for the mobile update gate
- [x] `middleware.py` — `ClientVersionGateMiddleware`: 426s requests from mobile builds older than that platform's `MIN_SUPPORTED_CLIENT_VERSION_*`. Per-platform floors, because App Store review runs weeks behind Play and a shared floor would hold the faster store hostage to the slower one
- [x] `viewer.py` — `ViewerId` (`UUID | None`): who a read is being annotated for. `None` is a legitimate anonymous viewer, entitled to the public catalogue and none of the personal annotations — threaded through the browse routes, services, converters and crud so one code path serves signed-in and signed-out alike
- [x] `username_filter.py` — Objectionable-username denylist for App Store guideline 1.2 (slurs, child-safety terms, hardcore sexual content, staff impersonation only — deliberately minimal, see its module docstring); leet-speak folding + a small `_ALLOWED_WORDS` strip to dodge the Scunthorpe problem (`Hitchcock` must pass)
- [x] `apple_auth.py` — Apple's OAuth token endpoints as a *client* (code exchange at sign-in, revocation at account deletion) — required so deleting an account also revokes its Sign in with Apple tokens (guideline 5.1.1(v)). Fails soft throughout: nothing here may block an account deletion

---

## Backend — Validators (`backend/app/validators/`)

- [x] `username.py` — Username validation rules and normalisation (moved from `core/`)
- [x] `cinema_seating.py` — Seating preset enum and seat input validation (moved from `core/`)

---

## Backend — Entry Points & App Setup

- [x] `main.py` — FastAPI app factory, middleware, Sentry, router mounting
- [x] `backend_pre_start.py` — Startup readiness check (waits for DB)
- [x] `initial_data.py` — Calls `init_db` to seed first superuser
- [x] `scheduler.py` — APScheduler setup, registers scraping jobs
- [x] `utils.py` — Generic helpers (`now_amsterdam_naive`, `to_amsterdam_time`, `clean_title`)
- [x] `mailer.py` — Email sending and template rendering (split out of `utils.py`; not named `email.py` because it would shadow stdlib `email`). Templates live in `email-templates/src/*.mjml` (source) and `email-templates/build/*.html` (rendered); `verify_email` is the "confirm your email" message sent at registration, whose link points straight at the API (`GET /users/verify-email`) rather than through the frontend, like the digest's unsubscribe link. Note `mjml` is not a project dependency and there is no build script, so `build/*.html` is edited by hand and `src/*.mjml` kept in sync manually — `watchlist_digest.html` is now plain hand-written table HTML rather than MJML output. All mail is multipart/alternative; `_html_to_plain_text` derives the text part for generators that don't hand-write one
- ~~`logging_/logger.py`~~ — deleted (loguru removed, stdlib logging used throughout)

---

## Backend — API Layer (`backend/app/api/`)

- [x] `deps.py` — FastAPI dependencies (get_db, CurrentUser, SessionDep, etc.)
- [x] `main.py` — Router composition (mounts all route modules)
- [x] `routes/login.py` — Auth endpoints (access-token, password reset)
- [x] `routes/me.py` — Current user endpoints (profile, settings, presets, watchlist) ⚠️ Large (485 LOC)
- [x] `routes/movies.py` — Movie listing endpoints
- [x] `routes/showtimes.py` — Showtime endpoints (list, selection, visibility, pings)
- [x] `routes/friends.py` — Friend request and friendship endpoints
- [x] `routes/cinemas.py` — Cinema listing endpoints
- [x] `routes/letterboxd_lists.py` — The curated (shared, non-account) Letterboxd lists, readable without a token — filtering the catalogue by the Top 250 is browsing, not an account action; adding/removing your own lists stays on `/me/letterboxd-lists`
- [x] `routes/users.py` — User lookup endpoints (admin + public profiles)
- [x] `routes/utils.py` — Utility endpoints (health check, TMDB cache override)
- [ ] `routes/admin.py` — Superuser-only endpoints (analytics overview, movie/showtime moderation, showtime reports)

---

## Backend — Models (`backend/app/models/`)

> Models are SQLModel classes — they define both the database table schema and
> the base data shape. Each model corresponds to one database table.

- [x] `auth_schemas.py` — Token and token-payload shapes (not a DB model)
- [x] `user.py` — User account (email, password hash, settings, flags)
- [x] `cinema.py` — Cinema venue (`key` identity + display `name` + `aliases`, city, seating preset). `CinemaBase` holds only what clients are served; `key`/`aliases` are backend-only and sit on `Cinema`/`CinemaCreate` **Update 2026-09-19:** `aliases` is now served too — `CinemaPublic` adds it (the table column needs its own `sa_column`, so it cannot live on `CinemaBase`), so a client filtering the cinema list matches the same names `_matching_cinema_ids_subquery` does
- [x] `cinema_selection.py` — Which cinemas a user has selected
- [x] `cinema_preset.py` — Saved named sets of cinema selections
- [ ] `saved_preset.py` — `cinema_preset_id` (new): when set, a `SavedPreset` follows a `CinemaPreset` live instead of its own frozen `cinema_ids`/`cinema_scope`, which are kept in sync as the fallback used if the linked preset is ever deleted
- [x] `movie.py` — Movie metadata (title, duration, genres, poster). Positive id = TMDB id; negative id = synthetic listing (e.g. sneak preview) via `sneak_preview_movie()` / `is_synthetic_movie_id`
- [x] `showtime.py` — Individual screening (datetime, cinema, movie, ticket link)
- [x] `showtime_selection.py` — User's going/interested status on a showtime
- [x] `sold_out_watch.py` — One user waiting on one full showtime for a returned ticket. Unique `user_id` (not a compound key with the showtime) is the "one watch at a time" rule; one-shot, deleted once it finds a seat
- [x] `cinema_room_capacity.py` — Largest seat count ever seen in one room of one cinema, keyed `(cinema_id, room)`. Shared across every screening in the room, which is what lets the estimate converge at all — a single showtime is read a handful of times, a busy room hundreds
- [x] `showtime_seat_map.py` — Which individual seats were taken at a showtime's last reading, keyed by showtime. The per-seat half of a seat availability reading, written by the same poller pass from the same response; kept off `Showtime` because every catalogue query selects that row in full and exactly one endpoint ever wants this. Absent means "unknown", never "nothing taken"
- [ ] `seat_check_request.py` — One hand-requested seat reading ("check" button). In the database, not memory, so the hourly budget and per-screening cooldown hold across every uvicorn worker; pruned past the budget window
- [x] `showtime_ping.py` — Notification sent to a friend about a showtime
- [x] `showtime_reminder.py` — Cooldown record for the manual "remind a friend" nudge, one row per (showtime, receiver). Keyed on the receiver rather than the sender pair on purpose: the 72h cooldown is about how often that person may be nudged about that screening, not about who did the nudging
- [ ] `showtime_ping_link.py` — Short opaque code (not a self-contained token) mapping a shared `/ping/{showtime_id}/{token}` invite link back to who minted it and for which showtime
- [x] `notification.py` — Notification-centre entry (match / invite-response / request-accepted)
- [x] `showtime_visibility.py` — Per-showtime visibility mode + effective-visibility cache
- [x] `showtime_source_presence.py` — Tracks which scraper provided a showtime
- [x] `scrape_run.py` — Metadata about each scraping execution
- [x] `scrape_recap.py` — Stored per-run scrape recap (metrics + HTML + attachments; the day's rows are rendered into one email)
- [ ] `listing_first_seen.py` — Publish-timing log: when each source stream first listed each screening, with the window since that stream's previous run (and Cineville's own creation time). No FK to showtime, so it outlives the screening
- [ ] `cineville_event.py` — Raw log of every Cineville event with Cineville's createdAt/updatedAt, our first/last sighting and when it dropped out; every venue, no TMDB match needed
- [x] `friendship.py` — Accepted friend relationships (+ per-friend `shares_status`)
- [x] `filter_preset.py` — Saved filter configurations (movies or showtimes scope)
- ~~`friend_group.py`~~ — deleted (friend groups retired in the visibility overhaul)
- [x] `letterboxd.py` — Cached Letterboxd watchlist data per user
- [x] `watchlist_selection.py` — Movies on a user's watchlist
- [x] `push_token.py` — FCM device tokens for push notifications
- [x] `tmdb_lookup_cache.py` — Cache of title → TMDB ID resolutions
- [x] `city.py` — City (currently Amsterdam only)
- [ ] `watchlist_digest_queue_entry.py` — Movies newly available for the watchlist digest (queued once, ever)
- [ ] `watchlist_digest_notified_movie.py` — Per-*source* record of movies already sent/seen in the digest (keyed `(source_id, movie_id)`, not per-user, since one user's sources are notified independently)
- [ ] `watchlist_digest_source.py` — A user's watchlist digest source (may have several): frequency, list/watchlist, cinema restriction (saved preset or one-off custom ids, never persisted as a preset)
- [ ] `analytics_event.py` — Single usage-analytics event (name + free-form properties)
- [ ] `showtime_report.py` — User-submitted report that a showtime is wrong
- [x] `user_block.py` — One user blocking another; directional storage, symmetric effect (see `crud/user_block.is_blocked_either_way`)
- [x] `user_report.py` — User-submitted report about another user; mirrors `showtime_report.py`

---

## Backend — Schemas (`backend/app/schemas/`)

> Schemas are Pydantic models used as API response shapes. They are richer than
> raw models — they may include computed fields or join data from related tables.
> Converters (below) transform models into schemas.

- [ ] `user.py` — Public/private user representations
- [ ] `cinema.py` — Cinema response shape. `CinemaPublic` carries `aliases` (additive, defaulted) for client-side cinema search — mirrored by hand into `shared/client/types.gen.ts`, which is what the next regen will produce
- [ ] `movie.py` — Movie response shape. `MoviePublic`/`MovieSummaryPublic` are the film itself; whatever depends on who asked lives under a nullable `viewer` block (`MovieViewerState`/`MovieSummaryViewerState`), absent for an anonymous read
- [ ] `showtime.py` — Showtime response shape. Same split: `ShowtimePublic`/`ShowtimeInMoviePublic` are public, `ShowtimeViewerState`/`ShowtimeInMovieViewerState` carry selections, invites and friend annotations. The two viewer shapes differ rather than sharing one with empty defaults, so an empty list always means "none" and never "we didn't look"
- [x] `legacy_viewer_compat.py` — TEMPORARY. Deprecation note shared by the flat `going`/`friends_going`/... mirrors that keep pre-1.1.0 app builds working now that those fields live under `viewer`. Delete this file and the `LEGACY_VIEWER_FIELDS` blocks once no old build is still calling
- [ ] `showtime_ping.py` — Ping response shape
- [x] `notification.py` — Merged notification-centre feed item shape
- [x] `showtime_visibility.py` — Per-showtime visibility mode response shape
- [x] `seat_availability.py` — `ShowtimeSeatAvailabilityPublic` (busyness level + counts + when it was read + whether a ticket watch applies here) and `SoldOutWatchPublic`. Viewer-independent on purpose, which is what lets it be prefetched and cached per showtime; a showtime with no usable reading is omitted from a batch rather than returned with nulls
- [ ] `feed_overview.py` — New 2026-09-22. `FeedOverviewPublic`: the website feed overview's lists, each a `FeedOverviewSection` (`kind` + `ShowtimePublic`s), only the non-empty ones, already trimmed to the card's budget
- [ ] `activity.py` — New 2026-09-22. `ActivitySummaryPublic` (`GET /me/activity/summary`): per-day counts (`ActivityDayCount`, evening buckets), total, next plan + plan count, unanswered invites (≤3) + count, `ActivityFriendTally` ranking
- [ ] `festivals.py` — New 2026-09-25. Places festival screenings at their real location: `resolve_cineville_festival_cinema` (a Cineville festival event takes the cinema of the festival scraper's screening at the same time and film) and `adopt_placeholder_showtime` (the scraper moves a row Cineville put at the festival to the real cinema, keeping selections)
- [ ] `cinema_preset.py` — Cinema preset response shapes: `CinemaPresetCreate` (with `overwrite`, the explicit opt-in to replacing a same-named preset), `CinemaPresetRename`, `CinemaPresetPublic`
- [x] `cinema_scope.py` — `CinemaScope`: a preset's cinema selection as the *rule* behind it (every cinema / whole cities / individual ones) rather than a frozen id list, so cinemas that open later land inside a selection the user meant to be open-ended
- [ ] `filter_preset.py` — Filter preset response shape
- [ ] `watchlist_digest_source.py` — WatchlistDigestSource create/update/public shapes; create/update validate `cinema_preset_id`/`custom_cinema_ids` are mutually exclusive
- [x] `friendship.py` — Friend status-sharing toggle request shape
- [ ] `push_token.py` — Push token registration shape
- [ ] `city.py` — City response shape
- [ ] `analytics_event.py` — Event create/public response shapes
- [ ] `analytics_dashboard.py` — Admin analytics-overview response shape
- [x] `scrape_monitor.py` — Admin scrape-run/recap response shapes (deltas + anomaly flags)
- [ ] `publish_timing.py` — Admin publish-timing view: weekday×hour heatmaps (screenings and publish moments), lead-time and our-delay buckets, per-cinema table incl. site-vs-Cineville comparison, latest additions
- [ ] `tmdb_ambiguity.py` — Admin view of a TMDB lookup that tied between several films: the listing metadata it had, the tied candidates, the tie-break that settled it (if any), and the review toggle
- [ ] `showtime_report.py` — Showtime report create/update/admin-view shapes
- [ ] `admin.py` — Admin movie/showtime moderation request/response shapes
- [x] `user_block.py` — Blocked-account list-row shape
- [x] `user_report.py` — User report create/update/admin-view shapes; `UserReportCreate.block_user` defaults true — reporting and blocking are one client gesture
- [ ] `away_events.py` — New 2026-09-24. `AwayEventsPublic` (upcoming and missed invites with their screenings, friend requests, upcoming interested screenings that sold out) for `GET /me/away-events`

---

## Backend — CRUD Layer (`backend/app/crud/`)

> CRUD functions are the only place that touches the database directly.
> They return raw SQLModel objects (not schemas). No business logic here —
> just reads and writes.

- [ ] `cinema_filter.py` — New 2026-09-25. `showtime_at_cinemas`: the "these cinemas" filter every showtime list/count/search uses, matching `festival_id` as well as `cinema_id`, so picking a festival (a cinema row of kind festival) shows all its screenings wherever they play
- [ ] `user.py` — User queries, create, update, password check ⚠️ Large (652 LOC). `get_going_status_for_movies` is the page-batched sibling of the (now-deleted) `is_user_going_to_movie`
- [ ] `movie.py` — Movie queries with filtering ⚠️ Large (599 LOC). The `*_for_movies` functions (`get_showtimes_for_movies`, `get_cinemas_for_movies`, `get_last_showtime_datetimes`, `get_total_number_of_future_showtimes_for_movies`, `get_friends_for_movies`) are page-batched siblings of the singular per-movie versions, for `converters.movie.summaries_for_page`. `get_showtimes_for_movies` is a top-N-per-movie window-function query — see its docstring for why `selected_statuses` uses a correlated `EXISTS` instead of a join there **2026-09-22:** `status_owner_clause` is the one rule for whose selections the going/interested filter matches (everyone visible + you; `friends_only`; `only_you`), used by the three movie status clauses here and `crud/showtime.py`'s — the movie ones now honour `friends_only` too, which they used to ignore
- [ ] `showtime.py` — Showtime queries, upserts, reconciliation ⚠️ Large (518 LOC)
- [x] `showtime_visibility.py` — Effective-visibility cache from mode + status-sharing + pings (incl. co-invitees)
- [ ] `showtime_ping.py` — Ping queries and creation
- [ ] `feed_overview.py` — New 2026-09-22. The per-section queries behind `services/feed_overview.py`: unanswered invites (open, undismissed pings on upcoming showtimes the viewer is not going to / interested in), interested showtimes with a seat reading (the level itself is worked out in Python), and screenings friends picked, ranked by `sum(going=2, interested=1)` then friend count then date with the badges' own visibility rule (direct friendship + `ShowtimeVisibilityEffective`) and an optional language filter. "Answered" means GOING/INTERESTED only — NOT_GOING is also what taking a status back leaves behind
- [ ] `activity.py` — New 2026-09-22. The Activity summary's own queries: next plan / plan count (GOING+INTERESTED), unanswered-invite count (same set as `feed_overview.get_unanswered_invited_showtimes`), friend tallies (visible selections only, via `ShowtimeVisibilityEffective`, grouped per friend). Per-day counts are NOT here — they live in `crud/showtime.py` beside the builders they share (`count_main_page_showtimes_by_day`, `count_agenda_showtimes_by_day`; the agenda query is now `_build_agenda_query`, shared by page and count)
- [ ] `showtime_ping_link.py` — Create/look-up for a shared invite link's short code
- [x] `showtime_reminder.py` — Read/upsert of that cooldown row; `record_showtime_reminder` moves the existing row's timestamp forward rather than inserting a second one, so the unique (showtime, receiver) constraint never sees two
- [x] `notification.py` — Notification-centre row queries (upsert, feed, decay)
- [x] `sold_out_watch.py` — Sold-out watch reads/writes; `set_watch_for_user` moves the user's single row rather than delete-and-insert, so the one-per-user constraint never sees two
- [ ] `friendship.py` — Friend request and friendship queries (+ status-sharing) **2026-09-23:** `set_friendship_status_sharing` locks the owner row (`FOR UPDATE`) before rebuilding, so quick toggles for several friends queue instead of racing over the owner's effective-visibility rows
- ~~`friend_group.py`~~ — deleted (friend groups retired)
- [ ] `cinema.py` — Cinema queries. Resolve by `get_cinema_id_by_key`; `get_cinema_id_by_name_or_alias` is only for names arriving from outside (Cineville venues). `upsert_cinema` matches on key so a rename in cinemas.yaml edits the row in place
- [ ] `cinema_preset.py` — Cinema preset CRUD; `resolve_preset_cinema_ids` is the only way ids come off a preset (it expands `cinema_scope`)
- [x] `cinema_scope.py` — Inferring a `CinemaScope` from ticked ids and expanding it again. Inference runs on the backend so installed builds get the follow-the-city behaviour without knowing rules exist; the two directions live together because they are inverses
- [ ] `filter_preset.py` — Filter preset CRUD
- [ ] `watchlist_digest_source.py` — WatchlistDigestSource CRUD (list/create/get/delete by owner)
- [ ] `watchlist.py` — Watchlist selection CRUD
- [ ] `push_token.py` — Push token registration and lookup
- [ ] `city.py` — City queries
- [ ] `analytics_event.py` — Event creation and dashboard aggregation queries
- [ ] `showtime_report.py` — Report creation, listing (joined), status updates
- [x] `user_block.py` — Block create/delete, symmetric `is_blocked_either_way`, `get_hidden_user_ids` (both directions folded into one set — the one every list-filtering caller wants)
- [x] `showtime_seat_map.py` — One showtime's taken-seat map. `record_seat_map` always replaces, never merges: a reading is a complete snapshot of the room, so merging would pin a freed seat as taken for ever
- [ ] `seat_check_request.py` — The manual-check log: window counts (global, per host), recently-asked showtimes, and a transaction advisory lock so count-then-insert is atomic across workers
- [x] `user_report.py` — Report creation, duplicate-open-report check, listing (joined, scalar-subquery count so status filtering doesn't shrink it)

---

## Backend — Services (`backend/app/services/`)

> Services contain business logic. They orchestrate CRUD calls, enforce rules,
> and raise domain exceptions. Routes call services — never CRUD directly.

- [ ] `me.py` — User profile, settings, presets, pins, watchlist sync ⚠️ Large (1058 LOC) — needs splitting
- [ ] `showtimes.py` — Showtime selection, visibility logic, pings ⚠️ Large (975 LOC)
- [ ] `push_notifications.py` — FCM push notification orchestration ⚠️ Large (578 LOC)
- [ ] `movies.py` — Movie listing and filtering
- [ ] `friends.py` — Friend requests, acceptance, removal
- [ ] `cinemas.py` — Cinema listing
- [ ] `users.py` — User management (admin operations)
- [ ] `watchlist.py` — Watchlist sync logic
- [ ] `watched.py` — Watched-list sync logic
- [ ] `watchlist_digest_sources.py` — CRUD orchestration behind `/me/watchlist-digest-sources`: create/update/delete, the 5-source cap, and the "setting one of preset/custom clears the other" rule
- [x] `letterboxd_sync.py` — Shared sync cooldown for `watchlist.py`/`watched.py` (counts failed attempts, not just successes)
- [x] `viewer_context.py` — Settles the browse filters that depend on who is asking: fills in `selected_cinema_ids` from the account's favourite cinema preset (then its legacy selection), and resolves the Letterboxd username the watchlist filters read against. For an anonymous viewer it leaves cinemas unrestricted — the whole catalogue, not an empty feed — and drops Letterboxd list ids, which can only belong to an account. Replaces the copy of that block that sat in each of the four movie/showtime list+count entry points
- [ ] `feed_overview.py` — New 2026-09-22. Fills the website's feed-overview card (`GET /me/feed-overview`): invited → selling fast (interested, ≥ very busy, fullest first) → the viewer's custom list (the request's own filter params, only with `include_custom`) → plans (going/interested, off on the agenda page) → friends going (every cinema, feed's language) → watchlist (one screening per film, feed's cinemas and language). At most 4 sections × 3 rows, 10 rows in all, filled in priority order; a screening shown higher up is never repeated, and later sections are not queried once the budget is spent
- [ ] `activity.py` — New 2026-09-22. `get_activity_summary` for `GET /me/activity/summary?mode=all|you|friends&snapshot_time=`: the Activity screen's numbers over the whole list, built from the same filters the clients send per mode, so nothing on the page grows as it scrolls. Tests: `tests/api/test_activity_summary.py`
- [ ] `scrape_sync.py` — Triggers scraping from the API layer
- [ ] `analytics_dashboard.py` — Aggregates AnalyticsEvent/Notification/ShowtimePing/User data for the admin overview
- [x] `seat_availability.py` — Decides which showtimes get their seat count re-read and what to write down. `seats_capacity` now comes from `CinemaRoomCapacity` when the room is known (shared across every screening in that room), else falls back to the per-showtime running max; a platform-reported exact total (Eagerly, Tricket) or a manual override always wins over either. `seat_availability_level` buckets a raw reading; `effective_seat_level` applies the ratchet on top — a screening's level is capped at its `seats_level_floor` (the fullest it has ever reached) and can only rise, never fall, except sold-out-to-not which is deliberately exempt. `apply_reading` returns whether that ratchet just crossed into `SEAT_ALERT_LEVELS`, which is the only trigger for the once-ever "nearly sold out" notice. Cadence is per showtime and written onto the row as `seats_next_check_at`, computed off the *effective* level so a ratcheted showtime keeps the cadence its shown level deserves; a run is capped both overall and per ticket host. `simulate_reading` drives the whole pipeline with made-up numbers for the superuser-only staging test hook — see `admin.py`
- [x] `sold_out_watch.py` — The one thing that polls a ticket shop hard, and every rule that keeps that affordable: one watch per user, `is_pro` to have one at all, a global `MAX_ACTIVE_WATCHES`, and a cadence that bursts on start, tapers through the middle, and ramps back up for the two hours before the screening when tickets actually get handed back. One-shot — it deletes itself the moment it finds a seat
- [x] `scrape_monitor.py` — Read-only aggregation of ScrapeRun/ScrapeRecap for the admin scrape monitor (deltas + anomaly flags)
- [ ] `publish_timing.py` — Read side of the publish-timing logs for the admin view; aggregates in Python and spreads a site sighting evenly over the hours of its window
- [ ] `listing_log.py` — Writes `ListingFirstSeen` rows for the presences a successful run created (called from `scrape_sync.record_success_run`)
- [ ] `cineville_events.py` — Upserts each complete Cineville sweep into `CinevilleEvent` (keeps the first sighting, marks upcoming events a sweep no longer lists as gone); also used by the backfill script
- [x] `scrape_recap_render.py` — `RecapRunMetrics` + the recap renderer; the daily email is grouped by statistic (combined value, then each run's) instead of stitching per-run reports, and the long diagnostic dumps live in JSON attachments only
- [ ] `unidentified_listings.py` — Screenings a cinema scraper lists but could not identify (no TMDB match). Scrapers record them through `BaseCinemaScraper.record_unidentified_listing`; after each cinema scraper run `find_cineville_matches` finds the Cineville showtimes at the same cinema and minute, which the trusted-scraper cleanup then leaves alone and the recap reports. Added after LAB111's Fahrenheit 9/11 showtimes were deleted and re-created every run
- [ ] `tmdb_ambiguities.py` — Lists and marks reviewed the lookup-cache rows the matcher flagged as a tie between several very-good candidates (`TmdbLookupCache.ambiguity_json`), for the admin Scrapes page
- [x] `showtime_title_conflict.py` — Recognizing the same screening listed by Cineville and a cinema scraper under near-identical titles; used both to stop the duplicate being inserted (`upsert_showtime`) and to clean up existing ones (`runner._delete_cineville_title_conflicts`). Also collects the resulting `SourceDisagreement`s, which the recap reports as TMDB matches to review
- [x] `moderation.py` — Blocking (a teardown of friendship/requests/invites + visibility rebuild, not a flag) and reporting another user, together — App Store guideline 1.2. Reporting blocks by default; the two are still separate rows so unblocking never withdraws a report. Mails the operator on every new report, best-effort
- [ ] `away_events.py` — New 2026-09-24. What happened since the app was last used, for the app's notification tips. Sold-out reads `ShowtimeSelection.sold_out_alert_sent_at`, which `push_notifications.send_seat_alerts` now stamps even for users with the notice off
- [ ] `notification_unsubscribe.py` — New 2026-09-24. Signed per-user, per-preference unsubscribe links in every notification email (`push_notifications._send_templated_email` fills them in per recipient via `EmailData.with_unsubscribe`); seat availability unsubscribes all three seat fields. Routes in `api/routes/users.py`: GET confirms, POST changes (mail scanners)

---

## Backend — Converters (`backend/app/converters/`)

> Converters transform a raw SQLModel model object into a rich schema object.
> They are the bridge between the database layer and the API response layer.

- [ ] `showtime.py` — Showtime → ShowtimeSchema (joins selections, visibility)
- [ ] `showtime_page.py` — the `viewer` block for a whole page of showtimes in a fixed
      number of queries, instead of nineteen per row; feed/agenda/friend-agenda lists
      pass its result to `showtime.to_public` as `viewer_states`. Also builds the
      narrower `ShowtimeInMovieViewerState` shape (`in_movie_viewer_states_for_showtimes`),
      handed to `showtime.to_in_movie_public` — shares the same batched
      `ShowtimePageViewerData` load, just fewer fields per row
- [ ] `movie.py` — Movie → MovieSchema (joins watchlist status, friend data). Movie
      *cards* are built page-at-a-time by `summaries_for_page`, mirroring
      `showtime_page.py`: one batched query each for a page's showtimes (window-function
      top-N-per-movie), cinemas, last/total showtime counts, going status and
      friends-going/interested, instead of each of those per movie plus nineteen-per-row
      for every nested showtime. `to_public` (the movie detail page) batches its own
      showtime list's viewer states the same way
- [ ] `cinema.py` — Cinema → CinemaSchema
- [ ] `user.py` — User → UserPublic/UserPrivate
- [ ] `city.py` — City → CitySchema

---

## Backend — Exceptions (`backend/app/exceptions/`)

> Domain-specific exceptions with HTTP status code mappings.
> Services raise these; the global handler converts them to HTTP responses.

- [ ] `base.py` — Base exception class with HTTP status
- [ ] `user_exceptions.py` — User-related errors (not found, duplicate email, etc.)
- [ ] `movie_exceptions.py` — Movie-related errors
- [ ] `showtime_exceptions.py` — Showtime-related errors
- [ ] `friends_exceptions.py` — Friend request errors
- [ ] `watchlist_exceptions.py` — Watchlist-related errors
- [ ] `city_exceptions.py` — City-related errors
- [ ] `scraper_exceptions.py` — Scraping-specific errors
- [x] `cinema_preset_exceptions.py` — Cinema preset errors: not found, empty name, and the 409 raised when a preset name is already taken (create and rename both need it, and the client turns it into a "replace?" prompt rather than a failure)
- [ ] `watchlist_digest_source_exceptions.py` — Not found, per-user source limit reached, cinema-preset/custom-ids conflict
- [ ] `letterboxd_list_exceptions.py` — Letterboxd list scrape/sync errors
- [x] `moderation_exceptions.py` — Block/report self-target and not-found errors, plus `UserBlockedError` (deliberately vague — never tells the sender which direction the block runs, so it can't be used to confirm a block landed) and `ObjectionableUsernameError`

---

## Backend — Scraping (`backend/app/scraping/`)

- [ ] `runner.py` — Main scraping orchestrator ⚠️ Very large (2042 LOC) — needs splitting
- [ ] `scrape.py` — Executes a single scraper and stores results
- [ ] `scrape_schedule.py` — When the full scrape runs, shaped by weekday after when cinemas publish: Monday half-hourly 09:00-17:30 (hourly 08-23 around it), Thursday half-hourly until noon then hourly, Tue/Wed/Fri every 2 h, weekend 4×, nothing 23:xx-08:00; each slot at a stable random offset; decided from the DB each minute so a late or missed slot is caught up rather than dropped
- [x] `cineville_client.py` — Shared Cineville POST helper with 429/5xx retry + backoff
- [ ] `base_cinema_scraper.py` — Abstract base class for cinema scrapers; `record_unidentified_listing` is how every scraper reports the screenings of a film it skipped for want of a TMDB match
- [ ] `date_conversion.py` — Date/time parsing helpers for scrapers
- [ ] `get_movies.py` — Cineville films: `get_films_by_production_ids_async` (REST /productions/search, used by the scrape) and the old website GraphQL query (broken server-side since 23 Sep 2026, unused)
- [ ] `get_showtimes.py` — Cineville events: `get_all_events_async` pages the whole listing (~12 requests) with each event's createdAt/updatedAt; the per-production fetch is kept but no longer used by the scrape
- [ ] `logger.py` — Scraping-specific log configuration
- [x] `seat_availability.py` — Turns a showtime's ticket link into a seat count, room, and (Eagerly, Tricket) exact room capacity. Four platforms, between them covering every cinema we scrape ourselves and a good many Cineville-only ones (Cineville's `ticketingUrl` is the cinema's own shop, so a cinema is read whether or not we scrape it): Z-ELITE (exact count in `data-configured-max`, max across badge types since some are per-order capped, no capacity), Tricket (the screening resource lists every seat in the room + `numberOfAvailableSeats`, so capacity is `len(seats)` — verified stable per `hallId` while availability moves, which is what makes it the room rather than the free list. Which seats are taken, and the room's layout, come from `/api/screenings/{id}/seats?basketId=` — one empty basket per host, reused for every reading and recreated on 404, which holds nothing since locking is a separate call. Gated on `TRICKET_SEAT_MAP_HOSTS`: Studio/K's map is decorative, its rooms are sold unreserved (`seating: free`), so only its count is read. Tricket names no room, so `TRICKET_ROOM_NAMES` maps Cinecenter's four `hallId`s to Zaal 1-4, joined once by hand off cinecenter.nl's own programme — without it a floor plan would have nothing to be filed under), Eagerly (exact count *and* exact capacity for all 8 sites — 10 cinemas, since Bioscopen Leiden runs Lido/Trianon/Kijkhuis off one feed with a booking app each, resolved by the feed's `cinema_id` through `eagerly_booking_host`, the only case where the ticket link's host is not enough — via each site's own unauthenticated `getSeatPlanData` seat-map endpoint — "ROL"/wheelchair-space entries excluded from both, falling back to the feed's sold-out-only status for any site not in the `EAGERLY_BOOKING_HOSTS` table, which is where Bioscopen Leiden sits). Eagerly links are recognised only on a host in `EAGERLY_SITE_HOSTS` — `/tickets/<number>` alone is too ordinary a path to identify a platform — and both tables are keyed on the host with any `www.` stripped, since our own scrapers and Cineville hand out different forms of the same site. ActiveTickets (9 tenants; every show page inlines the shop's Knockout view-model as `var jsonCart`, decoded from the opening brace with a real JSON parser rather than a regex). What it yields depends on how the room is sold: a numbered room ships its whole seat plan in `EditData.Seats` — availability, blocked flag, x/y and the row/seat names — so count, the room's real total and the taken map all come out of the one GET the link already needs, while a free-seating room ships no seats and only says sold-out-or-not. Four of the nine (Rialto De Pijp, De Balie, Cinebergen, Slieker) sell every room free-seating, so no count is possible there at any price. `TicketsAvailable`/`TicketsCapacity` exist on newer tenants and are deliberately unread — most don't send them, the ones that do send 0/0 for a screening whose sales have not opened, and the capacity is the sellable allocation rather than the room. A failed fetch raises rather than returning zero. `fetch_eagerly_room_geometry` is the floor-plan ingest's entry point: it only accepts a show's seat map for a room once the plan's own `screen_name` agrees, because the feed's room label and the booking system's screen disagree often enough to file one room's layout under another's name
- [x] `subtitles.py` — Parses cinema subtitle metadata (Dutch free text) into ISO-639-1 codes for `Showtime.subtitles`
- [ ] `title_hints.py` — Subtitle/year hints recoverable from a raw scraped title/slug
- [ ] `tmdb.py` — TMDB API client ⚠️ Large (1411 LOC)
- [ ] `tmdb_lookup.py` — TMDB movie resolution + fuzzy matching ⚠️ Large (1470 LOC)
- [ ] `tmdb_config.py` — TMDB configuration constants
- [ ] `tmdb_movie_details.py` — TMDB movie detail fetching
- [ ] `tmdb_normalization.py` — Title normalisation for matching
- [ ] `tmdb_parsing.py` — TMDB API response parsing
- [ ] `tmdb_runtime.py` — Runtime enrichment logic

**Cinema scrapers — Amsterdam:**
- [ ] `cinemas/amsterdam/eye.py` — Eye Film scraper ⚠️ Large (376 LOC)
- [ ] `cinemas/amsterdam/filmhallen.py` — Filmhallen scraper
- [ ] `cinemas/amsterdam/kriterion.py` — Kriterion scraper
- [ ] `cinemas/amsterdam/uitkijk.py` — Uitkijk scraper
- [ ] `cinemas/amsterdam/lab111.py` — Lab111 scraper
- [ ] `cinemas/amsterdam/themovies.py` — The Movies scraper
- [ ] `cinemas/amsterdam/fchyena.py` — FC Hyena scraper; fixed 2026-08-24 after the Framer migration broke it — now reads the film catalogue out of the Framer CMS's binary export instead of scraping HTML (no server-rendered listing exists anymore); per-showtime ticket page scraping (Z-ELITE) is unchanged and unaffected
- [ ] `cinemas/amsterdam/studiok.py` — Studio/K scraper
- [ ] `cinemas/amsterdam/rialto.py` — Rialto De Pijp + Rialto VU scraper

**Cinema scrapers — Rotterdam:**
- [ ] `cinemas/rotterdam/kinorotterdam.py` — KINO scraper (Eagerly)

**Cinema scrapers — Utrecht:**
- [ ] `cinemas/utrecht/hartlooper.py` — Louis Hartlooper Complex scraper (Eagerly)
- [ ] `cinemas/utrecht/slachtstraat.py` — Slachtstraat scraper (Eagerly)
- [ ] `cinemas/utrecht/springhaver.py` — Springhaver scraper (Eagerly)

**Cinema scrapers — Haarlem:**
- [ ] `cinemas/haarlem/filmkoepel.py` — Filmkoepel scraper (Eagerly)

**Cinema scrapers — Generic:**
- [ ] `cinemas/generic/eagerly.py` — Eagerly-based generic scraper; fixed 2026-08-24 a double-slash bug in every derived URL (`self.url`, `ticket_link`) since every call site passes a trailing-slash `url_base` — `self.url_base` now strips it once in `__init__` instead

**Festival scrapers:**
- [ ] `festivals/liff.py` — New 2026-09-25. Leiden International Film Festival from liff.nl's WordPress REST API (`shows_cpt`/`films_cpt`/`locations_cpt`/`sections_cpt`): every upcoming screening placed at its real cinema or venue (`LOCATION_PREFIX_TO_CINEMA_KEY`; an unknown location goes to the `liff` festival row with the location as its room), tagged `festival_id`, with `cineville_pass` true only for the competition sections. Adopts a placeholder row Cineville put at the festival. Runs as a cinema scraper keyed on the `liff` row

**Letterboxd integration:**
- [ ] `letterboxd/load_letterboxd_data.py` — Watchlist sync ⚠️ Large (1269 LOC after curl_cffi swap, 2026-09-24) — needs splitting
- [ ] `letterboxd/watchlist.py` — Watchlist parsing
- [ ] `letterboxd/watched.py` — Watched ("films") parsing; blocked/partial-page detection
- [x] `letterboxd/rss.py` — Member RSS feed parsing; the cheap "anything new?" check the incremental watched sync runs instead of a full page walk
- [ ] `letterboxd/utils.py` — Letterboxd utilities, shared `SlugScrapeResult`

---

## Backend — Inputs (`backend/app/inputs/`)

- [ ] `movie.py` — Input validation models for movie-related endpoints **2026-09-22:** `Filters.only_you` (`?only_you=`): with `selected_statuses`, only the viewer's own selections — the web's "Only my own plans", which replaced the agenda page `Filters.friend_ids` (`?friend_ids=`): only those friends' visible going/interested marks — a friend's agenda as a filter; implies GOING+INTERESTED when no status is sent (`_friends_imply_statuses`), skips the usual-cinemas default, and wins over `only_you`/`friends_only` in `crud/movie.status_owner_clause`. Optional, so older apps are unaffected

---

## Backend — Tests (`backend/tests/`)

> Current coverage: ~25%. Goal: 80%+. Prioritise services and large CRUD files.

- [ ] Review and understand existing test structure
- [ ] Check coverage report — identify untested services and CRUDs
- [ ] `tests/api/` — Route-level tests (are all endpoints covered?)
- [ ] `tests/crud/` — CRUD tests (are complex queries tested?); `test_seat_availability_candidates.py` covers the seat poller's selection + priority ordering
- [ ] `tests/services/test_seat_availability_interest.py` — the interest-triggered live read, its per-showtime cooldown, and the `checking` state the sheet shows
- [ ] `tests/services/test_festivals.py` — New 2026-09-25. Festival upsert rules (a None ticket link doesn't blank a festival row's link, festival_id/cineville_pass only overwritten by non-None values, explicit False sticks) plus `resolve_cineville_festival_cinema` (movie id first, then near-identical title, exact time, ignores rows at the festival itself) and `adopt_placeholder_showtime` (moves + clears room, keeps the row id through the following upsert, no-op cases)
- [ ] `tests/converters/test_festival_converters.py` — New 2026-09-25. `cineville_pass` falls back to `cinema.cineville` only when the showtime's is None; `festival` is None without festival_id and the festival cinema otherwise, on both ShowtimePublic and ShowtimeInMoviePublic
- [ ] `tests/scraping/test_cineville_festival_routing.py` — New 2026-09-25. `_persist_cineville_results_batch` at a festival venue: lands on the scraper's row at the real cinema (keeps its ticket link), else at the festival row; regular venues get no festival and cineville_pass None
- [ ] `tests/scraping/test_liff_parsing.py` — New 2026-09-25. LIFF `_parse_show` (cancelled/past skipped, end past midnight, ticket link precedence) and `_cinema_for_location` (known prefix → cinema without room, unknown → festival row with location as room), plus the title/flag/int helpers
- [ ] `tests/services/` — Service tests (most critical layer)
- [ ] `tests/converters/` — Converter tests
- [ ] `tests/scraping/` — Scraper tests
- [x] `backend/scripts/preview-digest-email.py` — Renders every digest variant (Eager/Weekly x watchlist/chosen list/deleted list) to one browsable page through the real `generate_watchlist_digest_email`, so the wording can be eyeballed without waiting for a scheduler tick. Fixture films only, no DB; the Movie/Showtime stand-ins carry just the attributes the mailer reads, and deliberately include a film with no poster and one with no Letterboxd slug
- [ ] `backend/scripts/backfill-cineville-events.py` — One-off: loads past weeks of Cineville events (default 8) into `CinevilleEvent` so the publish-timing view has history from day one; ~60 paced requests, never overwrites a live row
- [x] `frontend/public/favicon.ico` + `frontend/public/apple-touch-icon.png` — Root-path site icons, generated from `assets/images/favicon.png` with ImageMagick (ico at 16/32/48, touch icon 180x180 flattened on white since iOS composites transparency onto black). They exist because avatar fetchers (Proton Mail's sender images) request the conventional root paths rather than parsing index.html, and nginx's SPA fallback answered those with index.html under a 200
- [x] `tests/test_mailer.py` — Email shaping the digest depends on to reach the primary inbox: per-frequency subjects (Eager claims no timeframe because it has no horizon, Weekly says "this week"), never a brand prefix, and a text/plain part that keeps link URLs, drops MJML's <head>/<style>/Outlook comments, and collapses blank-line runs
- [x] `tests/services/test_seat_map_persistence.py` — The seat map is polled, stored and served: a reading stores the seats it already read, a later one replaces rather than merges, a platform that reports no seat map leaves the stored one alone, an unpolled showtime reads as unknown rather than free, and drawing the floor plan never touches a ticket shop
- [x] `tests/scraping/test_seat_floor_plan_room_match.py` — A stored floor plan must belong to the room it's filed under: the agenda feed names the room, the booking system supplies the geometry, and when a show's `screen_name` says a different room the ingest walks on to the next showtime rather than storing another room's seats
- [x] `tests/scraping/test_eagerly_host_matching.py` — Which ticket links count as Eagerly: `/tickets/<number>` is only an Eagerly link on a host in `EAGERLY_SITE_HOSTS` (AnnexCinema and De Sien sell at that path without running Eagerly, and matching them made the client promise seat counts that 404 for ever), and both the `www.` form our scrapers build and the bare form Cineville hands out must resolve to the same booking host or a cinema silently drops to sold-out-only depending on which source won the dedupe
- [x] `tests/scraping/test_tricket_seat_map.py` — Reading a Tricket seat map: positions come from the map's SVG and names from the screening resource, so neither is usable alone; a seat the resource does not name is dropped rather than drawn unmatchable. Also the one place a `screen_side` is *derived* — Tricket draws the screen line itself, and Cinecenter draws it below every seat — plus the guards that keep Studio/K's decorative map out and every seat-map host nameable
- [x] `tests/scraping/test_activetickets_seat_availability.py` — The platform sells two kinds of room through one page shape: a numbered room inlines its seat plan (count, capacity, taken map, blocked seats taken but still counted towards the room) and a free-seating room inlines nothing but the sold-out flag. Also the id-at-the-end-of-the-slug URL parsing, the unknown-host rejection, and the two states that must never read as sold out — a screening the shop no longer lists, and a page with no view-model at all
- [x] `tests/scraping/test_ticketlab_seat_availability.py` — The platform whose shops mostly never name a room: a nameless room is still identified by `util.seating.locationid` (keying on the name alone left Cinema Oostereiland, De Drom, Filmhuis Bussum, Fizi and Luxor Zutphen with no room, no stored floor plan and no seat map), a named one reports both, and the one red alert box the shop uses for "not found", "sale finished" and "sale disabled" alike drops the count — which reads 0 whatever the room holds — while keeping the room the plan is looked up by
- [x] `tests/crud/test_showtime_friends_of_friends_visibility.py` — Who FRIENDS_OF_FRIENDS actually reaches: a friend-of-a-friend only sees the status when the bridging friend is themself going/interested on that showtime, and the two opt-out directions (the owner's `shares_status` towards the bridge, the bridge's towards the viewer) gate different halves of the bridge
- [x] `tests/crud/test_showtime_friends_of_friends_display.py` — The other direction: which friends-of-friends are *listed* on a showtime the viewer is looking at, now that the viewing side is unconditional rather than a per-user opt-in
- [x] `tests/crud/test_showtime_hidden_attending_friends.py` — The friends who are already visibly attending but would lose sight of the actor's status when it goes INVITED_ONLY — the set `InviteBeforePrivateDialog` offers to invite
- [x] `tests/services/test_moderation_visibility_rebuild.py` — Blocking and unblocking rebuild the effective-visibility cache for both users' showtimes, so a block takes a status out of the other person's feed immediately instead of at the next write
- [x] `tests/services/test_default_visibility_apply.py` — Switching the account default with and without applying it to showtimes already selected: "new showtimes only" pins the mode each going/interested showtime was running under so the default moves out from under it, unselected showtimes still take the new default, an omitted flag keeps the old apply-to-everything behaviour, and explicit per-showtime choices are untouched either way, plus the `has_selected_showtimes` flag the screen skips the prompt on
- [ ] `tests/services/test_unidentified_listings.py` — A Cineville showtime at the same cinema and minute as a listing the cinema scraper couldn't identify is matched (active, upcoming, that cinema only; same-minute titles combined), exempt from the trusted-scraper cleanup, and reported with the skipped title
- [ ] `tests/scraping/test_unidentified_listing_recording.py` — LAB111 and the generic Eagerly scraper record every screening time of a film they skip for want of a TMDB match (Eagerly only its own venue's)
- [ ] `tests/services/test_tmdb_ambiguities.py` — Tied lookups listed newest first until reviewed, reopenable, only rows with a tie reviewable, and a correction through the TMDB cache tool counts as reviewed
- [ ] `tests/scraping/test_publish_timing_logs.py` — Scrape schedule (stable jittered slots that keep their order, Monday > Thursday > quiet weekdays > weekend, nothing due at night, a missed slot stays due), deletion grace in hours not runs, ListingFirstSeen only from a stream's second run, Cineville REST production/event parsing, the CinevilleEvent sweep (first sighting kept, dropped events marked gone), heatmap spreading
- [ ] `tests/fixtures/` — Test factories and shared fixtures
- [ ] Add tests for `services/me.py`
- [ ] Add tests for `services/showtimes.py` (visibility logic)
- [ ] Add tests for `crud/showtime_visibility.py` (default-mode resolution covered by `tests/crud/test_showtime_visibility_defaults.py`)
- [ ] Add tests for `crud/user.py` (time-range filtering)
- [ ] `tests/api/test_admin.py` — Admin route gating, analytics overview, movie/showtime moderation, showtime reports
- [x] `tests/api/test_anonymous_browse.py` — The browse endpoints answered without a token: catalogue in full, no personal annotations, `/me/*` still 401
- [x] `tests/api/test_client_compatibility.py` — Legacy flat mirrors match the `viewer` block, and the version gate's per-platform floors are independent
- [x] `tests/test_cinemas_yaml.py` — `data/cinemas.yaml` carries no field the seeder would silently drop, and no duplicate keys
- [ ] `tests/api/test_away_events.py` — New 2026-09-24. Window boundary for invites/friend requests; only upcoming sold-out screenings
- [ ] `tests/api/test_notification_unsubscribe.py` — New 2026-09-24. GET never changes anything, POST turns off exactly one type, seat emails turn off all three

---

## Frontend — Entry & Config (`frontend/src/`)
- [ ] `theme/tokens.ts` — The app's palette turned into Chakra semantic tokens, derived from `shared/theme/colors.ts` at load rather than copied, so a colour cannot drift between the two clients. Overrides Chakra's own `bg`/`fg`/`border` so stock components inherit dark mode instead of every screen handling it, and exposes the rest as `app.*`. It briefly also carried a web-only `rail.*` group for a tinted filter rail; the rail is a plain card again and the group is gone — there is nothing on the website now that wants a palette of its own
- [ ] `features/showtimes/day-clock.ts` — New 2026-09-20. The one day every row on a page is labelled against, as an external store (`useSyncExternalStore`). `when()` and the film kit used to read the clock themselves, cached for a minute — correct per card and wrong per page, because nothing re-renders a card just because the minute changed: after midnight a feed relabels itself *one row at a time*, as rows happen to re-render for other reasons ("currently it changes one by one if its just past midnight and you click on showtimes, that should not happen"). Now the day changes in one place and every subscriber is notified in the same batch. The value is compared by day and by whether it is past 06:00, so the store fires twice a day at most (midnight, and where tomorrow's small hours stop being "tonight"); the ticker only runs while something is subscribed. **Anything that prints a day must call `useDayClock()`**, even if it throws the value away — a memoised card without that subscription keeps yesterday's label. `PortraitTicketCard`, `FeedOverviewPanel`'s rows and the lab's `FilmRow` do
- [ ] `features/showtimes/feed-params.ts` — Every filter dimension the feed has, in one place: the URL schema, the defaults, and the mapping into the shared feed hook's arguments via the hoisted `shared/filters` helpers. The full set is declared even where no control exists yet, so building a control later is UI work with no plumbing behind it. `countActiveFilters` takes the account's own cinemas, because the rail writes a real selection into the URL as soon as a chip is touched and sitting on your usual cinemas is the resting state, not a filter — the app's `isCinemaSelectionDifferentFromPreferred` rule, now shared **2026-09-22:** `mine` ("Only my own plans") → `onlyYou`; with `status` at "all" it sends GOING+INTERESTED. Presets carry it as the app's `showtime_audience: "only-you"`, riding with the status dimension (`feed-presets.ts`)
- [ ] `features/showtimes/useFeedParams.ts` — The URL-state half of a feed, shared by the showtimes and films feeds so neither owns a private copy of the parsing and switching pages carries your filters across. Also reads the account's preferred cinemas, purely so the active-filter count can tell a chosen selection from the account's default one **2026-09-20, "it still isn't INSTANT":** now owns the optimistic overlay that `FeedFilterRail` used to keep to itself (`useOptimisticFeedParams`), so the rail's controls, `activeFilterCount` *and* the feed's query all read the one copy — the rail flipping a switch while the feed still showed the old filter's rows is what read as slow. The URL write stays deferred behind it, but nothing on screen waits for it any more: the feed keys on `params` (the overlay), not on `committedParams`, which cut the delay before the request is even sent from 325-505ms to ~90ms. Also exports `filtersKey` (a `filterIdentity` of the params — every dimension except `q`, so rows can be told apart from the previous filter's) and `isSearchOnlyLoad` (tracked with render-time refs, because an effect would be a frame late, and a frame of wrongly blanked feed). An earlier attempt kept an `awaitedFilters` string to wait out the navigation; keying the query on the overlay made it unnecessary and it is gone. **2026-09-22:** `applyParams(next)` replaces every dimension except the page's pinned ones, quick-filter style — the feed overview's "Show more"; threaded through `useShowtimesFeed`, `useMoviesFeed`, both `useAgendaFeeds` hooks and `FeedChrome`
- [x] `features/showtimes/feed-presets.ts` — Adapter between the shared preset model and the feed's URL state. The shared code applies a preset through a bag of individual setters (the app holds each dimension separately); these collect into one patch so the feed navigates once instead of putting twelve entries in browser history
- ~~`features/showtimes/useAgendaFeeds.ts`~~ — deleted 2026-09-22 with the friend agenda page; a friend's agenda is `FeedParams.friends` on the home feed
- [x] `features/showtimes/useMoviesFeed.ts` — The films feed, the same shape as `useShowtimesFeed` over the same URL state, so a screen can be written against either without knowing which it got. **2026-09-20:** optional `showtimeLimit`, passed through to the shared hook — the API sends five screenings per film unless asked and will not send more than ten (`showtime_limit`, `le=10` in `routes/movies.py`), which is the ceiling on any film card that wants to draw a timetable. **2026-09-21:** `MoviesPage` and the home feed's grouped mode now ask for ten too, ported from the lab — the row's six plate slots need it
- [ ] `features/showtimes/useActivityFeed.ts` — **New 2026-09-22.** The Activity page's rows: main-page showtimes (GOING+INTERESTED, `allCinemas`, `friendsOnly` for Friends) or `/me/agenda` with interested+invites for You — the app's exact queries. Uses `buildSnapshotTime`, now exported from `useFeedParams.ts`
- [ ] `features/showtimes/interested-elsewhere-reminder.ts` — **New 2026-09-24.** Web copy of the app's `mobile/utils/interested-elsewhere-reminder.ts`: localStorage flag (default on) + `useSyncExternalStore` hook for whether marking going offers to clear interested elsewhere
- [ ] `components/Showtimes/detail/RemoveInterestedElsewhereDialog.tsx` — **New 2026-09-24.** Web port of the app's dialog; wired in `ShowtimeStatusSection` (background lookup via `MoviesService.readMovieShowtimes`, serial NOT_GOING writes)
- [ ] `components/Settings/StatusesSection.tsx` — **New 2026-09-24.** "Going & interested" settings section holding the clear-interested switch (app moved it out of Tips the same day)
- [ ] `features/showtimes/useHeldShowtime.ts` — **New 2026-09-22.** The panel's showtime held under `["showtimes","held"]` (`HELD_SHOWTIME_KEY` in `showtime-cache.ts`, which `putShowtimeInFeeds` now also patches), so a row that leaves its list — status taken back — keeps a live panel open
- [x] `features/showtimes/useShowtimesFeed.ts` — The feed as one hook: URL state in, paged showtimes out, plus the snapshot-time refresh. Screens below it can be rearranged freely without touching data or state **2026-09-20:** returns `isReplacingRows` (`query.isPending && !isSearchOnlyLoad` — the rows on screen belong to filters no longer in force, so the page shows a loading screen instead of them; a search narrowing the list is excluded, since blanking under every letter reads worse than lagging) and passes `filtersKey` through. `isLoading`/`isEmpty` moved from `query.isLoading` to `query.isPending`: between a filter changing the key and the request for it starting, `isLoading` goes false for a frame or two with no data behind it, which left the feed blank with neither rows nor spinner. Passes `staleTime: ROWS_REUSABLE_FOR_MS` (30s) to the shared hook, so flicking a filter back re-uses the rows it already has — measured: two requests per toggle before, none after. The same three changes are in `useMoviesFeed` and both hooks in `useAgendaFeeds`. **2026-09-22:** takes an optional `feedState` (`FeedParamsState`, exported from `useFeedParams.ts`), as does `useMoviesFeed`: `MainShowtimesPage` now makes one filter state and hands it to both feeds, because each keeping its own optimistic overlay while the page read `group` off the URL made the film rows flash (redrawn for the new filters) before the ticket wall took over when the overview's "Show more" flipped grouping
- [x] `features/showtimes/showtime-cache.ts` — Writing one screening back into every cached feed that already lists it. Acting on a showtime used to `invalidateQueries(["showtimes"])`, which refetches every page of every feed — pressing "Going" a few hundred rows deep re-requested the lot, which is what made the panel feel like it reloaded the page. Untouched caches keep their identity (that prefix also covers the per-showtime availability and visibility entries, whose data is not a page of rows), and the feeds whose *membership* the viewer's own status decides — your agenda — are refetched instead, because no local edit can know a row should now leave
- [ ] `features/showtimes/useFeedOverview.ts` — New 2026-09-22. The feed overview's data from `GET /me/feed-overview` (was derived from the feed's loaded rows, so it changed as you scrolled). Sends the feed's language and cinemas, and the viewer's custom list — a saved preset followed live, or a snapshot of the filters on at the time — kept in `localStorage` (`mikino.overview.custom-list.v1`) since the account has no field for it. `paused` holds the lists while a showtime picked from them is open, so answering it does not refetch it out from under the selection. `showtime-cache.ts` also patches this cache in place and lists `["showtimes", "overview"]` as status-scoped. **Later 2026-09-22:** a custom list's unset dimensions take the viewer's defaults — an empty language means the remembered one (`readRememberedLanguages`, new export of `use-remembered-language.ts`), empty cinemas the account's usual ones (server-side). `showMoreTarget` maps each section to its "Show more": filters for custom (the list itself), friends going / selling fast (friends' status + all cinemas) and watchlist (watchlist only, feed's cinemas), a page for invites (`/pings`) and plans (`/me/showtimes`)
- [x] `features/showtimes/useShowtimeInvites.ts` — Everything the panel can do with an invite to one screening: the sent-pings query and its key, sending (batched, one request per friend because that is what the API offers), taking one back, nudging, and minting the shareable link. Extracted the moment a second section needed it — the "Watchlisted by N friends" list in the audience box invites people exactly as the app's watch popup does, and two copies of "who is already invited" would have disagreed with each other in the same card. Sending and uninviting are optimistic: inviting is a request to a *person*, and a button that sits unchanged while it flies reads as not having worked. The provisional ping's `id` is negative so nothing can mistake it for a row the server minted. **Bug fix 2026-09-19:** `sendInvites` sent a multi-friend batch with `Promise.allSettled` — all pings at once — which is exactly the pattern that deadlocks Postgres (every ping rebuilds the showtime's `ShowtimeVisibilityEffective` rows; concurrent rebuilds for one showtime deadlock, seen on staging from the app). Now one at a time, each awaited, counting failures the same way. Also exposes `sendInvitesAndWait` (`mutateAsync`) for a write that has to follow the invites — the invite-first dialog's switch to "Invited only". Same day, later: exports `uninvitedSelectedFriendsQueryKey` / `hiddenAttendingFriendsQueryKey`, and every invite/uninvite now invalidates both (being invited lets a friend see you whatever your mode, so both "who would lose sight of you" lists change).
- [x] `features/showtimes/useShowtimeSelection.ts` — Setting your status on a screening, and your seat in it. Optimistic, because the round trip includes a best-effort read of the cinema's ticket shop and a button that sits inert that long reads as broken; the server's own copy of the row replaces the guess when it lands, and a failure puts the old row back and says so 2026-09-19: `setStatus` takes an optional `waitFor` promise that holds back the *write* but never the paint — used by the "Let these friends know?" flow, whose invites must land before the status write (concurrent rebuilds of one showtime's visibility rows deadlock Postgres).

- [x] `auth/session.ts` — Who is here, answered synchronously: the token is read once before the first route renders and cached as module state, so `useIsSignedIn()` never makes a screen wait a microtask to decide whether to show account UI. Login/logout reach it through the storage adapter in `main.tsx`, so nothing has to remember to announce a sign-in
- [x] `auth/useSession.ts` — `useIsSignedIn`, `useRequireAccount` (gate the *action*, sending guests to `/login` with a return path) and `useAccountQueryEnabled` (the `enabled` every `/me/*` query needs so a guest does not 401 on a refetch loop)


- [ ] `main.tsx` — App entry point, React Query setup, Axios interceptors
- [x] `theme.tsx` — Chakra UI theme: app `semanticTokens` (light+dark) from `theme/tokens.ts`, plus `ui.main` + button recipe. Deliberately does not restyle scrollbars: a global thin/transparent-track override was tried and reverted, because over a transparent track an unhovered bar takes its colour from whatever scrolls past it, so every scroller on the page read as a different colour. The browser's own bars already follow the mode — `next-themes` sets `color-scheme` on the root 2026-09-19: added the `rail-chip-in` keyframe (scale 0.85 + fade) beside `panel-enter`, for the rail's filter chips.
- [x] `theme/tokens.ts` — Generates Chakra `app.*` semantic color tokens from `shared/theme/colors.ts` (matches the mobile palette, no drift)
- [x] `constants.ts` — `TOP_NAV_HEIGHT` (the site nav), `TOPBAR_HEIGHT` (a page's optional secondary bar) and the notice-banner offset. The `SIDEBAR_*` family is gone: a fixed rail needed four separate layouts to agree on its width through a CSS variable, and a bar in the flow needs none of them to know anything
- [x] `app-install.ts` — Store listing URLs, the Play install-referrer payload, the phone-platform sniff and the official store badge assets (`public/assets/images/app-store-badge.svg`, `google-play-badge.png` — Apple's and Google's own artwork, unmodified, which is why the two are sized to different heights: Google's PNG bakes its required clear space into the image). iPadOS 13+ needs the `Macintosh` + `maxTouchPoints` check; `IOS_APP_STORE_URL` stays null until the App Store listing is live
- [ ] `types.ts` — Custom TypeScript types (beyond auto-generated API types)
- [ ] `utils.ts` — Frontend utility functions

---

## Frontend — Routes (`frontend/src/routes/`)

- [ ] `__root.tsx` — Root layout (wraps entire app, providers)
- [x] `_layout.tsx` — The signed-in-or-not shell: session primed in `beforeLoad` without ever redirecting (the feed is open to guests), then `TopNavBar` as a row above a scrolling outlet, plus `BottomNavBar` on a phone. The nav being a row rather than a fixed overlay is what frees every layout below from carrying an offset for it
- [ ] `_layout/index.tsx` — Home / dashboard
- [ ] `_layout/movies.tsx` — Movies listing page
- [ ] `_layout/pings.tsx` — No longer a page (2026-09-22): kept because the app and the invite-link page point at `/pings`, it redirects to the home feed (replacing itself in history) and opens the notification panel. A guest gets the `RequireAccount` gate
- [ ] `_layout/activity.tsx` — **New 2026-09-22.** The app's Activity tab (`?mode=all|you|friends`, default omitted). Registered by hand in `routeTree.gen.ts` (generator broken repo-wide)
- [ ] `_layout/friends.tsx` — Friends page. **2026-09-22:** `?mode=discover` opens on Find people (`Friends/friends-modes.ts`), as the app's `?tab=users` does
- [ ] `_layout/settings.tsx` — Settings. **2026-09-23:** open to guests (no `RequireAccount`), wrapped in `DeferredPage`, draws `Settings/SettingsPage.tsx`; a section is addressed by the hash (`/settings#privacy`)
- [ ] `_layout/me/showtimes.tsx` — **2026-09-22:** no longer a page — redirects to `/?mine=true&status=interested` (the home feed's "Only my own plans") so old links still land somewhere. Kept as a file rather than deleted because the route generator is broken and `routeTree.gen.ts` would need hand-editing
- [ ] `_layout/$userId/showtimes.tsx` — **2026-09-22:** redirect only, to `/?friends=<id>` (the home feed narrowed to that friend, headed by `Feed/FeedSubjectHeader`). Kept for old links
- [ ] `login.tsx` — Login page
- [ ] `signup.tsx` — Signup page
- [ ] `pick-username.tsx` — New 2026-09-23. Username step for a new Apple/Google account (outside `_layout`); does signup's hand-over (guest cinemas, system theme, intro). `_layout.tsx` sends any nameless signed-in account back here. Registered by hand in `routeTree.gen.ts`
- [ ] `recover-password.tsx` — Request password reset
- [ ] `verify-email.tsx` — New 2026-09-23. Where the mailed "Confirm your email" link lands (`?token=`); POSTs `/users/verify-email` once via a query, then confirmed / dead link (resend when signed in, log in otherwise) / couldn't reach. `AuthShell` frame with its new `icon` slot. Also in the AASA paths so the app opens it. Registered by hand in `routeTree.gen.ts`
- [ ] `reset-password.tsx` — Set new password (from email link)
- [ ] `movie.$movieId.tsx` — Movie detail page
- [x] ~~`beta.tsx` — Beta signup page~~ — deleted 2026-09-23 with the root beta banner
- [ ] `cinema-showtimes.tsx` — Cinema showtimes wrapper
- [ ] `cinema-showtimes.$cinemaId.tsx` — **2026-09-22:** redirect only, to `/?cinemas=<id>`; the app's cinema links still land
- [ ] `friend-showtimes.tsx` — Friend showtimes wrapper
- [ ] `friend-showtimes.$friendId.tsx` — **2026-09-22:** redirects straight to `/?friends=<id>` (was a hop through `/$userId/showtimes`)
- ~~`friend-groups.tsx`~~ — deleted (friend groups retired; web parity pending)
- [ ] `add-friend.$receiverId.tsx` — Add a friend by ID (deep link)
- [ ] `ping.$showtimeId.$sender.tsx` — Ping deep link handler
- [ ] `forbidden.tsx` — 403 page
- [ ] `routeTree.gen.ts` — Auto-generated (do not edit manually)
- [ ] `_layout/admin/index.tsx` — Superuser analytics overview page
- [ ] `_layout/admin/movies.tsx` — Superuser movie-record / TMDB-cache editor page
- [ ] `_layout/admin/showtimes.tsx` — Superuser showtime moderation page
- [ ] `_layout/admin/reports.tsx` — Superuser showtime-report triage page
- ~~`_layout/lab/cards` (`_layout/lab.cards.tsx`)~~ — deleted 2026-09-20, "remove the other options beside ticket wall, i only want to use ticket wall": with one card left there is nothing to compare. Its `routeTree.gen.ts` entries were hand-removed, same as `/lab/filters`'s (the generator throws repo-wide)
- ~~`_layout/lab/films` (`_layout/lab.films.tsx`)~~ — deleted 2026-09-21 (the design won and moved into `Movies/cards/`, drawn by `Feed/MovieFeedPage.tsx`; hand registration removed from `routeTree.gen.ts` too, the way `/lab/cards` and `/lab/filters` went before it)

---

## Frontend — Components
- ~~`Showtimes/CinemaShowtimesPage.tsx`~~ — deleted 2026-09-22: one cinema picked on the home feed *is* the page now (`Feed/FeedSubjectHeader`)
- ~~`Movie/MovieSynopsis.tsx`~~ — The film's description and lead cast, both of which the API has always returned and the website rendered nowhere. Deleted 2026-09-21: `Movie/MovieDetail.tsx` prints both, the synopsis unclamped and eight cast names
- [x] `Friends/UserModerationMenu.tsx` — Block and report on any user row. Blocking does not confirm, because the moment you want to block someone is not the moment to be asked twice; the Settings page's Blocked accounts section (`Settings/PrivacySection.tsx`) is what makes that safe. Swallows the click so opening it inside a friend card's link does not navigate **2026-09-22:** optional `onRemoveFriend` puts "Remove friend" at the top of the menu (the caller owns the confirm); items carry icons
- ~~`UserSettings/`~~ — the whole tabbed settings folder (`Appearance`, `BlockedAccounts`, `ChangePassword`, `DeleteAccount`, `DeleteConfirmation`, `EmailVerificationNotice`, `LetterboxdLists`, `Notifications`, `UserInformation`, `WatchlistDigest`) deleted 2026-09-23, replaced by `Settings/` below
- [ ] `Settings/SettingsPage.tsx` — **New 2026-09-23.** The app's Settings tab as one page: every section stacked, with a sticky index on the left (≥992px) that tracks the scroll (IntersectionObserver band, plus "last on screen" once scrolled to the end) and jumps to a section. Guests get Appearance, Cineville and About plus a sign-in offer. **2026-09-23 later:** index tracking is a reading line 40% down the scroll box (last section whose top passed it); a click in the index pins the picked entry for the whole smooth scroll (unpinned 150ms after scrolling stops), so the highlight no longer steps through the sections in between, and `.st-sections` has `padding-bottom: 60vh` so the bottom section can reach it; Letterboxd uses `PanelIcon.letterboxd` (the film row's logo); the danger zone shows for superusers too, button greyed with "Admin accounts can't be deleted from here" Never touches the URL hash on a jump: that wakes the router's own `scrollIntoView`, which scrolls the layout's `overflow: hidden` grid and hides the top nav
- [ ] `Settings/settings-sections.ts` — Section ids, index labels/icons, and which ones a guest gets
- [ ] `Settings/settings-controls.tsx` — `SettingsSection`, `SettingsRow`, `SettingsField`, `Segmented` (app's SegmentedControl), `Switch` (AppSwitch), `ConfirmDialog` (app's ConfirmDialog; `closeOnConfirm={false}` for one that reports back)
- [ ] `Settings/settings.css` — Plain `st-*` classes over Chakra vars, the Friends/Activity idiom
- [ ] `Settings/page-scroll.ts` — Scrolls only the page's own scroll box; never `scrollIntoView` here (see above)
- [ ] `Settings/ProfileSection.tsx` — Username + email, current password required (or "add a password first"), verified badge with the resend dialog and a re-check button, "confirm your new email" after an address change
- [ ] `Settings/PasswordSection.tsx` — Change or add (social-only accounts) a password
- [ ] `Settings/LetterboxdSection.tsx` — Username (emptied = unlink), counts, independent watchlist/watched sync with backend cooldowns and failure lines, plus list add/re-sync/remove (curated shown, not removable)
- [ ] `Settings/LetterboxdNotFoundWarning.tsx` + `letterboxd-warning.css` — New 2026-09-23. Red "There is no Letterboxd account called …" box under the username field, in Settings and the intro, when `UserMe.letterboxd_account_not_found` (a definite 404 only). Mirrors the app's `ui/LetterboxdNotFoundWarning.tsx`
- [ ] `Settings/NotificationsSection.tsx` — Off/Push/Email per notification (optimistic, via `buildDeliveryUpdate`), email gated on verification, "Notify on new films" switch with the no-username warning and Advanced settings
- [ ] `Settings/DigestSources.tsx` — Port of the app's `WatchlistDigestSourcesSection`: up to 5 sources, summary + edit, local draft, Eager/Weekly info from the backend, list chips + add-by-URL, cinema chips + a custom picker dialog on `Feed/CinemaChecklist`
- [ ] `Settings/EmailVerificationDialog.tsx` — The app's `EmailVerificationRequiredDialog`
- [ ] `Settings/PrivacySection.tsx` — Default status visibility (inline, with the "apply to your showtimes too?" question) and Blocked accounts
- [ ] `Settings/AppearanceSection.tsx` — Light / Dark / System
- [ ] `Settings/CinevilleSection.tsx` — Card number kept in this browser, barcode dialog (Code 128 from `shared/cineville/code128.ts`), copy code, auto-copy on ticket links. No shortcut-button switches: the web has no floating pass button
- [ ] `Settings/AboutSection.tsx` — TMDB / affiliation notices, privacy and support links
- [ ] `Settings/AccountSection.tsx` — Log out (confirmed) and the collapsible danger zone (for superusers the delete is greyed out with the reason, since the backend refuses)
- [ ] `features/cineville/cineville-card.ts` — **New 2026-09-23.** The web's Cineville card store in localStorage (digits + auto-copy), synced across tabs; `copyCinevilleCardForTicketLink` is called from `Showtimes/detail/SeatAvailabilityPanel.tsx`'s ticket row
- ~~`Friends/FriendStatusSharingToggle.tsx`~~ — deleted 2026-09-22; the per-friend opt-out is `Friends/friend-controls.tsx`'s `FriendVisibilityChoice` (the app's Always / Only when invited) on the Friends page and the feed's friend header

- [x] `Feed/FeedLayout.tsx` — The shell every feed screen sits in — toolbar across the full width, then filter rail / list / detail panel — and the only file that positions anything. On a desktop the whole content area is one scroll container and everything that stays put is `position: sticky` inside it, replacing the per-column `overflow: auto` that put a second scrollbar beside the showtimes (Cineville's filmagenda is the reference). The panels pin *below* the toolbar, which is why its height is measured with a `ResizeObserver` and published as a CSS custom property rather than assumed: it grows a presets row when signed in and a header on a cinema's page. A custom property and not state, because the panels are its siblings and a resize should not go through React. Nothing horizontal is left to clear since the nav moved to the top; the fixed bottom bar on a phone still is. Panel widths step down by breakpoint so the list between them does not become the ribbon `LIST_MAX_WIDTH` prevents at the other end — grown a second time on request (`DETAIL_WIDTH` up, `LIST_MAX_WIDTH` down from 900), with the growth back-loaded onto `2xl` rather than spread evenly: an even step across every breakpoint left a plain 1280px window so little for the list that a title truncated to its first letter, and the room the panel wanted was there on a wide monitor already. The detail column opens and closes rather than appearing and vanishing: it stays mounted for the length of the close so there is something to animate, and carries its own leading gap, because a zero-width flex item still gets the row's `gap` and left a 24px hole that snapped away at the end. It runs on a four-phase machine — `closed`, `arming`, `open`, `closing` — built around the fact that rendering the panel is expensive, 43-110ms of blocked main thread for a poster, five sections and a handful of queries. A CSS transition keeps its own clock while the main thread is blocked, so a column whose width is flipped in the same commit that renders the panel loses however long that render takes: nothing paints, the clock runs on, and the first frame the eye gets is already a third of the way in. Measured reopening mid-close, the column sat at 112px for 62ms and reappeared at 213px, a 101px jump in one frame — the lurch that read as the panel snapping, in exactly the band that gets reported, too late for the close to be near full width and too early for it to have finished. `arming` is the commit that renders the panel, with the column pinned to an exact size and its transition off so that render cannot consume any of the animation; two frames later the pin comes off and the column travels. One frame is not enough, because an effect runs before its commit has necessarily painted. A mid-close reopen pins to the last size the column was *painted* at, sampled every frame while closing, and not to what it measures at the click — those differ by exactly the blocked render, and the difference is the jump. The unmount is scheduled from the effect that reacts to the click rather than from one keyed on the phase: a phase-keyed effect only clears the timer in the commit that leaves `closing`, and a reopen in the last twenty milliseconds of the close does not get that far before the timer comes due, so it fired in the gap and took a panel that had just been armed to `closed` — reopening at a 200-220ms gap left the panel shut for good. The card inside is full width while open and pinned to the column's width only while it shuts: a fixed width the whole time meant a panel long enough to scroll had its own scrollbar cut a strip off its right edge, while a card at full width the whole time would be squeezed to nothing on the way out instead of leaving at full size. The gaps are margins on the asides themselves and never on a wrapper: a wrapper would become the sticky element's containing block and, since these columns are `align-self: flex-start`, would be exactly as tall as the panel and silently end the pinning. Changing the web layout is an edit here and nowhere else, which is the point `grid` mode (the ticket wall): the list takes the whole column and the detail column is always there at full width, empty until something is selected, so opening the panel never reflows the grid under the pointer — the reflow also ran on every frame of the column's animation **2026-09-20:** `fillWidth` is `grid` without the column — the list takes the whole width and no detail column is reserved at all, for a feed with no panel to open (the films feed: a film's detail is its own page). `FeedPageShell` passes it through **2026-09-22, collapsible rail:** the rail folds to a 52px strip (`RailCollapse.tsx`) by the reader's choice (round handle centred in the gap on its right edge; remembered in localStorage `mikino.feed.railCollapsed.v1`) or by itself when `minListWidth` (ticket wall: one ticket) no longer fits beside a full rail and the detail column — worked out from the row, a hidden `w={RAIL_WIDTH}` ruler and the detail column, never the list (it would oscillate). The detail column never gives way (the showtime panel lives there). Folded for lack of room, the strip opens the rail `floating` over the list (`SidePanel` trailing margin of minus its width, zIndex 4); a pointerdown inside the layout but outside rail/strip closes it (portalled sheets are outside, so ignored). New props `activeFilterCount` (strip badge) and `railFooter(variant)` (under the rail card, or compact in the strip). Screenshotted docked at 1600px and auto-folded at 950px; floating open/close not clicked through. The floating rail's `zIndex` is a stacking context that would trap the rail's raise-above-the-cinema-sheet (2100), so the rail marks itself `data-raise-panel` while raised and a floating `SidePanel` follows it up via `:has()` (`RAISED_PANEL_ATTRIBUTE`/`RAISED_PANEL_Z_INDEX`, exported here and used by `FeedFilterRail`). Verified with playwright at 950px: floating rail bright beside the sheet; the tickets-per-row card under it lifts too, as it shares the column **2026-09-22:** the detail column's open/close state machine (arming/open/closing, double rAF, painted-width pin, timings) moved out to `Feed/useDetailColumn.ts` so the film page can share it; `SidePanel` takes its column/card styles from there
- [ ] `Feed/useDetailColumn.ts` — **New 2026-09-22.** `useDetailColumn(hasDetail)` plus `detailColumnStyle` / `detailCardStyle`: the docked showtime panel's open/close animation, shared by `FeedLayout` and `Movie/MoviePage`. Differs from the old inline copy in one way: a column already open on its first render (a page loaded with a screening selected) no longer animates in from 0
- [x] `Movies/cards/` — The films feed's card, ported control-for-control out of the lab (`/lab/films`, deleted) into production 2026-09-21: one row, its plate, and the showtimes feed's own panel beside it. **The row** (`FilmRow.tsx`) is one film to a line — the poster at its own 2:3, the tear down the middle, and beside it the title, year/runtime/director, the cast, two lines of synopsis, your Letterboxd marks, six screenings as plates and three buttons. It is the whole feed now: the wall of tickets this lab started with is deleted ("remove the wall option for movies, the rows are better"), along with `FilmCard.tsx`, the Wall/Rows switch, the venue cap and its patterns, the legend and every `.fc-film*` rule. The ticket is the showtime wall's turned on its side — `.fr-film__seam` is a **zero-width flex item** between the two columns whose `::before` paints `mk-wall__seam`'s five gradients rotated ninety degrees, so the tear lands wherever the poster ends with no width written in two files. **Nothing is clickable but the things that do something**, also on request: the poster and "All showtimes" go to `/movie/$movieId`, a plate opens that screening in the panel, "Letterboxd" opens the film there (its slug, or a search when it has none — the movie page's own fallback), and "Share" copies the film's link and says "Copied" where it stands rather than in a toast. A row-wide click target swallowed all of that. The row's cursor is `auto` (2026-09-22): the browser's own rule, I-beam over the letters and the arrow everywhere else. It was `default` with `cursor: text` put back on each line of prose, and since each line is a block as wide as its column, the I-beam showed over the empty end of every short line too ("really only have it become the text cursor ON the text, just like regular text"). Clicking anywhere that isn't a plate or the detail panel itself (`[data-feed-side-panel]`) closes the panel, alongside the plate's own click-again-to-deselect; a plate's click handler stops its propagation so the two never fight. **The plate** (`FilmPlate.tsx`) is the unit of action: the venue in its own colour across the top, the day written out with its month ("Tonight", "Sat 26 Sep"), the start time — the end time was dropped on request, freeing the width for a larger clock — the friends who picked that screening standing on the bottom-left edge (two and a tally, a little larger; hovering opens the showtime wall's own list via `AudienceHoverCard`), how full it is in the bottom-right corner (`isBusySeatAvailabilityLevel` — the three levels that cost you a ticket, nothing calmer, now sitting a few px lower so it doesn't crowd the time above it), and your own going/interested as the plate's *fill*, since a green row would say you are going to this film while leaving you to find out which night. Six slots, one line — the sixth becomes a `.fc-plate--more` tile ("+N more", to `/movie/$movieId`) whenever a film has more screenings than fit, so the row never wraps to a second line; it reads as a different kind of plate on request — a darker fill (`color-mix` toward black) and a dotted border, rather than the same muted background every other plate starts from. The venue band is a fixed **one** line's worth, so the clocks align across the row — it grew to two lines for a day so three real cinemas fit whole ("Cinema Oostereiland", "Filmtheater Hilversum", "Filmtheater Voorschoten"), then those three were shortened in `backend/data/cinemas.yaml` instead (old names kept as `aliases`, so search and Cineville ingestion still find them under them) and the band went back to one line, on request; a name that long list doesn't cover now ellipsizes rather than wrapping or growing the plate. Selected drew a halo and a lift for a while; both are gone on request ("remove the green glow... make it look like it does when you hover") — `.fc-plate--on` now sets the exact same inset ring hovering does, and the clock staying `app.tint` is the only thing left saying which plate the panel is open on. The row itself used to carry a matching tinted outline (`.fr-film--on`, and the `isSelected` prop that drove it) while one of its screenings was selected; removed outright on request ("remove the green border on the movie row") rather than swapped for something quieter, since the plate's own ring already says which screening is open — a second green outline one level up was the same fact twice. `FilmCardProps` lost `isSelected` with it, since nothing else read it. The going/interested fills carry no border either, and neither does the plate itself by default — hover (and selection) draw an inset `box-shadow` rather than a `border-color`, because a real (even transparent) border leaves a 1px gap the fill doesn't paint under, which showed as an unwanted hairline of the page's own colour around every toned plate. The venue band kept its own border, though, and it now runs on **all four sides** rather than just the bottom, on request ("make the one pixel separator of the cinema go around the cinema part") — the band reads as its own chip now rather than a colour with a line under it. `.fr-film__info` went from `overflow: hidden` to `visible` for the same family of bug: it clipped a selected plate's halo on the side nearest the poster, since that column has no left padding and the leftmost plate sits flush against it — nothing there still needs the clip, since every truncated line (`mk-ellipsis`, the title's own line-clamp) already clips itself. **Alignment follow-up 2026-09-21**: the plate's venue name and the three `.fr-act` button labels (Letterboxd, All showtimes, Share) both sat visibly high against the icon beside them — a no-descender line still has its line-box's descender space split evenly by flex centering, leaving the actual ink above the box's true middle. `.fc-plate__label-name` and the buttons' new `.fr-act__label` span (text used to be a bare child next to the icon, now wrapped so it has something to nudge) both carry a small `translateY` down. Confirmed with a scaled screenshot with a centre guideline drawn through it, after an unscaled DOM-metrics pass had given a false "already balanced" reading — trust the render, not the box-model math. **The column beside the feed is the showtimes feed's own** — `ShowtimeDetailPanel` on the screening you pressed, `FeedOverviewPanel` (fed a `ShowtimePublic[]` flattened out of the loaded films) while nothing is picked; a film panel of the lab's own lived here for a day and is deleted on request. `film-card-kit.tsx` holds the derived facts and the poster; `film-cards.css` is the one stylesheet (plain classes — a feed draws these hundreds of times); `sample-films.ts` is the no-backend fallback, carrying friends on individual screenings, a going and an interested one, seat levels, end times derived from each film's runtime (still used by the showtime panel's own "ends at", just not printed on the plate any more), a deliberately overlong venue name to prove the ellipsis, a 00:30 screening for the "Tonight" rule, and the watched/watchlisted marks the API does not send yet. With samples up a plate only selects, since the showtime panel's buttons would write to a screening that does not exist (`FilmCardLab.tsx` and `sample-films.ts` were the lab's own scaffolding and did not move — deleted with the route). **Not done, and the one thing the row is faking:** `MovieSummaryViewerState` carries `going` and the two friend lists and *nothing about the viewer's own Letterboxd state*, although the feed's filters (`watchlistOnly`, `hideWatched`, `watchedOnly`) prove the backend knows both. `watchOf` in the kit reads `viewer.watchlisted` / `viewer.watched` and defaults to false, so the marks light up the day those two fields are added to that schema and its converter — batched per page like the friend lookups beside them, since a per-movie query there is a query per row. **Moved into production 2026-09-21**, ported against the lab's own source rather than re-derived from `Feed/MovieFeedPage.tsx`'s old shape: the old `Movies/MovieCard.tsx`-based row and its whole exclusive chain (`MovieInfoBox.tsx`, `MoviePoster.tsx`, `CinemaBadges.tsx`, `MoreCinemas.tsx`, `MovieTitle.tsx`, `OriginalTitle.tsx`, `ShowtimeInfo.tsx`, `ShowtimeList.tsx`, `MoreShowtimes.tsx`) are deleted, since nothing else referenced any of them. `MoviesPage.tsx` and the home feed's grouped mode now both ask `useMoviesFeed` for `showtimeLimit: 10`, same as the lab did, so the row's six plate slots (plus the "+N more" overflow tile) actually have screenings to fill them. **2026-09-22, more faces** ("allow some more user profile pictures to show up, there is plenty of space"): a plate's bottom edge now holds up to **five** circles — faces, or four faces and a "+N" — and **four** when the plate carries the seat mark, whose corner starts at 81px on the narrowest (96px) plate while five circles end at 82.5px (`MAX_CIRCLES` / `MAX_CIRCLES_BESIDE_SEAT_MARK` in `FilmPlate.tsx`). Everyone shows when everyone fits, since a "+1" takes the room of the face it hides. Was two and a tally. `Feed/FeedOverviewPanel.tsx`'s "Friends are going" rows went from three faces to five (the ticket wall already drew five)
- ~~`Movies/Movies.tsx`~~ — deleted (the films list is rendered by `MoviesPage` inside `FeedLayout` now. Took a window-based virtualizer with it, which could not have been working: `_layout` scrolls an inner Box, not the window)
- ~~`Movies/MoviesTopBar.tsx`~~ — deleted (replaced by the shared `Feed/FeedToolbar`, which both feeds use)
- ~~`Showtimes/Showtimes.tsx`~~ — deleted (every showtime feed renders through `Feed/ShowtimeFeedPage` now)
- ~~`Movies/MoviesContainer.tsx`~~ — deleted (width and centring belong to `Feed/FeedLayout`, which is the one place the web layout lives)
- ~~`Pings/PingsPage.tsx`~~ — deleted (superseded by `Notifications/NotificationsPage.tsx`, which shows the merged feed the app's Activity tab does -- invites are one source in it)
- [ ] `Activity/ActivityPage.tsx` — **New 2026-09-22.** The app's Activity tab on the web: All / You / Friends as a segmented control, rows grouped under a sticky day gutter, a reserved docked side column (showtime panel, or `ActivitySummary`). Not in `FeedLayout` — no rail, no toolbar; a plain padded box like the film page
- [ ] `Activity/ActivityRow.tsx` — **New 2026-09-22.** One screening as an agenda line: time | poster | film | where | who, friends named per status. Reuses `card-parts` pieces (`AvatarDot` exported for it)
- [ ] `Activity/ActivitySummary.tsx` — **New 2026-09-22.** The side column while nothing is selected: next plan, unanswered invites, most active friends — all from `GET /me/activity/summary` (server counts over the whole list, not loaded rows)
- [ ] `Activity/activity-days.ts` — **New 2026-09-22.** Groups rows by evening (before 04:00 counts as the night before)
- [ ] `Activity/activity-modes.ts` — **New 2026-09-22.** The three modes, their copy, and `?mode=` parsing
- [ ] `Activity/activity.css` — **New 2026-09-22.** All of the above's styling; folds on the list's width via a container query
- ~~`Notifications/NotificationsPage.tsx`~~ — deleted 2026-09-22: notifications are a floating panel under a bell in the top nav rather than a page and a nav tab. Replaced by the four files below
- [ ] `Notifications/NotificationBell.tsx` — The bell (signed-in only; closes the tab row on desktop behind a hairline, sits by the account on a phone) with the unseen-count badge, and the Chakra popover the panel floats in. The popover drops from the bell and slides left (`crossAxis` offset, re-applied via `reposition` while open) to end 16px short of the page's right-hand `[data-feed-side-panel]` column, and pointer/focus moves into that column don't close it. Prefetches the list on pointer-enter/focus
- [ ] `Notifications/NotificationPanel.tsx` — The panel's contents: header with "N new", New/Earlier sections, skeleton + empty states. Mounted only while open, so the list is fetched/polled only then. Opening marks both notifications and pings seen; what was unread at open stays "new" until close. Dismiss/accept/decline remove the row optimistically
- [ ] `Notifications/NotificationRow.tsx` — One row: actor avatar with the type's icon as a corner badge (seat alerts, which nobody caused, lead with the icon circle), same MaterialIcons glyphs and accents as the app's row, wording from `shared/notifications/feed-copy`. The whole row is a stretched link (movie page with `?showtime=`, or Friends) so its buttons stay valid; a plain click on a screening opens it in the current page's side panel via `showtime-panel-slot` instead, leaving the notification panel open. Dismiss replaces the time on hover
- [ ] `Notifications/notification-panel.ts` — Module-level open/close store (same shape as `Feed/feed-search-slot.ts`), so the feed overview's invites "Show more" and the `/pings` redirect can open the panel without a context over the whole layout
- [ ] `features/showtimes/showtime-panel-slot.ts` — Module-level slot where a page with a showtime side panel (showtimes feed, films feed, Activity, film page) registers "open this screening here"; `openInShowtimePanel` returns false on pages without one (Friends, Settings) so the notification row falls back to the film page. Screenings not in the page's own list are shown from `useHeldShowtime`
- [ ] `Feed/FeedPresets.tsx` — Rewritten 2026-09-19 as a 1:1 port of the lab's `QuickFilters` (the user, after an interim version added favourite/delete icons and a different layout: "it should be 1 to 1 a perfect carbon copy... dont add anything dont remove anything"). Heading and "Save current filters" share one row (disabled until `activeFilterCount > 0`, a new required prop from `FeedFilterRail`); below it, either a muted "None saved yet" line or a `flex-wrap` row of squared, apply-only chips — never a sideways scroll, never one full-width row per preset. **Favourite and delete are gone from this screen** — the lab never drew them on a chip, so per the carbon-copy instruction they aren't rendered here either; `useDisplayPresets`'s `remove`/`setFavorite` are simply unused now. If that capability needs to come back, it needs a new spot designed for it, not a silent re-add to the chip. **2026-09-20: chips ported the app's `PresetButton` behaviour** — a new `PresetChip` component plays the same middle-out flash (`preset-chip.css`'s `mk-preset-chip-flash`, a CSS translation of the app's `FLASH_GROW_MS`/`FLASH_HOLD_MS`/`FLASH_FADE_MS` timings and gradient-overhang math) on click, then dims to 0.45 opacity and disables itself once `presetChangesNothing` (shared) says applying it again would do nothing — mirroring `SavedPresetChips`' `isSatisfied`. `applyContext` (new, `useMemo`'d) resolves `currentCinemaIds` the same way the pill itself does: `params.cinemas` if set, else the account's `useFetchSelectedCinemas`. Untested in a browser
- [ ] `Feed/SavePresetDialog.tsx` — New 2026-09-24: the "Save quick filter" dialog, split out of `FeedPresets.tsx` and rebuilt to match the app's `SavePresetDialog` control for control (explainer line, name field, folded "Partial filters" box of checkable pills from shared `summarizeCurrentSelections`, equal-width Cancel/Save). Replaces the old plain Chakra dialog with its single "include cinemas" checkbox. Untested in a browser
- [ ] `Feed/preset-chip.css` — New 2026-09-20, alongside `FeedPresets.tsx`. Plain CSS (not Chakra style props) for `PresetChip`'s press flash and dim transition, following `Showtimes/cards/card-parts.css`'s pattern of styling against `--chakra-colors-*` custom properties. Untested in a browser
- [x] `Feed/ShowtimeFeedPage.tsx` — Every showtime feed on the website, rendered once: home, a cinema's programme, your agenda, a friend's agenda. They differ only in where their rows come from and what they say when empty, so a change to how a feed looks happens here instead of four times with three forgotten Now draws a detached card variant from `Showtimes/cards/`: the selection ring is urgent and the panel renders from a deferred copy; new pages render from a deferred copy of the rows (with loading held until they catch up, or the sentinel ran away); cards sit in `content-visibility: auto` cells; a grid is capped at 5 columns. In a grid the reserved detail column shows `FeedOverviewPanel` while nothing is selected, and a selection resolves from your next plans as well as the loaded rows **2026-09-19:** rows carry `.mk-feed-item` and a per-row `animationDelay` from `useFeedEntranceDelays` (the app's `FeedItemEntrance`). And `cellStyle`'s `containIntrinsicSize` no longer adds `CELL_BLEED * 2` — it sizes the content box, so the padding was counted twice and every undrawn card stood 48px too tall, shrinking the page as cards reached the screen (the jump at the foot of the feed). **2026-09-20, real bug report ("hangs on the wrong set of showtimes for a moment"):** a filter change is a brand new query with nothing cached, so `feed.showtimes` drops to `[]` and `feed.isLoading` goes true in the same render — but `rows` was always the *deferred* copy of `feed.showtimes`, and `useDeferredValue` keeps painting the old value until React gets an idle moment to catch up, so the old (now-wrong) cards sat on screen, sometimes under the loading spinner, until that idle render landed. `rows` is now `NO_SHOWTIMES` (a stable empty array) outright while `feed.isLoading`, bypassing the defer, so the cards clear in the same commit the loading screen appears in; `isDrawingRows` (pagination's own defer-lag flag, unaffected) is computed from the underlying `deferredShowtimes` so infinite-scroll gating and the footer spinner keep their old behaviour. Verified in headless Brave: timestamped the URL commit, the loading screen and the card grid's first row text per frame — no frame ever showed both the spinner/empty state and the previous filter's cards **2026-09-20, second report ("it makes the OLD showtimes appear and right after the NEW ones"):** the deferred list is now deferred *with the filters it belongs to* (`{ key, rows }` against `feed.filtersKey`), because a lagging copy is otherwise indistinguishable from the current one — on an already-cached combination the rows arrived with no loading state at all and the previous filter's wall flashed back up in between. A copy whose key no longer matches is never painted. The page also owns the loading-screen decision now (`isLoadingRows`, passed down): up whenever there is nothing to draw and the feed is not genuinely empty, which also covers the frame or two where the deferred copy is still behind a list that has arrived. **2026-09-20, "i only want to use ticket wall":** the card variants are gone — the page draws `PortraitTicketCard` (memoised here now) directly, always as a grid, and the dev switcher, the `isGrid`/`variant` branching, the list `Flex` fallback and the per-variant `cellStyle(intrinsicHeight)` went with them (`CELL_STYLE` and `CARD_INTRINSIC_HEIGHT` are constants). `FeedLayout`'s `grid` prop stays, since the movie feed is still a list **2026-09-22, tickets per row:** the wall is now exactly 2, 3 or 4 columns (default 3) — or 5 with the rail folded — as the reader picks on `WallColumnsControl`, under the filter rail via `railFooter`. The grid is its own `TicketWall` component so it sits inside `FeedLayout`'s `RailFoldedProvider`; it wraps the grid in an `inline-size` container publishing `--mk-wall-columns` and `--mk-wall-gap` and `--mk-wall-margin`. **Gaps:** 12px (the original) while a ticket's share of the wall (`TICKET_SHARE` = 100cqw / N) is ≤240px — the default 4-beside-the-rail at 1920 — then +7% of every extra pixel, capped 48px; rows get only 30% of that growth (`--mk-wall-row-gap`, user: vertical gaps smaller). **Margins:** side padding on the grid of 40% of every pixel of share past 360px, times N — most of the extra room goes here rather than to the gaps (user: 2 columns full screen had 'WAY too large posters', then 'more of the gap increase should go to the margins'). Measured (ticket/gap/row gap/margin): 1920 4-open 209/12/12/0 (unchanged), 3-open 280/15.5/13/0, 2-folded 391/38/20/200, 2560 2-folded 450/48/23/456. `CARD_INTRINSIC_HEIGHT` = 16px + 88×`TICKET_UNIT` + 1.5×`TICKET_WIDTH` (ticket width floored at the 168px minimum for when a row has dropped one); estimates within ~1% of the real height, each a share of the width, so tickets resize with the choice; the 5-column cap and 240px card cap (`GRID_MAX_COLUMNS`, `GRID_MAX_CARD_WIDTH`) are gone. `CARD_INTRINSIC_HEIGHT` became `cardIntrinsicHeight(columns)`, a CSS `calc` over `100cqw` (the grid is an `inline-size` container): poster 1.5× the column plus a fixed 104px worked out from the card CSS (not measured). Passes `minListWidth` (`PORTRAIT_MIN_COLUMN_WIDTH`) and `railFooter` to `FeedPageShell` → `FeedLayout`
- [ ] `Feed/use-feed-entrance.ts` — New 2026-09-19. `useFeedEntranceDelays(ids)`: each row's entrance delay, fixed the first time the row is seen (a CSS animation handed a later delay replays). A fresh list (shrank, or a different first row) staggers 45ms a row up to the 8th; appended pages get 0; rows carried over keep theirs. `FEED_ITEM_CLASS` is the global `.mk-feed-item` in `theme.tsx` (`feed-item-in`, 240ms, fill `backwards` so no transform stays on the cell; off under reduced motion). Mutates refs during render, idempotently
- [ ] `Feed/FeedOverviewPanel.tsx` — What a ticket wall's reserved detail column shows while nothing is selected. **2026-09-22:** renders the server's prioritised lists from `useFeedOverview` (you're invited, selling fast, your own list, your next plans, friends are going, from your watchlist) instead of deriving them from the loaded rows, so it stays put while the feed scrolls; `useNextPlans` is gone. A footer menu picks the custom list (a saved preset, the filters on now, or none). A guest still gets the sign-up card. Rows select exactly like tickets. Filling the column rather than widening the rail or the wall, because both move every ticket at the moment one is picked
- [ ] `Feed/FeedSubjectHeader.tsx` — New 2026-09-22. Above the home and films feeds when exactly one friend is picked (avatar, name, "Your plans: Sharing/Hidden", remove with a themed confirm, block/report menu; add/accept/cancel for a non-friend) or exactly one cinema is in force after resolving the selection (name in its palette colour linking to its website, city → Google Maps, Website, Cineville). × takes the filter off. Replaces the friend and cinema pages **Later same day:** `useFeedSubject(params)` decides whether there is a subject, and the pages pass `header` only then — handing the shell a component that rendered nothing left an empty band under the nav. The friend controls became a two-option "Your plans" choice (eye/green "<name> sees them", crossed eye/orange "Hidden from <name>", both always shown, optimistic, with a line saying what the current one means) plus labelled icon buttons: add/accept/decline/cancel request, remove friend (red outline, themed confirm), and Block/Report via `UserModerationMenu`'s new `layout="buttons"` **Redone to match the app, on request ("don't make it so tall … look at mobile"):** one compact row — 44px avatar, name in the friend's avatar-palette colour, the app's status line ("You're friends" / "You're not friends yet" / "Friend request sent" / red "You've blocked this account"), and for a friend the app's `FriendVisibilityControl` ("Can see your showtimes:" over an Always / Only when invited track, chosen side on a green/amber thumb); non-friends get the app's buttons (solid tint "Add Friend" / "Accept", outline "Decline" / "Cancel Request"). Remove friend, Block and Report all sit in the ⋮ menu (`UserModerationMenu`'s `onRemoveFriend`); the `layout="buttons"/"links"` variants were removed again. Checked in headless Brave, light and dark, friend / menu / non-friend / cinema / home **Later still:** its friend buttons, visibility choice, remove dialog and the five mutations moved to `Friends/friend-controls.tsx`, shared with the rebuilt Friends page
- [ ] `ui/icon-label.tsx` — New 2026-09-22. `IconLabel`: the label of an icon + text pair nudged 1px down, because flex centring against the line box leaves descender-less words sitting high. Use it for every icon+text button/link (flagged three times)
- [ ] `Feed/FriendFilter.tsx` — New 2026-09-22. "Only these friends" under More filters: picked friends as removable rows with avatars, a search box that lists up to six matches. Picking friends turns "Only my own plans" off and vice versa. Chip in the folded section: "Only <name>" / "Only N friends"
- [ ] `Feed/FeedOverviewPanel.css` — The overview's rows: plain classes over Chakra's theme variables, reusing the card pieces' tags, posters and avatars
- [x] `Feed/FeedPageShell.tsx` — The chrome every feed page shares: layout, toolbar, rail, presets and the loading/empty/paging states. Rows are the caller's, which is what lets the home feed swap between screenings and films without swapping anything around them. It also places the feed's two page-level controls, which is a question about the window and not about the feed: the search field goes to the navigation (falling back to a bar above the list where there is no nav, or no room for one on a phone) and "Clear filters" goes to the filter rail, beside the filters it clears. On the home and films feeds both land elsewhere, so nothing is rendered above the list at all **2026-09-19:** the paging spinner now sits in an always-mounted 56px footer (while there are rows) and only fades (160ms in / 260ms out), the app's `LoadMoreFooter`; new `isLoadingMore` prop (defaults to `feed.isFetchingNextPage`), which `ShowtimeFeedPage` extends with `isDrawingRows` **2026-09-20:** `FeedChrome` carries `isReplacingRows`, and the loading screen is now an `isLoadingRows` prop the *page* computes — only the page knows whether the rows it holds are still worth showing. The empty message and the paging footer are held back while it is up. **2026-09-22:** passes `minListWidth`, `railFooter` (only with the rail) and `activeFilterCount` through to `FeedLayout`.
- [x] `Feed/MovieFeedPage.tsx` — A feed of films rather than screenings: the films tab, and the home feed with "one row per film" on. **2026-09-21:** rewritten control-for-control against the film-card lab (`Movies/lab/FilmCardLab.tsx`, deleted) — draws `Movies/cards/FilmRow.tsx` in `grid` mode, replacing the old `MovieCard.tsx` row entirely. The detail column is the showtimes feed's own: `ShowtimeDetailPanel` on the screening a plate opened (a row hands the picked `ShowtimeInMoviePublic` back up with its film, making a whole `ShowtimePublic`), `FeedOverviewPanel` otherwise. A film's own detail is still its own page — this page opens no panel of its own for one
- ~~`Feed/CinemaPresets.tsx`~~ — deleted 2026-09-19, when the filter lab's redesign was folded into the shipped rail. Its two jobs split into real files ported from the lab almost verbatim: `Feed/CinemaSection.tsx` (the rail's summary + sets) and `Feed/CinemaSheet.tsx` (the glued picker + presets), both now backed by `features/showtimes/use-cinema-preset-state.ts` instead of inline mutations
- [ ] `Feed/FeedFilterRail.tsx` — **Feed style + Letterboxd sections (2026-09-23), on request.** "Group by film" left the switch list: its own "Feed style" part above the rest, a neutral `RailSegmented` Ticket wall / Movie rows, with a `RememberChoice` (now takes a `title`) backed by `use-remembered-feed-style.ts`; Clear filters keeps the style (a view, not a filter). "Basic filters" renamed "Letterboxd"; with no `letterboxd_username` it shows `LetterboxdLink` (inline username field + Link, same `updateUserMe` write as `Settings/LetterboxdSection`, gated by `useRequireAccount`) and greys out the watchlist/seen switches (`RailSwitchRow` gained `disabled`; `RailInput` gained `type="text"` + `placeholder`). `RailSegmented` now uses one font weight (600) for every segment: the selected/unselected 600/500 flip visibly landed seconds after the click on a feed-style switch (with the feed re-render), which read as unprofessional. That alone did not fix it: the real cause was layer promotion — while the new feed's cards run `feed-item-in` beside the sticky rail, Chrome gives the rail a layer and rasterises its text differently, then drops it ~1s later. `.mk-rail` now has `will-change: transform` (in `FeedFilterRail.css`) so it is always its own layer; verified frame-by-frame, identical from the first frame after the click. `LetterboxdLink` now uses the settings page's `.st-affix` "letterboxd.com/" field + `FriendButton primary` "Save" on one row (imports `Settings/settings.css`, all class-scoped). **Switch speed:** `Showtimes/MainShowtimesPage.tsx` removes the left feed's inactive queries once a switch settles, so coming back opens on a fresh first page (was every scrolled page at once, ~15s → ~0.7s measured); `MovieFeedPage`/`ShowtimeFeedPage` pass `hasNextPage && !isSwitchingLayout` to `useInfiniteScroll`, since the loading screen put the sentinel in view and the feed being left fetched ~15 pages on its way out
- [ ] `Feed/FeedFilterRail.tsx` — **"Clear filters" leaves a remembered language alone (2026-09-19).** A remembered English-only choice is a *default* now, the same status the account's own preferred cinemas already have — clearing filters was silently turning it back off, since `onReset` (`useFeedParams`'s `resetParams`) just navigates to an empty search. `clearFilters` here now builds one explicit `onChange({...defaultFeedParams, languages: ...})` patch instead of calling `onReset` (calling it and then patching languages straight after is racy: the patch would merge onto *this render's* stale `params`, since the reset's navigation hasn't landed yet — spelling out every key sidesteps that). `activeFilterCount` is adjusted locally too (`displayedFilterCount`), so a remembered language doesn't count toward "Clear N filters" either — `countActiveFilters` in `feed-params.ts` has no way to know about a device-local preference, so this stays a rail-local correction rather than a shared one.
- [ ] `Feed/FeedFilterRail.tsx` — Rebuilt 2026-09-19 from the filter lab's `/lab/filters` design (kept, never committed there — see below), replacing the accordion-of-every-dimension rail. Top to bottom: `CinemaSection` (+ the glued `CinemaSheet`, opened by "Select cinemas"), Quick filters (`FeedPresets`, unchanged), Show me (`RailSwitchRow` for watchlist-only and hide-seen — simplified from the old tri-state `RailModeRow` to a single "Only" switch each, at the user's explicit call, since a rail this size does not need "Hide my watchlist" too — plus "Group by film" where `showGroupToggle`), Language (one "English subtitled (or spoken) only" switch, simplified from the old two-pill `en`/`nl` picker — same call, Dutch-only filtering is gone from the UI though `FeedParams.languages` still carries it — with a quiet "Remember my choice" checkbox on the heading's own line, `features/showtimes/use-remembered-language.ts`), When (days + a time-of-day `RangeSlider`, replacing the old preset-pills-plus-exact-range box), More filters (friends `RailSegmented`, Letterboxd `RailModeRow`s — tri-state kept here, only watchlist/watched were simplified — and a movie-length `RangeSlider`), and Clear filters as a full-width button pinned to the rail's bottom (`position: sticky`, which is why `.mk-rail` is `overflow: clip`). The old top "Filters" heading row is gone. Cinema state is still resolved through `shared/filters/cinema-selection` exactly as before; the sheet holds its own draft (`draftIds`), committed on Apply/outside-click/Esc, and the rail raises its own `z-index` to 2100 (the root notice banner it once cleared is gone since 2026-09-23) while the sheet is out so it stays live beside the dimmed page **Later same day, on request:** the folded When and More filters sections now list what they hold as the app's active-filter chips (one per filter — days, time of day; friends' status, shown lists, hidden lists with ⊘, film length — several values summarised "Today +2", click to clear that filter), because a filter on inside a collapsed section was invisible and confusing. The header's text summary is now only the idle "Any day, any time" / "Friends, length, lists"; the chips replace the old "Fri, Sat · 18:00–22:00" / "2 on" summaries. The friends chip says "Friends going" rather than the app's bare "Going", which away from its control read as your own status. "Show me" renamed "Basic filters". **Then, on request ("toggles, cinema sets and chips should be optimistic"):** the rail reads and writes `useOptimisticFeedParams` instead of its `params`/`onChange` props (renamed `committedParams`/`commitChange` at the top), so every control in it, presets and Clear included, answers in the click's own frame; `activeFilterCount` is adjusted by the difference `countActiveFilters` sees between the optimistic and committed params, so whatever the page pins stays out of it exactly as upstream. **Sheet exit (2026-09-19):** closing the cinema sheet sets `leavingIds` and commits them in the timer that unmounts it (`commitAfterExitRef`, so the timer sees the latest `commitCinemas`), not before; `shownCinemaIds` includes `leavingIds`; `openCinemaSheet` reopens mid-exit with the leaving selection; a rail set picked mid-exit cancels it and commits at once
- [ ] `Feed/FilterRailControls.tsx` — Gained four primitives 2026-09-19 for the redesign: `RailSwitchRow` (a switch as a whole-row sentence, ported from the lab's `SwitchRow` — the *row* never fills, only the track does, solid `app.tint` with a white thumb, matching the app's own `AppSwitch`), `RailPartLabel` (the section heading's own 11px/0.6-tracking uppercase spec, for a block with no accordion), `RailPart` (a block that is always open — Show me, Language, Quick filters — carrying the same full-bleed tear `RailSection` gives a collapsible one, so the rail still reads as one strip of stubs regardless of which parts fold), and `RailFacetHeading` (11px uppercase but *muted*, `fg.subtle` rather than full ink, for a single dimension nested inside an open section — "Days", "Time of day" — with its own "Clear" riding the same line; ported from the lab's `FacetHeading`, added after an interim version skipped it and drew ad-hoc 13px labels with no per-dimension Clear, which the user called out as not actually copying the lab). Otherwise unchanged: collapsible section, pill, tri-state row, sub-heading, text button, native date/time/number box, every one the app's own control rendered for a mouse. A pill is state, so it is fully round even when it carries a second line; `RailActionButton`/`RailIconButton` are the other half of that rule, squared at `shared/filters`' own `PRESET_BUTTON_RADIUS`. `RailSegmented` is for an exclusive choice where one option is always on. `RailModeRow` is the app's tri-state card (only / hide / off), now used for Letterboxd lists alone — watchlist and watched moved to `RailSwitchRow`. Everything pressable is `chakra("button")`, not `Box as="button"` 2026-09-19, later: `RailSection` takes `chips` (`RailChipItem[]`) and, while closed, draws them under its heading as `RailChip`s — the app's `ActiveFilterChip` (pill hairline, 12px/500, optional ⊘, trailing ×, whole chip removes) — instead of the text summary; open, the controls speak for themselves and the chips step aside. `summarizeChipValues` is the app's "first value +N" rule. Then: `RailSwitchRow`'s thumb slides (absolute thumb, `translateX` over 160ms) instead of jumping ends via `justify-content`; `RailChip` is coloured and a size up (green soft fill/hairline, red for an excluding filter, 13px/600, grows in with `rail-chip-in`) on request — "you shouldn't be able to miss them", but in the rail's own "this is on" family so it isn't loud.
- [ ] `Feed/WallColumnsControl.tsx` — New 2026-09-22. The ticket wall's 2/3/4 tickets-per-row control. Full: its own card under the rail card ("TICKETS PER ROW" label + neutral segmented groove, glyph + number). `compact`: a vertical column of glyph-only buttons in the folded rail strip. `role=radiogroup`. Reads/writes `use-wall-columns`. Went through the rail's filters and a floating bottom pill first; the user moved it here.
- [ ] `Showtimes/cards/PortraitTicketCard.css` — **2026-09-22:** `.mk-wall--portrait` (new class, added by `PortraitTicketCard.tsx`; `FilmRow` also carries `.mk-wall` for the shape and must not scale) is an `inline-size` container and defines `--mk-u`, a "ticket pixel": `clamp(1px, 1px + (100cqw - 210px) * 0.00167, 1.4px)` — 1px on the default ticket, growing at about a third of the ticket's rate. The title/original, the stub's padding/rows/date/time/seat line, and (scoped `.mk-wall--portrait …` overrides at the bottom) the xs tags, seat mark, invited line and avatar overflow are all `calc(N * var(--mk-u))`, so the text grows a little with bigger tickets; the poster inset, radius and notches stay fixed. `PortraitTicketCard.tsx` passes `unit="var(--mk-u)"` to `AvatarStack` (new optional `unit` prop in `card-parts.tsx`: sizes count in that length instead of px). Measured: date 15px on the default ticket, 16.7 at 278px, 19.8 at 401px, 21 at the cap
- [ ] `Feed/MovieFeedPage.tsx` — **2026-09-22:** owns `isLoadingRows` (`isSwitchingLayout || feed.isLoading || feed.isReplacingRows`) and draws no rows while it's up — film rows used to stay under the spinner when Group by film was turned off (the ticket wall had the same bug the other way; `ShowtimeFeedPage` now empties `rows` on `isSwitchingLayout` too). Verified per frame: no visible loading spinner with rows of the old layout either way. Passes `minListWidth={FILM_ROW_LAYOUT.minWidth}` (440) so the rail folds before rows get squeezed
- [ ] `Movies/cards/film-cards.css` — **2026-09-22:** `.fr-film` is a named container (`fr-film`; under 600px the actions go icon-only). `.fr-film__info` is a *size* container (`fr-info`) so it adds nothing to the row's height: **a row is exactly as tall as its poster** (user: "never make the movie row extend further than the poster goes high"), and height queries drop cast (<200px), synopsis to 1 line (<172), synopsis + original title (<154), credits (<136). Plates inside a row are sized in `--fr-u` (1px at a ≥500px info column → 0.85px at 350px; min width, label, date, time, +N). Measured at 960–1920, docked/folded: no row taller than its poster, nothing overflowing, plates always one line
- [ ] `Movies/cards/FilmRow.tsx` — **2026-09-22:** `usePlateSlots` reads the plate grid's resolved column count (`auto-fill`) via ResizeObserver and shows that many plates (last slot "+N" when more), replacing the fixed `ROW_PLATE_SLOTS = 6` — so plates are always one line and the lone "+N" on a second line is gone. **Fixed same day (user: rows said "No screenings in these filters" after halving the window, and stayed that way after growing it again):** a count taken mid-resize (the list squeezed for a frame before the rail folds) could be 1 slot → 0 plates → the `fr-film__none` branch, which unmounted the observed grid, so nothing ever recounted. Now the observer watches `.fr-film__info` (always mounted), "No screenings" renders only when the film has no times at all, and a row always shows ≥1 plate beside its "+N". `--fr-u` now goes down to 0.75px. **"+N" tile restyled (user: "much sleeker"):** no longer the dotted, filled `mk-pal-gray` block — `.fc-plate--more` is a 1px `border`-colour outline with no fill, the count 17px/700 in `fg`, and "more →" (`PanelIcon.arrowForward`, `.fc-plate__more-arrow`) muted under it; hover fills `app.surfaceMuted`, darkens the outline and nudges the arrow 2px. Checked light/dark at 1920 and 960. Verified with a live resize 1920→960→820→1920: never a false "No screenings", always one line, "+N" whenever screenings are hidden, time text 16px → 12px and back. `FILM_ROW_LAYOUT.minWidth = 440`
- [ ] `features/showtimes/poster-sources.ts` — New 2026-09-22. `posterSrcSet(url)` rewrites stored poster URLs into a `srcset`: Letterboxd `…-0-230-0-345-crop` → 230/460/1000 (only whitelisted sizes resolve, e.g. 700 is a 403), TMDB `w342` → 342/500/780; anything else undefined. `POSTER_SIZES = "auto, 400px"`. Used by `card-parts` `Poster` and `film-card-kit`'s poster. Verified: 267px card loads 460 at 1x, 1000 at 2x, no broken images
- [ ] `Feed/RailCollapse.tsx` — New 2026-09-22. `RailStrip` (folded rail: sliders button with active-filter badge, then the compact footer), `RailCollapseHandle` (zero-width sticky box; a slim 16×40 pill tab centred on the rail card's right edge — `inset` is the rail column's measured scroll lane (`scrollbar-gutter: stable`, 15px here), which the card ends short of), `useRailCollapsed` (localStorage), `RAIL_STRIP_WIDTH` (44px) and `RAIL_STRIP_GAP` (12px, half the docked gap — user: less space for the collapsed filters), and `RailFoldedProvider`/`useIsRailFolded` (context `FeedLayout` provides around the list; the floating rail has no footer, the strip carries it).
- [ ] `features/showtimes/use-wall-columns.ts` — New 2026-09-22. Tickets per row on the wall (2/3/4, plus 5 only while the rail is folded — `wallColumnOptions`/`effectiveWallColumns`; a stored 5 draws as 4 beside an open rail; default 3): a module-level store over `useSyncExternalStore`, persisted in `localStorage` (`mikino.feed.wallColumns.v1`); an invalid stored value falls back to the default. A view preference, so kept out of the URL and presets. A `storage` listener keeps other open tabs in step.
- [ ] `Feed/FeedFilterRail.css` — Gained `overflow: clip` on `.mk-rail` 2026-09-19 (ported from the lab's `LabCard`), so the pinned "Clear filters" button's `position: sticky` pins to the aside the rail scrolls in rather than the card trapping it as its own scroll container, while corners stay rounded under it. Otherwise unchanged: the rail's card and the perforations that divide it. The card is the *ordinary* panel card — same fill, hairline, radius as the detail panel across the feed — painted here rather than taken from `SidePanel` only because the dividers have to run the full width. What is left of the ticket is the tear — sections divided by a full-bleed dashed seam in the card's own hairline colour. Also reserves the scroll lane (`scrollbar-gutter: stable`) on the column the rail sits in
- [ ] `Feed/CinemaSection.tsx` — New 2026-09-19, ported from the lab almost verbatim. The rail's cinema part: the selection's name (your preferred set under its own name, wrapping to two lines) with the cinema count smaller underneath, "Select cinemas", and one line — "Save current selection as preferred cinemas" or "These are your preferred cinemas" (gated for guests via `useRequireAccount`). Saved sets show once there are two (preferred counts), as chips or a list; the preferred one carries the app's star in the set's own ink — a gold star was tried and rejected as ugly. **Follow-up same day, real bug report**: "Save current selection as preferred cinemas" was calling `setAsPreferred`, which overwrites whatever the *current* favorite preset's cinemas are, in place — so picking a saved preset ("Breed") and saving it as preferred copied Breed's cinemas onto the existing favorite ("Amsterdam best") while Amsterdam best kept the star, instead of the star moving to Breed. `sets` entries are no longer bare `CinemaPresetPublic`s — a plain `{id, name, cinemaIds, isPreferred}` (`SetEntry`) — and now always end with a synthetic "All cinemas" entry (id `__all-cinemas__`, every cinema's id), so there is always one click back to no cinema filtering regardless of what is saved; `showsSets` now gates on `sets.length` (which already counts it) rather than `presetState.savedSetCount`. The actual promote-not-copy fix lives one level up, in `FeedFilterRail.tsx`'s `onSavePreferred` (and mirrored in `CinemaSheet.tsx`'s footer button) — see below
- [ ] `Feed/CinemaSheet.tsx` — **Reworked 2026-09-19 for the desktop form factor, at the user's explicit request.** Picker's search box, "N of M selected · Select all · Clear all" and the double-click hint now sit in a `flexShrink={0}` block above the checklist rather than scrolling with it — only the city list itself scrolls (`Picker` is now `flex:1, minH:0, display:flex, flexDirection:column` with its own inner scrolling `Box`, and `CinemaSheet` renders it as a direct flex child instead of wrapping it in the scroll container itself). "Apply" is gone: closing the sheet (X, Esc, outside click) already commits the draft, so it was a fourth way to do what three others already did. "Save as preset" and "Manage presets" moved from a footer row into two `HeaderIconButton`s beside Close — reasoned from how often each is actually used ("95% of the time they just want to select some cinemas... maybe only once or twice ever do they need to save or manage their presets"): the footer now holds exactly one button, "Set as preferred cinemas" (the "sometimes" action), full width, shown only when signed in; a guest's footer is nothing at all, matching that closing alone is the whole interaction 95% of the time. `FooterTextButton` was deleted (no longer used). **Follow-up same day**: the footer's "Set as preferred cinemas" now checks `namedMatch` (already computed for the header's "Save as preset" state) — when the draft selection *is* an existing named preset, it calls `presetState.makePresetPreferred(namedMatch.id)`, the exact same write `PresetManager`'s per-preset "Set as preferred cinemas" makes, instead of `setAsPreferred(draftIds)` (`MeService.setCinemaSelections`), which would otherwise leave a duplicate anonymous "My cinemas" row next to the named preset it already matched. **Styling follow-up**: "Save current selection as preferred cinemas" now matches `FeedPresets.tsx`'s "Save current filters" — a padded, rounded, tint-coloured chip with a `bg.subtle` hover fill, rather than plain underlined text, so the rail's two "make this the default" actions read as the same kind of control. **Second follow-up, real bug report**: that first fix wasn't enough — `CinemaSection.tsx`'s own "Save current selection as preferred cinemas" line (the rail's, not the sheet's) still called plain `setAsPreferred`, which is what the user actually hit: switching the shown selection to a saved preset ("Breed") and saving it as preferred *overwrote the existing favorite preset's cinemas in place* rather than moving the star to Breed. `FeedFilterRail.tsx`'s `onSavePreferred` now does the same `namedPresetFor` check → `makePresetPreferred(id)` the sheet's footer does, so both entry points promote a matched preset instead of copying into whichever one currently holds the star. **Alignment follow-up 2026-09-21**: the star/check icons next to "These are your preferred cinemas", "Save current selection…" and a chip's name all sat visibly high against their icon — a no-descender line (which most of this text is) still gets its line-box's descender space evenly split by flex centering, so the *ink* ends up above the box's true centre while a flat icon glyph doesn't have that gap. Each of those text runs is now wrapped in its own `<Box as="span" position="relative" top="1px">` rather than left as a bare text child, since a `transform`/`top` nudge needs something to grab — confirmed empirically (a scaled screenshot with a centre guideline drawn through it) after a first, unscaled measurement pass gave a false "already balanced" reading. Also fixed same day: the "N cinemas" count, when it stands in for the label with no named preset, used to sit as small 14px text above an invisible (`visibility:hidden`) meta line kept only to hold its height — reading as small text over a dead gap. It now renders as one larger (20px) line, centred in a `minH="35px"` box that both states share, so the rail never jumps switching between a named preset and a plain count, and the plain-count state looks chosen rather than empty.
- [ ] `Feed/CinemaSheet.tsx` — New 2026-09-19, ported from the lab almost verbatim. The cinema sheet, opened by "Select cinemas": glued to the rail's right edge over the showtimes (fixed, so nothing moves), scrim + Esc + outside click close it, page scroll blocked meanwhile. Holds a draft committed on Apply/outside click/Esc like the app, because committing an empty selection means *every* cinema. "Manage presets" and "Save as preset" open a second panel beside the sheet. Edit mode: name field + Cancel/Save changes. **Rendered through a Chakra `Portal` (to `document.body`), added same day after a real bug report** ("the select cinemas appear BEHIND the showtime cards"): the lab never hit this because its stub list had no grid card variant, but the real "Ticket wall" layout lays each card in a `content-visibility: auto` cell, and those cells always establish their own stacking context — a `position: fixed` sheet painted *inside* the rail lost to them regardless of z-index. This codebase had already hit the identical wall once, for `card-parts.tsx`'s `HoverCard`, and solved it the same way. `SHEET_Z_INDEX` here (2100) must stay in sync with `RAISED_Z_INDEX` in `FeedFilterRail.tsx` — the rail card itself isn't portalled, so it needs an explicit z-index at the same level to still paint above the (now-portalled) scrim, which sits one below at `SHEET_Z_INDEX - 1` **Motion, 2026-09-19, on request ("like how the showtime modal opens"):** the sheet now slides out from behind the rail's right edge (`sheet-in`, 260ms ease-out-quint) while the scrim fades in (`scrim-in`), and its contents settle in with the showtime panel's own `panel-enter` 90ms behind; closing runs it back under the rail in 180ms (`sheet-out`/`scrim-out`, `SHEET_EXIT_MS`, exported). The slide is clipped by a static `clip-path` on a new fixed full-viewport wrapper (at `SHEET_Z_INDEX`; the two panels inside are now z 2 and 1), so it never crosses the rail; the presets panel slides from behind the sheet and leaves with it (`--mk-sheet-from`). New `isLeaving` prop: plays the exit, drops pointer events, ignores Esc. `FeedFilterRail.tsx` keeps it mounted for the exit via a `leavingIds` state (the already-applied selection) cleared after `SHEET_EXIT_MS`, and keeps the rail raised until then. Keyframes in `theme.tsx`; all four honour `_motionReduce`. **Revised same day:** the contents' `panel-enter` fade was dropped at the user's call ("just a fast slide"), enter 170ms / exit 140ms. "Doesn't look right on first open": the slide started in the frame that mounted the sheet (a ~250ms render of every chip in dev), so it appeared already halfway out, and its shadow poked past the rail at the start. Now the scrim and sheet mount with `animation-play-state: paused` and `usePrimedSlide` sets them running on the DOM two frames later (not via state — a re-render of the sheet cost as much as the mount), and the default `--mk-sheet-from` is `calc(-100% - 64px)` to hide the shadow. The mount itself is still the ~250ms (dev) wait before anything moves — the next thing to cut if opening still feels slow **Exit fixes, same day:** it only slid shut when nothing had changed — a changed selection committed at once, and the feed's re-render held the slide back until the sheet was due to unmount, so it snapped. `FeedFilterRail` now commits *after* the exit (below). `Picker` is `memo`'d so the `isLeaving` re-render skips the chips. The presets panel had no exit at all: new `useLeavingValue` keeps it mounted for `SHEET_EXIT_MS` when it closes (or editing takes its place), sliding back behind the sheet; its shadow reach (64px) is added to every offset so no sliver shows
- [ ] `Feed/CinemaChecklist.tsx` — New 2026-09-19, ported from the lab almost verbatim. Port of the app's `CinemaPickerList`: city sections with Select all/Deselect all, "Other cinemas" with the city under each name, accent-trio chips with the round check (border = fill in dark mode). Double-click selects only that cinema
- [ ] `Feed/PreferredCinemasPrompt.tsx` — New 2026-09-23. `usePreferredCinemasSave(presetState)` → `{ save, prompt }`: the one path both "Set as preferred cinemas" buttons (rail's `CinemaSection` line and `CinemaSheet`'s footer) go through, via shared `planSaveAsPreferred`. A selection that is a preset → promote it; otherwise save under the reserved "My Cinemas"; if a "My Cinemas" exists, a hand-drawn dialog (Chakra's dialog layers under the sheet's z 2100, so this sits at 2300) asks Replace / Rename old set. **Follow-up same day, user report:** promoting another preset while "My Cinemas" was preferred silently left "My Cinemas" as a stray preset. Now a second question (`planPromotePreset` → `confirm-demote`): Delete it, or Keep as preset (rename field, prefilled "Previous cinemas"; keeping the same name skips the rename), via shared `promotePresetOverReserved`. The hook also returns `promote(preset)`, which `PresetManager`'s "Set as preferred cinemas" now uses (new `onMakePreferred` prop, threaded through `CinemaSheet`'s `onPromotePreset`). Same day `CinemaSheet.tsx` got Apply back (filled, beside an outlined preferred button; guests see Apply alone), reversing the 2026-09-19 removal at the user's request
- [ ] `Feed/PresetManager.tsx` — New 2026-09-19, ported from the lab almost verbatim. Manage presets in the app's words ("Your preferred cinemas", "Saved presets", Edit / Set as preferred cinemas / Delete with an in-place confirm, reorder arrows), one card per preset, names wrapping rather than truncated; plus `SavePresetForm` (prefilled name, 409 turns Save into Replace)
- [ ] `Feed/RangeSlider.tsx` — New 2026-09-19, ported from the lab almost verbatim. The app's dual-handle slider (`TimeRangeSliderInline` / `RuntimeRangeSliderInline`) for the web: same scales (09:30→03:00 in 15s; 5→200 min in 5s), geometry and colours, exporting `TIME_SCALE`/`RUNTIME_SCALE`. Writes on release and holds its place until the feed catches up; handles are keyboard `slider`s. Replaces the old preset-pills-plus-exact-range-box approach in `FeedFilterRail.tsx`
- [x] `Feed/FeedToolbar.tsx` — The bar above the feed, for the two cases where the navigation cannot carry the search field: a phone, whose nav is a brand and an account chip, and the deep-link routes outside `_layout`, which have no nav at all. Everywhere else it is not rendered and the list gets the whole band back. "Clear filters" comes with it only where the filter rail is not on screen to hold it instead. Purely presentational; every value and setter comes from `useShowtimesFeed`
- [x] `Feed/feed-search-slot.ts` — The search field's seat in the top navigation, and who is sitting in it. The field moved a floor up from the page that owns its state, and it cannot be handed up through props: the nav is a sibling of the router's `Outlet`, not a parent of it. A module-level store rather than a context, the same shape as `auth/session.ts` — it answers synchronously, so the nav paints the field in the first frame a feed page renders, and only the subscriber re-renders, where a provider above the `Outlet` would put every keystroke through the whole page. Nothing here knows which routes have a feed: a page with one fills the slot while mounted, so adding a feed screen needs no list kept in step
- [x] `Feed/FeedSearchBar.tsx` — The search field and its scope in one track, the way the app builds it. "Director" is not a setting that happens to sit near the search box, it is half of what the box means: a bare `<select>` beside a bare `<input>` read as two widgets and let the placeholder say "Search showtimes…" while the scope said something else. The placeholder is the scope's, so they cannot disagree; the clear button has a fixed-width slot so the field does not resize as you type; and "Friends" is hidden for a guest, who has none to search
- [x] `Showtimes/SeatFloorPlan.tsx` — The room, drawn. All the geometry is `shared/showtimes/seat-floor-plan-layout` (the same code the app runs); this measures its container with a ResizeObserver, hands over the two numbers, and paints what comes back. Takes the availability reading's `checked_at` so the map and the seat count beside it cannot disagree. Seats are keyed on the room's own geometry and not on their label: row "4" seat "15" and row "41" seat "5" both spell "415", and React was quietly dropping one of the two. A seat the booking system marks unselectable — the spacers padding a room's aisles, the wheelchair spaces it will not sell online — takes its slot and paints nothing, as the app does; painting them as free seats is what made The Movies' Zaal 4 read as a broken room, 72 squares for the 52 seats the count beside it was talking about. Every real seat is clickable, taken ones included: `taken` is the poller's last reading of the ticket shop, not a statement about where the viewer is allowed to say they sat, and a returned ticket or a stale count made the old free-seats-only rule wrong as often as right. Clicking the seat you are already in gives it back, the same one-setting rule the status buttons follow. **Three separate things made a big room slow to open**, measured on Louis Hartlooper's 300-seat LHC 1: the element the width is measured from did not exist until the plan arrived, so a chain of renders and a frame sat between the data landing (185ms) and the seats appearing (337ms) — the container is mounted from the first render now, whether or not there is ever a plan for it; committing 300 seats costs ~74ms and landed mid-animation, stalling the column and pushing a 325ms open out to 411ms — the seats wait for an idle frame now, so the animation finishes first; and the space they will fill is reserved as soon as the plan lands, because the layout maths is cheap even though drawing its result is not. A seat is a plain `<div>` with a native `style` object rather than a `Box`, since every seat's position is distinct and Chakra's style props meant one trip through the style engine and one injected rule per seat; the click is delegated to the grid rather than closed over per seat, which is also what lets the tiles memoise. After all that, room size no longer moves the open at all: 76ms blocked for 300 seats against 86ms for 72
- ~~`Showtimes/SeatAvailabilityBadge.tsx`~~ — deleted (only the old feed row used it; the card variants draw busyness with `cards/card-parts` `SeatMark`)
- ~~`Showtimes/SeatAvailabilitySection.tsx`~~ — deleted (superseded by `Showtimes/detail/SeatAvailabilityPanel.tsx`)
- ~~`Showtimes/ShowtimeVisibilityControl.tsx`~~ — deleted (superseded by `Showtimes/detail/ShowtimeVisibilityPanel.tsx`)
- ~~`Showtimes/ShowtimeInvites.tsx`~~ — deleted (superseded by `Showtimes/detail/ShowtimeInvitePanel.tsx`, which replaced the always-open friends list and its column of repeated Invite buttons with a picker)
- [x] `Showtimes/ShowtimeDetailPanel.tsx` — The panel that opens beside the feed for the selected showtime. Docked rather than a centred dialog so the list stays visible while you act on one row — the one real advantage the web has over the app's full-screen sheet, and the only place the two are allowed to differ in shape. Everything else follows that sheet, in its order and its words: header with "More info" under the poster, who is already here, the status buttons, one card holding the seat count / ticket link / your seat, "Status visible to", "Invited", and Share beside "Invite friends" at the foot. The web had reached the same features by a different arrangement, and the two clients disagreeing about where a thing lives — and about whether a status is a star or a bookmark — cost more than either layout was worth. The deliberate exception is Share, which copies a link here instead of opening a share sheet. Owns three things itself: the sticky-header card (which is why the layout hands the detail slot over `bare`), the entrance replayed on each change of selection, and scrolling the panel back to its top, because a docked card serving near-identical rows that changes silently reads as though the click did nothing. Status changes patch the cached rows in place rather than invalidating `["showtimes"]` 2026-09-19: the status buttons are now `detail/ShowtimeStatusSection` (which owns `useShowtimeSelection` and the "Let these friends know?" dialog), so the panel no longer holds the status itself. 2026-09-22: "Report an issue" (`Movie/ReportShowtimeButton`) sits at the panel's foot for signed-in viewers.
- [x] `Showtimes/detail/PanelChrome.tsx` — The panel's vocabulary: action button, icon button, empty state, the type scale for the two rows that state a fact and answer it (`PANEL_ROW_LABEL_SIZE`/`PANEL_ROW_VALUE_SIZE` — larger than the rest, because "Available seats" and "Status visible to" are what people come to the panel to read and they were set at sizes meant for a phone sheet), and the real `<button>`/`<a>` factories everything pressable is built from (`Box as="button"` only swaps the tag — the props stay a `div`'s, so `type`, `disabled` and `href` are not among them). `FilterRailControls` for the other side of the feed, and it borrows that file's rules on purpose, because the two cards face each other across the list — 120ms transitions and the shape rule, round is state and squared at `PRESET_BUTTON_RADIUS` is an action. It also owned a section heading, a meta line and a count bubble until the panel took the app's layout; the app has no uppercase section headings here, and the facts they organised are now the header's own badges and the seat card's rows. What differs is the ground: this sits on `bg.panel`, so the ordinary `fg`/`fg.muted` neutrals work where the rail's tint needed its own ink
- [x] `Showtimes/detail/PersonAvatar.tsx` — A person: their Letterboxd photo when they have one linked and synced, a coloured initial otherwise (and always underneath, so a broken/stale photo URL falls back to it), in two shapes (circle, chip); the overlapping stack went with the collapsed attendance groups it existed for. The colour is `shared/users/avatar-color`, the rule the app reads too, so somebody who is teal in the app is teal here; it comes from the id and not from a list position, which is what lets it survive re-ordering and mean anything
- [x] `Showtimes/detail/ShowtimeDetailHeader.tsx` — Which film, when, where. Laid out as the app's sheet lays it out: poster on the left with "More info" directly beneath it — the link belongs to the thing it is more information about, rather than competing with the title for the text column — and the film and the screening stacked beside it. The app also tucks the Letterboxd watch counts up here as an icon and a number; a desktop column has room the sheet does not, so they are spelled out by name in the audience box instead. Sticky inside the panel's scroller, because the panel is docked beside a list of near-identical rows. The state is carried by a strip in the status's own colour, blue when someone has invited you. Its own type scale (title/subtitle/date/time/badge sizes, plus a matching poster width) grew on request once the panel itself grew wider and an unchanged 11px director line looked left behind at the old size — a first pass pushed the title to 24px and it read as oversized rather than roomy, so the shipped scale is a smaller second pass, each line a couple of pixels over where it started rather than a size class up. The original title is italic, the film-database convention for a work's title in its own language. **Follow-up 2026-09-19, on request:** now also renders `detail/FriendWatchPills` under the badges — the watchlisted/watched counts moved back up here from the audience box, as small pills in the text column, which the taller poster beside it leaves room for, so they add no height.
- [x] `Showtimes/detail/ShowtimeStatusControl.tsx` — The three-way going/interested/not-going control: one setting, not three toggles, so pressing what you already hold clears it. Optimistic, because marking interest is also what sends the backend to read the ticket shop and the round trip can run to seconds. Visible for guests and gated on press, per the app's rule. The app's order and the app's glyphs — a cross, a bookmark and a tick from `panel-icons`, where the web had reached for a slash and a star and made the same three choices unrecognisable between the two clients
- [x] `Showtimes/detail/ShowtimeAttendance.tsx` — The audience box: one bordered block directly under the header, holding `detail/FriendBadges`, and a plain sentence when it is empty. Sits *above* the status buttons, the app's order and the right one — who is already going is usually what decides whether you are. It replaced four collapsed groups behind disclosure triangles, which were tidier and answered the wrong question: "is anyone I know going" is something you want to read, not open. Also holds the Letterboxd marks, which is where this deliberately does more than the app: there they are an icon and a number in the header's corner, and both the count's meaning and the names behind it cost a tap. Here each is a named row — "Watchlisted by 3 friends" — that opens in place into the same list the app's popup shows, with the same per-friend Invite button on the watchlisted side. A desktop column has the room, and it puts "who is here" and "who would come if I asked" in one block. Watched stays a plain list even open, for the app's reason: those friends have already seen the film. Entirely off the payload the feed already fetched, so it costs no request. Hidden for a guest, who has no friends list for it to describe. **Follow-up 2026-09-19, on request:** the "Watchlisted by N" / "Watched by N" rows (`WatchList`) moved out to the header as `detail/FriendWatchPills`, so this box is only the going/interested pills and the invite section now; its inner padding also dropped 12px→10px. **Follow-up 2026-09-20, on request ("remove 'No friends are interested in this showtime yet.'"):** the empty-state sentence is gone outright, not reworded — the invite panel right below already answers "so invite someone", which was the only thing that sentence said. When there is no audience the box now renders nothing above `ShowtimeInvitePanel`, whose own top border becomes the block's only divider; verified empty and populated in the headless-Brave mock (`attendance-check.mjs`).
- [x] `Showtimes/detail/ShowtimeInvitePanel.tsx` — Invites, in the app's two halves: an "Invited" list of who you asked and whether they have looked (Seen / Pending / Dismissed, with nudge and take-back), then a bar at the foot holding Share beside a collapsible "Invite friends". The picker is multi-select and sends in one go — inviting is almost always plural — and orders friends by who has the film watchlisted, which the flat list never did. Share is the one deliberate break from the app: it copies a server-signed link to the clipboard rather than opening a native share sheet, so it carries a link icon and not the app's share glyph. Hidden for guests, who have no friends list behind it. **Follow-up 2026-09-19, on request:** the "Invited" section made shorter — the "Invited by you" caption under every row is gone (everyone in the list is invited by you), so each invite is one line, and with nobody invited the heading and "You haven't invited anyone yet." share one line instead of two. **2026-09-19:** Invited rows now show the photo: `avatar_url` looked up from the friends list by `receiver_id` (a ping carries only the name), initials otherwise
- [x] `Showtimes/detail/SeatAvailabilityPanel.tsx` — How full the room is, the floor plan, the ticket link and your seat, folded into one card the way the app folds them: "how full is it", "where am I sitting" and "where do I buy one" are the same decision, taken in that order, and a card with none of the three renders nothing. Renders no busyness when `trackable === false`, which means "this cinema never tells us" rather than "not looked yet". The check used to look broken because the server turns `can_request_check` off the moment a read is pending and the button simply vanished: `checking` is now painted on press and the working state keeps the button's own footprint so the row does not reflow under the press, the hook polls every three seconds while it is pending, and the fresh reading is written back into the feed rows so the list's badge cannot disagree with the panel. The seat editor is inline here rather than a dialog, since the panel is already a column and has the room. "Available seats" and its answer are set from `PANEL_ROW_LABEL_SIZE`/`PANEL_ROW_VALUE_SIZE` rather than the app's own sizes, which are sized for a sheet that fills a phone. The ticket row carries the MiKiNO mark, which is a ticket — the app puts the same image there, and a picture of the thing beats an outbound-link glyph that would only repeat the chevron at the other end. It is sized for the *ticket* and not for the file: the mark is 770x460 inside a 1024-square transparent canvas, so a contained box draws it at 45% of that box's height, and an 18px box gave an 8px ticket
- [x] `Showtimes/detail/ShowtimeVisibilityPanel.tsx` — Per-showtime visibility, shown to every signed-in viewer whatever their status (it used to appear only once going/interested — removed 2026-09-19 on request, since the point is to narrow who sees you *before* you mark it; the backend already stores a mode set ahead of a status, and the app never had the gate). Drawn as the app draws it: a "Status visible to" row with the mode as a coloured pill, the three options folded away behind it, and "Change your default" under them, the row set from the shared `PANEL_ROW_*` scale so it and the seat row cannot drift. Collapsed by default because the mode is usually something you *check* rather than change — the pill answers it without opening anything, and the panel stays short enough that the invites below are still on screen. Copy, icon and palette all come from `shared/showtimes/visibility-mode` so the website cannot describe or mark a privacy setting differently from the app; it paints from the payload's `visibility_mode` and revalidates behind that, so switching screenings never blanks the control. **Follow-up 2026-09-19, on request:** moved from after the seat card to directly under the status buttons (the order is now status → visibility → seats), and the options no longer unfold inline: the row is a bordered trigger for a portalled Chakra `Popover` (`strategy: "fixed"`, `sameWidth`), so opening it never lengthens the panel or pushes the seat card down. Picking a mode closes it. **Second follow-up, same day, on request:** the orange inline warning ("One friend going to this is not in the invite, so 'Invited only' would hide you from them. Invite them first.") is gone; picking "Invited only" when such friends exist now opens `detail/InviteBeforePrivateDialog`, as the app does. The uninvited-friends query is kept as a prefetch so the dialog opens at once (`fetchQuery` with the same 30s `staleTime`). The pill paints "Invited only" immediately; the chosen invites are sent and awaited *before* the visibility PUT (concurrent writes on one showtime deadlock), and a ref guards against a second answer applying the switch twice.
- [ ] `Showtimes/detail/FriendWatchPills.tsx` — New 2026-09-19. The Letterboxd watch counts as two small pills ("3 watchlisted", "2 watched") under the header's badges, each opening a portalled Chakra `Popover` with the names; the watchlisted one carries the per-friend Invite button the audience-box rows used to (`useShowtimeInvites`, so "Invited" agrees with the invite section). Watched wins over watchlisted for a friend in both. Replaces `ShowtimeAttendance`'s full-width `WatchList` rows, which each cost a line of panel height; up here they sit in room the poster already reserves. Hidden for guests and when both lists are empty
- [ ] `Showtimes/detail/InviteBeforePrivateDialog.tsx` — New 2026-09-19, the web port of the app's `mobile/components/showtimes/InviteBeforePrivateDialog.tsx`, in its words ("Keep these friends in the loop?", Skip / "Invite & continue") and with its `visibility-off` icon. Asked before a switch to "Invited only" when friends already going/interested were never invited: a checklist with everyone ticked (reseeded on every open, not on array identity, since a cached query hands back the same array), and closing it any other way (Esc, backdrop) counts as Skip. Takes `open` separately from `friends` so the list doesn't empty during the close animation. Both of the app's uses are ported: the default copy is the visibility switch's, and `title`/`message` props carry the second ("Let these friends know?", `detail/ShowtimeStatusSection`)
- [ ] `Showtimes/detail/ShowtimeStatusSection.tsx` — New 2026-09-19. The status buttons plus the app's "Let these friends know?" question: stepping from no status to going/interested, when friends already attending wouldn't see it (your mode or a per-friend opt-out — `getHiddenAttendingFriendsForShowtime`), asks via `InviteBeforePrivateDialog` first; switching between going and interested never asks. The list is prefetched while there is a first status to set (30s `staleTime`), so the press asks at once. "Invite & continue" paints the status immediately and passes the invites as `setStatus`'s `waitFor`, so the write follows them. Rendered inside the panel's per-screening keyed body so an answer can't land on another showtime; a ref stops a second answer from setting it twice
- [x] `Showtimes/detail/FriendBadges.tsx` — The audience as the app draws it: a wrap of name pills, each with a status dot in the status's own colour and the seat in brackets when they told us. Carries no Letterboxd mark per pill — those hung off the end of a name as a glyph you had to hover to identify, and are named rows in `ShowtimeAttendance` now. Friends of friends are dashed and muted, the app's mark for "reachable through someone, not actually yours" — the distinction earns its keep, because they are who an invite has to travel to. A name links to that person's agenda, since "who else is going?" is almost always followed by "what else are they going to?". **Follow-up 2026-09-19, on request:** each pill now carries the person's profile photo (`PersonAvatar`, 18px, initial fallback) where the status dot was, in the same pill height (22px), with the full name still beside it and the whole pill still a link; friends of friends keep the dashed, muted look that now alone marks them, since the dot's filled/hollow distinction went with the dot.
- [x] `Showtimes/detail/panel-icons.ts` — The panel's icons, named after the app's. The app draws this screen with MaterialIcons and `react-icons/md` has the same set, so every icon is the *same* glyph rather than the nearest Feather lookalike; the two clients had been showing a slash and a cross, a star and a bookmark, an eye and a "visibility" for the same three things. Keyed by the MaterialIcons name so the app's source is greppable from here. One exception, `shareLink`, is named for what it does rather than for a glyph: Share copies a link where the app opens a share sheet, and Feather's diagonal chain reads as a URL where Material's horizontal `link` does not. 2026-09-19: added `check` and `visibilityOff`, for `InviteBeforePrivateDialog` (the app's `check` / `visibility-off`). 2026-09-20: added `chevronLeft`, for the films lab's "Back to <film>" step out of a screening's panel (since deleted with that panel), and `letterboxd` — `SiLetterboxd`, the second non-Material glyph here, because a film's Letterboxd page is a link off this site and the brand's own mark is what makes that obvious.
- ~~`Showtimes/cards/card-variants.ts`~~ — deleted 2026-09-20 (the wall won; the registry, the localStorage store and the dev switcher went with the variants it chose between). `PORTRAIT_MIN_COLUMN_WIDTH` and the intrinsic height now live with the card and `Feed/ShowtimeFeedPage.tsx`
- ~~`Showtimes/cards/CardVariantSwitcher.tsx`~~ — deleted 2026-09-20 (nothing left to switch between)
- [ ] `Showtimes/cards/TicketRoot.tsx` — **Renamed from `TicketShape.tsx` 2026-09-20.** The pressable root (keyboard activation, box-shadow resting/hover layers); the shape is passed in as `shape`. The prop-driven `TicketShape` — notches, border and tear lines from props, drawn with CSS masks — went with the two list variants that used it; the wall draws its own shape from static CSS. Plain elements; the shadow is a box-shadow layer rather than `filter: drop-shadow`, which was most of the raster cost of hovering and selecting
- [ ] `Showtimes/cards/card-parts.tsx` — The shared pieces every variant is built from (tags, seat mark with optional seat count, friend pills, avatar stack, Letterboxd counts, invite line, lazy poster), the derived facts (tone, a string-parsed `when`, credits), `tokenVar` and `paletteClass`. Plain elements and classes: Chakra style props were more than half the render cost of a page of cards. `SeatMark`'s `withCount` label is `shared`'s own `formatSeatCount` ("31 of 120 seats left" when the room's capacity is known, "31 seats left" otherwise) rather than a local seats-left-only string, so every client says the same thing. `AvatarStack` draws each person's Letterboxd photo when they have one (an `<img>` layered over the initial, hidden in place on `onError` so a broken/stale URL falls back to it without a re-render), and hovering or focusing it opens a list of everyone in it (photo, name, a link to their agenda), the one place in this file that uses Chakra rather than plain elements: `HoverCard` (`lazyMount unmountOnExit`, so it costs nothing for a card never hovered), portalled — a card sits in a `content-visibility: auto` cell, which clips paint to its own box regardless of how a plain-CSS popover inside it was positioned, so escaping the card needed a real portal, not just `position: absolute` — and positioned below the stack with the library's own hover-intent tolerance for the pointer crossing from stack to list. Which edge the list lines up on (`align`, `bottom-start`/`bottom-end`) is the caller's call — a stack pinned to a card's own right edge needs `bottom-end`, or the list hangs off toward the middle of the viewport instead of under the stack. The list opens with the viewer's own seat first (`PanelIcon.eventSeat`, a rule under it, worded "Your seat" rather than just "Seat" so it doesn't read as one of the friends' below it), when they have one, and each friend who's going with an assigned seat gets theirs printed under their name — `friends_going`/`friends_interested` already carried `seat_row`/`seat_number` per person from the backend (`AudiencePerson.user`'s `Pick` widened to include them), unused until now. The avatar (or the seat icon, on the "Your seat" row) sits on the right of each row, text on the left — text before the avatar in markup with `justify-content: space-between`, not `flex-end`, which was tried first and put the text flush against the avatar on the right, reading wrong. A row's click stops its own propagation (still reaches the card in the React tree despite the portal) so visiting a friend doesn't also select the card's showtime **2026-09-19, on request:** `SeatMark` only draws for `very_busy` (the three-figure icon), `last_few` and `sold_out` — `isBusySeatAvailabilityLevel`, new in `shared/showtimes/seat-availability-level`. It used to draw every level with a count on the ticket wall and every level but the calmest elsewhere, which put a badge on most cards. The panel's "Available seats" still states every level. **2026-09-20, the wall won:** the pieces only the dropped variants used are gone — `FriendPills` (name pills; the wall shows faces), `WatchCounts` (Letterboxd counts, dropped from the wall's stub), `creditsLine`, `toneGlow` — as are the `CardContent` knobs and `DEFAULT_CARD_CONTENT`, which only the lab ever switched; `ShowtimeCardVariantProps` is now `ShowtimeCardProps` without its `content` field. What's left is what the wall and `Feed/FeedOverviewPanel.tsx` draw **2026-09-20, for the film card lab:** the hover list is split out of `AvatarStack` as `AudienceHoverCard` — same popover, same rows, same portal, but the trigger is now the caller's child, so a stack of faces that is not a `ShowtimePublic`'s (a *film*'s audience, or one screening's on a film card) opens this list rather than a second one built to look like it. `AvatarStack` is that component plus the stack it always drew; `HoverCardPerson` is the widened shape it takes (`AudiencePerson` is assignable to it). Same day: `when()` no longer reads the clock — its per-minute calendar is gone and the day comes from `features/showtimes/day-clock`, with the "Today/Tonight/Tomorrow" rule itself in `shared/showtimes/day-label`. Any card calling `when()` has to hold `useDayClock()`, or it keeps the label it was last rendered with
- [ ] `Showtimes/cards/card-parts.css` — Stylesheet for the pieces and the ticket root: one class per palette trio setting `--p-*`, all colours from Chakra's own CSS variables so dark mode needs nothing. The going/interested avatar ring reads `app.friendGoing`/`app.friendInterested` (the status axis's own palette, not `green`/`orange`) at its `secondary` step and a 2px band — `border` at 1.5px was too thin and too pale to tell the two apart at a glance, and a first pass at 3px read as too heavy. In dark mode `secondary` equals `border` (both a pale mint/peach that barely reads as green vs orange), so a `.dark .mk-avatar--going`/`--interested` override sets two hardcoded vivid colours (`#34d399`/`#fb923c`) instead — a deliberate one-off scoped to this ring alone, on request, rather than a change to the shared palette (`shared/theme/colors.ts`, also mobile's); every other going/interested marker on this page (`FriendBadges`'s pill dot, say) still reads the paler shared token, so this ring is now visibly more saturated than those until/unless the palette itself changes. **Follow-up 2026-09-19**: `.mk-avatars-pop`'s hover popup toned down on request ("remove the little popup... make it more subtle") — the three-layer shadow (blur-30 dark spread + blur-8 + hairline) collapsed to one soft `0 4px 14px` shadow plus the hairline, radius dropped 12px→10px, and the open/close animation's travel shrunk (`scale(0.97) translateY(-4px)` → `scale(0.99) translateY(-2px)`, both durations trimmed ~20ms) so it reads as a quiet card rather than a dialog popping in. **Second follow-up, same day**: turned out "the little popup" meant the card itself — `.mk-ticket:hover`'s `translateY(-2px)` lift, which read as the whole ticket popping toward the pointer. Removed the transform entirely (every card variant shares `.mk-ticket`, not just the wall); hover is now only `--mk-lift`'s shadow fading in, and it too was dimmed (`0 6px 14px @0.12` → `0 3px 8px @0.08`) so nothing about hovering moves, only a faint shadow appears. **Third follow-up, same day**: that shadow was invisible in dark mode (a black drop shadow under a card already on a dark page) and too faint generally — bumped to `0.22` opacity, and `.dark .mk-ticket__lift` gets its own treatment, a light 1px ring plus a stronger shadow, since dark mode needs something lighter than the page to show at all, not just a darker dark **2026-09-20:** the `.mk-pill*`/`.mk-pills*` and `.mk-watch*` blocks went with `FriendPills` and `WatchCounts`, and `.mk-ticket__border`/`__fill`/`__tear` with `TicketShape`; `.mk-ticket__shadow`/`__lift` keep the root's own layers
- ~~`Showtimes/cards/AppDetachedCard.tsx`~~ — deleted (variant dropped)
- ~~`Showtimes/cards/StubLeftCard.tsx`~~ — deleted 2026-09-20 (variant dropped; was the default until the wall won)
- ~~`Showtimes/cards/CinematicCard.tsx`~~ — deleted (variant dropped)
- ~~`Showtimes/cards/CompactStripCard.tsx`~~ — deleted (variant dropped)
- [ ] `Showtimes/cards/PortraitTicketCard.tsx` — **The showtime card, 2026-09-20 on** ("i only want to use ticket wall"): upright tickets the feed lays out as a grid (`FeedLayout` `grid`), at most 5 columns, at least `PORTRAIT_MIN_COLUMN_WIDTH` each. A plain rounded frame plus one seam element at the top of the stub that paints both notches (in the page colour, fading out past the edge so a glow dips around them) and the tear with background gradients — CSS masks were most of the raster cost of hovering and scrolling, and could not shape the glow; no resting shadow on a plain ticket (~40% of scroll raster at 2x); every stub row fixed-height so all tickets match. The seat/invite line shares its row with the friends avatars (left/right of the same line, `AvatarStack`'s hover list right-aligned via `align="end"` since the stack itself sits at the row's right edge) rather than getting a row of its own — both are rarer than not, and a row reserved just for one on top of the other's was mostly dead space. The cinema/subtitle row is last, after that combined row rather than right under the date — a first attempt at the empty-row problem skipped rendering empty rows entirely and let the stub's fixed height distribute what was left as gaps (`justify-content: space-between`), but that read as worse, not better; this way the one row every ticket always has something in anchors the bottom, and empty space sits in the middle where it doesn't read as a hole right at the edge. Busyness is a colour-coded seat count on the poster; the date is the bigger chip, on the left, with the time smaller and plain beside it on the right — a wall of showtimes mostly starting on the hour is told apart by day, not by time, so the date carries the weight the time used to; a tinted ticket's seam is a half-strength hairline. **Follow-up 2026-09-19**: tried a `.mk-wall__check` badge (round, `app.tint` fill, `MdCheck`, top-right over the poster) fading/scaling in on `aria-pressed`, for a nicer selected state than a flat border — removed same day on request ("remove that checkmark"); the glow-ring change in `PortraitTicketCard.css` below is the selected-state fix that stuck **2026-09-20:** takes `ShowtimeCardProps`, with seats and friends always drawn — the `content` knobs that could hide them only ever moved in the lab. Also holds `useDayClock()`: it is memoised, so without a subscription to the page's day it kept the date label it was last rendered with, and a wall crossing midnight relabelled itself a card at a time
- [ ] `Showtimes/cards/PortraitTicketCard.css` — The wall card's shape (tone, glow and selection as classes setting variables, no per-card style strings) and its content rows, each a fixed height with `margin-top` in the order the `.tsx` renders them (when → note+friends combined → tags), so an empty row still holds its place. `--mk-glow`'s resting shadow and hover lift are toned down from an earlier pass that read as too dramatic (lower blur/spread and opacity on both). The date chip's background on a toned (going/interested) ticket is an opaque neutral (`bg-panel`) rather than a tint mixed from the same palette as the card's own face — a chip mixed from the palette it sits on read as barely a different shade of the same colour ("washed out", especially in dark mode where the palette's `primary` and `border` sit close in lightness); breaking it out into an unrelated, un-toned surface is what makes it read as a chip at all. **Follow-up 2026-09-19**: `[aria-pressed="true"]`'s selected state was a flat 2px `app.tint` inset ring; now it's a thinner 1.5px ring plus a soft `app.tint` glow riding on the existing `--mk-shadow` var (so it composites with the card's resting shadow rather than repainting a new layer) — briefly paired with a `.mk-wall__check` badge, removed same day (see `PortraitTicketCard.tsx`'s entry). Also same day, on request: the seats-left badge (`.mk-wall__seats`) moved out of the poster into the card's own top-left corner, 14px → 6px, so it straddles the poster's edge (which sits at 8px) and covers less artwork — a first step to 11px (still inside the poster) wasn't far enough, and 4px sat too tight against the card's edge
- ~~`Showtimes/lab/` (whole directory: `ShowtimeCardLab.tsx`, `lab-samples.ts`)~~ + `routes/_layout/lab.cards.tsx` — deleted 2026-09-20, "remove the other options beside ticket wall, i only want to use ticket wall". Same ending as the filter lab below: the design was picked by eye in the real feed, so the side-by-side page and its made-up sample rows had nothing left to show. Never committed, so no git history lost
- ~~`Feed/lab/` (whole directory: `FilterLab.tsx`, `filter-facets.ts`, `lab-kit.tsx`, `FilterRail.tsx`, `CinemaSection.tsx`, `CinemaSheet.tsx`, `CinemaChecklist.tsx`, `PresetManager.tsx`, `RangeSlider.tsx`, `cinema-search.ts`, `use-cinema-preset-state.ts`, `use-remembered-language.ts`)~~ + `routes/_layout/lab.filters.tsx` — deleted 2026-09-19, "ok i like this general style, remove the lab and put it in the showtimes page". The design it settled on (variant O, then the requirements-list rebuild of 2026-09-19) moved into the shipped rail — see `Feed/FeedFilterRail.tsx` and the new real files it pulls in below; most of the lab's own files were ported over nearly verbatim. Never committed, so no git history lost. `routeTree.gen.ts`'s `/lab/filters` entries were hand-removed (the generator throws repo-wide, same as the `/lab/cards` note); `/lab/cards` (the unrelated showtime-card lab) is untouched
- [ ] `features/showtimes/use-cinema-preset-state.ts` — New 2026-09-19, ported from the lab almost verbatim (it was always real-data: preferred cinemas, named presets in the device order the app keeps, and every preset write — set preferred, make a preset preferred, create/update/delete, reorder — over the same `MeService` endpoints `Feed/CinemaPresets.tsx` used to call inline). Feeds `CinemaSection`/`CinemaSheet`/`PresetManager`
- [ ] `features/showtimes/use-optimistic-feed-params.ts` — New 2026-09-19. The rail's filters ahead of the URL: a change paints from local state at once and is written to the URL after that frame (`requestAnimationFrame` → `setTimeout(0)`), each key stepping aside once the URL matches it (JSON-equal), with a 1.5s give-up for a value the URL spells differently. Every write sends all unconfirmed keys, not just the newest, because `useFeedParams.setParams` patches the URL as of its own last render and would otherwise write an earlier, not-yet-landed change back out. Why it exists: TanStack's `navigate` lands a render later, behind the feed re-rendering, and every rail control read its state from the URL — measured, a switch now paints 23-52ms after the click where the URL (and it, before) landed at 195-281ms **2026-09-20:** moved up a level — `useFeedParams` holds it now, so the rail and the feed share one overlay instead of the rail keeping a private one. Also returns `pendingKeys`, so a caller can tell a filter change from a keystroke.
- [ ] `components/Auth/AuthShell.tsx` + `auth.css` — New 2026-09-23. Shared frame for /login and /signup (brand + "Back to showtimes", title card, a second card with a real button to the other form, "Browse without an account"), and `AuthField` (label, error, show-password toggle). Replaces the old Chakra Container/InputGroup forms. recover-password / reset-password still use the old look. Optional `icon` above the title (2026-09-23, for /verify-email) with `.au-status-icon` tones in auth.css
- [ ] `components/Auth/SocialSignIn.tsx` + `social-sdk.ts` — New 2026-09-23. Web Apple/Google sign-in at the top of the login/signup cards; SDKs loaded on demand from their hosted scripts, each button hidden while its `VITE_*` client ID is unset. `AuthShell`'s `switchTo` is now optional (for /pick-username)
- [ ] `components/Intro/IntroDialog.tsx` + `IntroHost.tsx` + `intro.css` — New 2026-09-23. Web first-run intro after signup: cinemas (skipped when a guest set was carried over) → Letterboxd → status buttons → friends (reuses `Friends/InviteCard`). App's copy; no notifications page, no spotlight tour. Host mounted in `_layout`. Letterboxd page: after the username saves, a "Use your Letterboxd picture?" card (`LetterboxdAvatarPrompt`, preview + Not now / Use my picture) replaces the page before moving on.
- [ ] `features/intro/intro.ts` — New 2026-09-23. Intro pending flag in localStorage (`mikino.intro.pending.v1`, `{ skipCinemas }`), set by `routes/signup.tsx`, cleared by skip/finish
- [ ] `features/showtimes/guest-preferred-cinemas.ts` — New 2026-09-23. A guest's preferred cinemas in `localStorage` (key `guest_preferred_cinemas`), and `usePreferredCinemaIds()`, the one read for "your cinemas" signed in or not (account query when signed in, this browser's set when not). Replaced `useFetchSelectedCinemas` in useFeedParams, FeedFilterRail, FeedPresets, FeedSubjectHeader, MoviePage; `use-cinema-preset-state.saveAsPreferred` writes here for guests. `claimGuestPreferredCinemas` hands it to a new account in `routes/signup.tsx`. `useFeedParams.queryParams` spells a guest's set out in requests (an empty list means "the account's" server-side). `Movies/Filters.tsx` still reads the account hook — nothing imports it, candidate for deletion
- [ ] `features/showtimes/use-remembered-feed-style.ts` — New 2026-09-23. "Remember my choice" for the rail's Feed style switcher (ticket wall / movie rows = `FeedParams.group`), same shape as `use-remembered-language.ts`, key `mikino.filters.feedStyle.v1`; restores a remembered "movie rows" once on open (group is off by default, so only that has anything to restore)
- [ ] `features/showtimes/use-remembered-language.ts` — New 2026-09-19, ported from the lab (storage key renamed from `mikino.filterLab.language.v1` to `mikino.filters.language.v1`, the lab prefix dropped now that this is shipped). "Remember my choice" for the English-only toggle, kept in `localStorage` (the account has no field for it yet) and restored when the URL says nothing about language. **2026-09-23:** `skipLanguageRestore` export, for `unfiltered-links`
- [ ] `features/showtimes/unfiltered-links.ts` — New 2026-09-23. `UnfilteredLinksContext` (provided by `Activity/ActivityPage`) + `useUnfilteredLinks`: film/friend/cinema links under it add `allCinemas` (unless they name cinemas) and skip the remembered-language restore, so the page opens unfiltered. Used by the showtime detail header, `FriendBadges`, `PersonChip`, `CinemaTagLink`, `AudienceHoverCard`, `ActivitySummary`
- [ ] `features/showtimes/cinema-search.ts` — New 2026-09-19, ported from the lab almost verbatim. Cinema search matched the way the server's search-by-cinema matches: accent- and case-insensitive substring over name *or* any alias, separators `- ' . /` and spaces stripped. Used by `CinemaSheet`'s picker — the rail itself no longer has an inline cinema search, since picking now happens entirely in the sheet

- [ ] `Common/DeferredPage.tsx` — New 2026-09-23. Tab routes (feed, films, activity, friends) wrap their page in it: commits a spinner, mounts the page two frames later. TanStack Router navigates inside `startTransition`, which held the old page on screen until the new one finished rendering, so tab switches felt sluggish
- [x] `Common/RequireAccount.tsx` — Per-page sign-in panel for the five routes that cannot mean anything without an account (agenda, friends, invites, settings, admin). Replaces the `_layout` `beforeLoad` redirect, which covered the browsing pages too and is what made the whole website account-only


**Common (shared UI):**
- [x] `InstallAppGate.tsx` — Wraps the shared-link routes (`/ping`, `/movie`, `/add-friend`): a phone with no web session gets the store instead of a login wall, because an installed app would have intercepted the link before the browser saw it. A panel rather than a redirect — universal links do fall through with the app installed, and a wrong guess must cost one tap, not a store bounce **Reworked 2026-09-23:** full-screen prompt with one filled install button (store badges dropped), personalised headline/card from `features/install-prompt.ts`, and it also wraps `routes/_layout.tsx` for plain visits, where skipping is remembered.
- [ ] `features/install-prompt.ts` — Public reads for the install prompt (sender name, screening, film) + screening time format. New 2026-09-23.
- [ ] `Layout.tsx` — Page layout wrapper
- ~~`Navbar.tsx`~~ — deleted (an unrendered stub commented out of `_layout`; `TopNavBar.tsx` is the real thing)
- ~~`Sidebar.tsx`~~ + ~~`SidebarItems.tsx`~~ — deleted, replaced by `TopNavBar.tsx` + `NavTabs.tsx`. The rail cost 264px of every screen and, worse, had to be *cleared* by every layout beside it (`TopBar`, `Page`, `FeedLayout` all read a `SIDEBAR_WIDTH_CSS_VAR`), which is what put the feed behind four stacked columns. There is no `NavRail.tsx` — an earlier pass listed one here that was never written
- [x] `TopNavBar.tsx` — The site's primary navigation: brand, tabs, search, account, across the top. In the document flow rather than fixed, which is the point — a bar that takes its height off the top is cleared by nobody, so no layout below it carries an offset for it. The search field sits here because it was the only thing left in the bar above the feed, and a full-width band spent on one control is a band the list does not get — while the nav's middle was empty. Which page is being searched is not this file's business: it renders whatever is in `Feed/feed-search-slot`, so the field is simply absent on Friends and Settings. The notification bell sits with the account chip (2026-09-22). Narrows to brand + bell + account below `md`, where `BottomNavBar` owns the tabs and the search goes back above the feed the way the app does
- [x] `NavTabs.tsx` — The app's four tabs laid across the bar, off `nav-items.ts`. Active state is a tinted pill (`app.green.primary` + `secondary`, the palette's own accessible pairing for ink on a fill). No collapsed state, no edge marker, no per-label tooltip: those all existed to survive the rail losing its width, and a horizontal bar never does. Labels hide below `lg`, where the icons and badges carry it and the name stays for a screen reader
- [x] `NavBrand.tsx` — The logo lockup and the way home, horizontal where login/signup stack it; the wordmark drops on a phone and the mark stays. Names itself with `aria-label`, since the mark is decorative and the wordmark is conditional (was `SidebarBrand.tsx`)
- [x] `NavAccount.tsx` — The account chip at the right of the nav: avatar, name, email, and a menu with profile + log out, dropping downward from the bar. Log out used to be reachable only from a page's own user menu, which not every page had. A guest gets a sign-in button in the same slot, since browsing without an account is a supported way to use the site (was `SidebarAccount.tsx`)
- [x] `nav-items.ts` — The one definition of the primary nav (entries, order, icon size, badge cap), shared by `NavTabs` + `BottomNavBar` so the two web navs cannot drift from each other or from the app's tab bar. Films and the agenda are deliberately absent: films is the Showtimes feed's `?group=` mode, and the agenda is the Activity page's "You" mode. **2026-09-22:** Activity now goes to `/activity`; the old `/pings` entry is "Notifications" (bell) and carries the unseen-count badge, renamed `activity` → `notifications` in `NavBadge`/`useNavBadgeCounts`. Later the same day the Notifications entry was removed altogether: it is the bell in `TopNavBar` now (`Notifications/NotificationBell.tsx`), which still reads the `notifications` count from `useNavBadgeCounts`
- [x] `BottomNavBar.tsx` — Mobile-web bottom navigation. Was six bare icons in an order of its own; now the same four tabs as the top bar, labelled and badged like the app's bar. It owns the tabs on a phone, which is why `TopNavBar` drops its own row of them below `md`
- [x] `TopBar.tsx` — The *secondary* bar a page can fix under the navigation (the film page's day filters, the friends page's search) — not the site nav, which is `TopNavBar`. Full width now that there is no sidebar to start after; all it clears is the notice banner and `TOP_NAV_HEIGHT`, neither of which changes width ⚠️ **Unreferenced since 2026-09-22** — the friends page was its last user; awaiting deletion
- [ ] `UserMenu.tsx` — User avatar dropdown ⚠️ **Unreferenced since 2026-09-22**: its only caller was the film page's old secondary bar (Filters + User menu), removed on request. Delete it
- [x] `Page.tsx` — Page container with consistent padding. Clears the one piece of chrome still fixed, the secondary `TopBar`; the nav is in the flow, so a page simply starts where its parent ends
- [ ] `SearchBar.tsx` — Reusable search input. Gained a `leftSlot` rendered in the same row and stretched to the field's height, so the feeds' Filters button lines up with the search box without either side hardcoding a height. Each dropdown option carries its own icon (three of the five are film-related, so they need distinct silhouettes — note `movie-creation` draws the same clapperboard as `movie` despite its own codepoint), and `getSearchFieldLabel` exports the dropdown's wording for anything else that has to name a field ⚠️ **Unreferenced since 2026-09-22** (the friends page's search was its last user); awaiting deletion
- [ ] `CinemaBadge.tsx`, `FriendBadge.tsx` — Badge components ⚠️ `CinemaBadge.tsx` is **unreferenced since 2026-09-22** except by the equally orphaned `CinemaToggle.tsx`
- ~~`Badge.tsx`~~ — deleted 2026-09-21; its last caller was `Movie/MovieLinks.tsx` (the two commented-out imports in `CinemaToggle.tsx`/`CinemaBadge.tsx` predate it)
- [ ] `CinemaToggle.tsx` — Cinema selection toggle ⚠️ **Unreferenced since 2026-09-22**: only `Movies/CityCinemas.tsx` used it, and that went unreferenced with the film page's old Filters dialog. Delete it (it is also `CinemaBadge.tsx`'s only caller)
- [ ] `DayFilter.tsx` — Day-of-week filter control ⚠️ **Unreferenced since 2026-09-22**: only the film page's old Filters dialog (`Movies/Filters.tsx`) used it. Delete it
- [ ] `MyButton.tsx` — Styled button wrapper ⚠️ **Unreferenced since 2026-09-22**: only `Movies/FilterButton.tsx` used it. Delete it
- [ ] `NotFound.tsx` + `Forbidden.tsx` — Error pages

**Movies list:**
- [ ] `MoviesPage.tsx` — Top-level movies list page
- ~~`MoviesContainer.tsx`~~ — deleted (see `Frontend — Routes` above: width/centring moved to `Feed/FeedLayout`)
- ~~`Movies.tsx`~~ — deleted (rendered through `MoviesPage` inside `FeedLayout` now)
- ~~`MoviesTopBar.tsx`~~ — deleted (replaced by shared `Feed/FeedToolbar`)
- ~~`MovieCard.tsx`~~ + ~~`MovieInfoBox.tsx`~~ + ~~`MoviePoster.tsx`~~ + ~~`MovieTitle.tsx`~~ + ~~`OriginalTitle.tsx`~~ + ~~`ShowtimeList.tsx`~~ + ~~`ShowtimeInfo.tsx`~~ + ~~`MoreShowtimes.tsx`~~ + ~~`MoreCinemas.tsx`~~ — deleted 2026-09-21, the old row and its whole exclusive chain, replaced by `Movies/cards/FilmRow.tsx` (see `Movies/cards/` above)
- [ ] `Filters.tsx` + `FilterButton.tsx` — Filter panel ⚠️ **Unreferenced since 2026-09-22**: the film page's old secondary bar was its only caller, removed on request. Delete both (with `CityCinemas.tsx`, `Common/DayFilter.tsx`, `Common/MyButton.tsx`, `Common/CinemaToggle.tsx`, `Common/CinemaBadge.tsx`, `Common/UserMenu.tsx`)
- ~~`CinemaBadges.tsx`~~ — deleted 2026-09-21 with the old row chain
- [ ] `CityCinemas.tsx` — Filter chips (`CinemaBadges.tsx` and `FriendBadges.tsx` were the other two; gone with `MovieCard.tsx` and with the movie page's `ShowtimeRow.tsx` respectively) ⚠️ **Unreferenced since 2026-09-22**, with `Filters.tsx`
- ~~`FriendBadges.tsx`~~ — deleted 2026-09-21; its last caller was `Movie/ShowtimeRow.tsx`. `Showtimes/detail/FriendBadges.tsx` is a different, live component
- [ ] `WatchlistToggle.tsx` — Add/remove from watchlist (already unreferenced, predates this pass)
- [ ] `FetchWatchlistButton.tsx` — Sync Letterboxd watchlist (already unreferenced, predates this pass)

**Movie detail:**
- [x] `MoviePage.tsx` — A film's own page, rebuilt 2026-09-21 as **the films feed's row with more room** ("can you make the movie page look very much like a movie row, just using some more space"), then on 2026-09-22 taken *out* of the row's card ("dont have the movie page litterally be in a card / row") and stripped of the old secondary bar ("remove the old navbar from the movie page"). It was the last screen still drawing the pre-lab design: Chakra headings, a 250px poster, the run as `Day`/`ShowtimeRow` green/orange bars opening a centred Going/Interested dialog, under a fixed `TopBar` with a green "Filters" button (cinema + day dialog) and a "User" menu. Now: the site nav, then `MovieDetail.tsx`, with the shipped `ShowtimeDetailPanel` docked beside it on a plate press, as in both feeds. The dialog, its invite list, the ping and going mutations and the hand-written `["movie"]`/`["movies"]` cache patching all went — `useShowtimeSelection` behind the panel already does that (and invalidates `["movie"]` on settle). Selection is held as an **id** and the `ShowtimePublic` rebuilt from the fresh film each render (the `Feed/ShowtimeFeedPage` pattern), so a status change leaves the panel on the new row; a screening that drops out closes it. `?showtime=` still opens its screening once, in the panel. With the bar gone the screenings are still those in the account's saved cinemas (the query still reads them) but **no longer narrowed to days** — the day picker lived in that dialog. No longer wrapped in `Common/Page`: that exists to clear the fixed bar, and its `overflow-x: hidden` makes it a scrollport a sticky panel can never stick in (measured: pushed down by its own `top`, then scrolled away). `CONTENT_MAX_WIDTH` is film max + gap + the panel's widest step, or opening the panel on a wide monitor takes the width out of the film. The pair is centred, so opening a screening slides the film left into the margin; anchoring the film instead (so it narrowed rather than slid) was tried 2026-09-22 and the user preferred the slide. Below `md` the panel opens under the film **2026-09-22 (later):** the docked panel opens/closes on `Feed/useDetailColumn` like the feeds' (gap is the column's own margin now, not the row's), and the panel's screening gets the film's `viewer.friends_watchlisted`/`friends_watched` merged in, so the watch pills (and their per-friend Invite) show again. Also registers `useShowtimePanelSlot` (notification panel opens screenings here; other films' via `useHeldShowtime`)
- [x] `MovieDetail.tsx` + `movie-detail.css` — New 2026-09-21 (as `MovieRow.tsx`/`movie-row.css`). **Relaid out 2026-09-22 for what a film page is** ("change the layout of the movie page to work more for what it is, note that the scroll can go quite far, dont have any text or buttons or info below it, use more of the empty space, expand the watched by and watchlisted sections"): a header — poster | title, meta, cast, synopsis, Letterboxd/Share | friends — then the run at the page's full width, and **nothing after the run** (the counts, buttons and "until …" line used to sit at its end; the counts are now the run's own heading, and the refetch spinner that sat under it in `MoviePage` is gone). The days are **blocks in a grid** (`minmax(330px, 1fr)` — three to a line on a full page, two with the panel open, one on a phone), read like a calendar week, instead of one full-width line per day with one plate in it; a three-week run went from several screens to about one. Friends are **lists of people** (`PersonChip`, the panel's own row, each a link to their agenda) under "Watchlisted by N friends" / "Watched by N friends" in the watch kind's icon and colour, watched winning over watchlisted, folded past six behind "Show all"; absent entirely for a guest or a film no friend has marked, and the synopsis takes the room back. `.mp` is a **size container**, so the header reflows on the page's own width rather than the window's (the docked panel takes a fifth of it): friends move under the synopsis below 860px, everything stacks below 560px. Same plate as the feed at the same size (`Plate`'s `showDate={false}` under the day headings, grouped by *label* so 00:30 files under "Tonight"), same `.fr-act` buttons; the row's own `.fr-film__*` classes are no longer used here. `asFilmSummary` adapts `MoviePublic` to the kit's `MovieSummaryPublic`, counting cinemas/last screening/total off the whole run
- [ ] `ReportShowtimeButton.tsx` — "Report an issue" dialog (incorrect movie/time, etc.). Lost its only caller 2026-09-21 (the movie page's showtime dialog); 2026-09-22 it moved to the foot of `Showtimes/ShowtimeDetailPanel`, signed-in only, so it shows in both feeds and on the movie page. Still lives under `Movie/` — candidate to move to `Showtimes/detail/`
- ~~`Showtimes.tsx`~~ + ~~`ShowtimeRow.tsx`~~ + ~~`Day.tsx`~~ + ~~`Directors.tsx`~~ + ~~`MovieLinks.tsx`~~ + ~~`MoviePoster.tsx`~~ + ~~`MovieTitle.tsx`~~ + ~~`OriginalTitle.tsx`~~ + ~~`ReleaseYear.tsx`~~ + ~~`MovieSynopsis.tsx`~~ — deleted 2026-09-21 with the old movie page; the row and the panel draw all of it, and nothing else referenced any of them

**Showtimes (app-parity rebuild):**
- ~~`ShowtimesPage.tsx`~~ — deleted 2026-09-22: the friend agenda is the home feed's "Only these friends" filter with one friend picked
- [x] `MainShowtimesPage.tsx` — Main showtimes view: 3-zone master-detail (filters | list | detail drawer) **2026-09-22:** one `useFeedParams()` for the page and both feeds (`feedState`), and the layout (ticket wall ↔ film rows) now follows a `useDeferredValue` of `group`: the page on screen shows its loading screen at once (`isSwitchingLayout` on both feed pages) while the other layout is built in the background, instead of the click's own render rebuilding the whole page and freezing with nothing on screen
- ~~`MyShowtimesPage.tsx`~~ — deleted 2026-09-22, on request ("remove your agenda as a separate page and add filters to the more filters section"). Your agenda is the rail's "Only my own plans" switch under More filters now, so it can be left like any other filter; the overview's plans "Show more" turns it on
- [x] `Showtimes.tsx` — Showtime card list (optional select/onSelect for the detail drawer)
- ~~`ShowtimeCard.tsx`~~ — deleted (the feed draws a detached card variant from `Showtimes/cards/` instead)
- [x] `ShowtimeDetailDrawer.tsx` — Right slide-over: set status, audience, invite banner, movie link
- [x] `useUpdateShowtimeStatus.ts` — Going-status mutation with optimistic list patch (candidate to share with mobile)
- [x] `badges/CinemaPill.tsx` + `badges/FriendBadges.tsx` + `badges/SubtitlesBadges.tsx` — Card badges ported from the app
- ~~`ShowtimeInfoBox.tsx`~~ + ~~`DatetimeCard.tsx`~~ — deleted along with `ShowtimeCard.tsx`

**Filters (`components/Filters/`, app-parity rebuild):**
- [x] `FiltersSidebar.tsx` — Always-open desktop filters panel (status, days, subtitles/language, group-by, watchlist, cinemas; time/runtime/lists/presets to come)
- [x] `useShowtimeFilters.ts` — Showtimes-filter state over the shared `useSession*` hooks + derived query filters
- [x] `cinema-grouping.ts` — Group cinemas by city (≥3 → own section, else "Other cinemas"), ported from the app's CinemaFilterModal
- [x] `CinemaFilterSection.tsx` — Grouped cinema chips with per-city + global select-all

**Friends:** (rebuilt 2026-09-22 after the app's Friends tab)
- [ ] `FriendsPage.tsx` — **Rebuilt 2026-09-22.** The app's Friends tab on the web: an Activity-style header ("Friends" + one-line description) with a Friends / Find people segmented control (`?mode=discover`, default omitted), one search box, and the invite card in a sticky side column (under the list on a phone). *Friends* is one list — "Wants to be friends" (orange, call to action), "Requests you sent", "Your friends" (always shown, empty state with "Find people") — filtered locally per keystroke; a query matching nobody offers "Find people instead", which carries the query across. *Find people* searches the server only once something is typed (debounced 280ms, previous results kept up). Switching modes clears the box. Replaced the two-column "All users | requests + friends" layout under a fixed `TopBar`
- [ ] `FriendRow.tsx` — **New 2026-09-22.** The app's `FriendCard`: 40px avatar + name linking to that person's agenda (`/?friends=<id>`), relationship-aware end — Decline/Accept, Cancel, Add, or for a friend `FriendVisibilityChoice` beside the name (wraps under when narrow) — and the ⋮ menu (Remove friend with confirm, Block/Report). `showStatus` adds the dot + "Friend" / "Sent you a request" / "Request pending" line in search results
- [ ] `InviteCard.tsx` — **New 2026-09-22.** The app's "Scan To Add Me" card: your name, a QR code of the link (`qrcode` package, added to the frontend 2026-09-23, drawn as a 2x data URL on a white plate), your `/add-friend/<id>` link (built from `window.location.origin`, so staging links stay on staging) in a select-on-focus field, "Copy link" (says "Copied" for 2s) and, where `navigator.share` exists, "Share"
- [ ] `friend-controls.tsx` — **New 2026-09-22.** Shared by `FriendRow` and `Feed/FeedSubjectHeader` (lifted out of the latter): `useFriendActions(userId, name)` (the five friendship writes + refreshes + error toasts), `FriendButton` (app primary/outline/destructive), `FriendVisibilityChoice` (Always / Only when invited), `RemoveFriendDialog` **2026-09-23:** `FriendVisibilityChoice` now writes through `shared/hooks/useFriendStatusSharing` (was a plain mutation that let go of the pick on settle, so quick toggles across friends visibly flipped back and forth as stale refetches landed)
- [ ] `friends-modes.ts` — **New 2026-09-22.** The two modes' labels, icons, descriptions and placeholders; `parseFriendsMode` for the route
- [ ] `friends.css` — **New 2026-09-22.** `fr-` classes for the page header, mode control, section headings/counts, empty states and invite card (same idiom as `Activity/activity.css`)
- ~~`Friends.tsx`, `FriendsTopBar.tsx`, `UserCard.tsx`, `SearchUsers.tsx`, `ReceivedRequests.tsx`, `SentRequests.tsx`, `FriendStatusSharingToggle.tsx`~~ — deleted 2026-09-22 with the Friends page rebuild (`FriendsPage` + `FriendRow`; the Sharing/Hidden toggle became `FriendVisibilityChoice`)

**Pings:**
- [ ] `PingsPage.tsx` — Showtime pings list ⚠️ Large (259 LOC)

**Settings:**
- [ ] `UserInformation.tsx` — Profile form ⚠️ Large (195 LOC)
- [ ] `ChangePassword.tsx` — Password change form
- [ ] `Notifications.tsx` — Notification preferences
- [ ] `Appearance.tsx` — Theme preference
- [ ] `DeleteAccount.tsx` + `DeleteConfirmation.tsx` — Account deletion

**UI primitives (`components/ui/`):**
- [ ] Review generated Chakra UI wrappers — understand what each one does
- [x] `tooltip.tsx` — Chakra v3 tooltip snippet, added for the collapsed sidebar where hovering an icon is the only way left to read its label. `disabled` renders the child bare rather than mounting a tooltip that never opens

**Admin (`components/Admin/`):**
- [ ] `AdminGuard.tsx` — Renders Forbidden for non-superusers
- [ ] `AdminOverview.tsx` — Analytics overview (logins, feature usage, invite/notification rates)
- [ ] `AdminMovies.tsx` — Movie-record edit form + TMDB lookup-cache override form
- [ ] `AdminShowtimes.tsx` — Showtime search, inline edit, delete
- [ ] `AdminReports.tsx` — Showtime-report triage (resolve/dismiss)
- [ ] `TmdbAmbiguities.tsx` — TMDB lookups that tied between several films, on the Scrapes page (with a count notice on the overview): the tied candidates, which one was picked and by what, and mark-reviewed / correct-match actions
- [ ] `PublishTiming.tsx` — "When cinemas publish" on the Scrapes page: Cineville vs cinema-site source, day range, cinema filter; weekday×hour heatmap (screenings / publish moments), lead-time and our-delay bars, per-cinema table, latest additions

---

## Frontend — Hooks

**Frontend-only hooks (`frontend/src/hooks/`):**
- [ ] `useCustomToast.ts` — Toast notification helper
- [ ] `useInfiniteScroll.ts` — Infinite scroll detection. The observer is rebuilt only when loading can change, not after every render (it walked computed styles up the tree each time, forcing a style recalc on every click)
- [x] `useNavBadgeCounts.ts` — The unseen-activity and pending-friend-request counts behind the nav badges, resolved once for both navs and gated on `useIsSignedIn` so a guest polls neither endpoint
- [ ] `useIsMobile.ts` — Responsive breakpoint detection
- ~~`useSidebarCollapsed.ts`~~ — deleted with the sidebar. A horizontal bar has no width to reclaim, so there is nothing to collapse and no width to publish
- [x] `useThemeColors.ts` — Raw app palette for the active light/dark mode (web mirror of the mobile hook), for dynamic colors that aren't static tokens

**Shared data hooks (`shared/hooks/`):**
- [ ] `useFriendStatusSharing.ts` — **New 2026-09-23** (moved from `mobile/hooks/`). Per-friend "Can see your showtimes" writes: debounced 450ms, one in flight per friend, the picked value painted until the server agrees; `onError` callback per client (app alert, web toast). Used by the app's `FriendCard`/`FriendAgendaOptions` and the web's `Friends/friend-controls`
- [ ] `useAuth.ts` — Login, logout, token management
- [ ] `useGetUser.ts` — Fetch current user
- [ ] `useFetchMovies.ts` — Movie list with filters. Takes an optional `showtimeLimit` (2026-09-20) which is part of the query key, so a caller asking for ten screenings per film does not share a cache entry with one taking the default five
- [ ] `useFetchMovieShowtimes.ts` — Showtimes for a single movie
- [ ] `useFetchMainPageShowtimes.tsx` — All showtimes for the main view
- [ ] `useFetchMyShowtimes.ts` — Current user's selected showtimes (mobile now uses `useFetchAgenda.ts` instead; web may still use this)
- [ ] `useFetchAgenda.ts` — Mobile agenda feed: going + interested + invited showtimes (GET /me/agenda)
- [ ] `useFetchUserShowtimes.tsx` — Another user's showtimes
- [ ] `useFetchCinemas.ts` — Cinema list
- [ ] `useFetchSelectedCinemas.ts` — User's selected cinemas
- [ ] `useFetchFriends.ts` — Friend list
- [ ] `useFetchUsers.ts` — User search results
- [ ] `useFetchReceivedRequests.ts` + `useFetchSentRequests.ts` — Pending requests
- [ ] `useFetchShowtimePings.ts` + `useFetchUnseenShowtimePingCount.ts` — Pings
- [x] `useShowtimeVisibility.ts` — Showtime visibility mode: per-showtime read plus a coalesced batch prefetch that seeds the cache so the showtime sheet opens without a loading state
- [x] `useShowtimeSeatAvailability.ts` — Same batching for how busy a showtime is, plus `useCachedShowtimeSeatAvailability`, a read that never fetches — a list row must not be able to turn a screenful of showtimes into a screenful of requests. Ids the server had nothing for are cached as `null` so they aren't re-asked every render
- [ ] `useFetchFavoriteFilterPreset.ts` — Saved filter preset
- [ ] `useSessionCinemaSelections.ts` — Session-level cinema filter state
- [ ] `useSessionDaySelections.ts` — Session-level day filter state
- [ ] `useSessionTimeRangeSelections.ts` — Session-level time range state
- [ ] `useSessionRuntimeRangeSelections.ts` — Session-level runtime range state
- [ ] `useSessionShowtimeAudience.ts` — Session-level audience filter (mobile no longer uses the Only You / Including Friends distinction; web may still use this)
- [ ] `useSessionShowtimeFilter.ts` — Combined session filter state
- [ ] `useSessionWatchlistOnly.ts` — Session-level watchlist toggle
- [ ] `useTrackEvent.ts` — Fire-and-forget POST /me/events for usage analytics (web + mobile)

---

## Shared (`shared/`)

- [ ] `storage.ts` — Async storage abstraction (localStorage web / AsyncStorage mobile)
- [ ] `types.ts` — Shared TypeScript types across web and mobile
- [ ] `utils.ts` — Shared utility functions
- [ ] `client/` — Auto-generated OpenAPI client (do not edit manually)
- [ ] `authRefresh.ts` — Axios interceptor: transparently refreshes the access token on 401 (moved from `mobile/utils/auth-refresh.ts` so web shares it too)
- [x] `updateRequired.ts` — Axios interceptor: surfaces the backend's 426 Upgrade Required (client-version gate) to the app as a callback
- [x] `theme/colors.ts` — Single source for the app color palette, light + dark (moved out of `mobile/constants/theme.ts`; mobile re-exports it, web builds Chakra tokens from it)
- [x] `filters/day-filter-utils.ts` — Day-selection token model + API resolution, shared by web + mobile (moved out of `mobile/components/filters/`, which now re-exports it)
- [x] `filters/cinema-grouping.ts` — City grouping + canonical `sortCinemaIds`/`serializeCinemaIds` for a selection, extracted from `CinemaFilterModal` so the sheet and the preset tip group and compare selections identically (mobile twin of the web `Filters/cinema-grouping.ts`) (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/cinema-preset-order.ts` — The user's manual left-to-right ordering of their cinema presets, persisted through `shared/storage` so the row comes back in the order they left it rather than the server's (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/cinema-presets.ts` — Shared cinema-preset data access: one query key, `useCinemaPresets`, and `invalidateCinemaPresets` (presets + cinema selections), so the sheet, the tip and tip-eligibility all read and invalidate the same cache entry (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/cinema-selection.ts` — What "which cinemas am I looking at" means, for both clients: resolve the session pick → the account's saved cinemas → every cinema there is, commit an emptied picker back to "every cinema", tell a selection apart from the account's resting one (so sitting on your own cinemas is not counted as a filter), and name a selection by the preset it *is*. Extracted from `mobile/utils/cinema-selection.ts` and `useCinemaSelection` when the website's filter rail turned out to be drawing the raw URL value — which is empty by default — and so showed nothing selected over a feed that was plainly showing cinemas
- [x] `filters/filter-control-metrics.ts` — The numbers behind the filter UI's *action* controls (Filters button, saved-preset buttons), in one place so the same family of control cannot drift into looking like two unrelated things now that they no longer share a row. Holds the squared radius that separates "does something" from the fully-rounded pills and chips that hold state, and the explicit line height every label in these controls needs (ThemedText's default 24pt survives a fontSize override and silently made both buttons 40pt tall). The Filters button takes the larger type of the two: it is the entry to the whole sheet and stands next to a 16pt search field, not in a row of small chips (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/filter-preset-utils.ts` — Canonicalises a page's live filter state into the comparable shape a preset stores, so "is this preset currently applied?" is one deep-equal rather than a per-dimension comparison that drifts every time a dimension is added (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/runtime-range-utils.ts` — Runtime-range tokens (`runtime:min-max`), the bounds they resolve to for the API, and the pill label they render as (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/saved-presets.ts` — The saved filter-preset model: applying a preset to a page's setters, deciding which dimensions a preset leaves untouched, inferring cinema scope, and the query keys the preset caches hang off. The largest single piece of filter logic, and entirely pure (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/shared-tab-filters.ts` — The filter state the Showtimes and Movies tabs share, plus the pill/active-filter descriptors both render from. Takes the two accent trios it needs as a structural `ThemeColors` argument rather than importing a theme, so web and mobile each pass their own (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/time-filter-presets.ts` — The premade time-of-day ranges (morning, afternoon, evening, late) (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/time-range-utils.ts` — Time-of-day range tokens, the hour bounds they resolve to, and their pill labels (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/useDisplayPresets.ts` — Joins the user's saved presets with the premade ones into the single ordered list the presets row renders, and owns the delete mutation (favourite removed 2026-09-23: quick filters have no default any more) (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `filters/preset-apply-signal.ts` — A `useSyncExternalStore` signal fired when a preset is applied, so controls scattered across a screen can react without threading a callback through every level (moved out of `mobile/components/filters/`, which now re-exports it, so the website shares the same logic)
- [x] `moderation/report-reasons.ts` — The reasons offered when reporting a user. Shared because these labels are what a reviewer sees attached to a report, and two clients wording the same enum differently makes the moderation queue harder to read
- [x] `notifications/feed-copy.ts` — How one notification feed item is worded, split out of the app's `NotificationRow` so the website says the same thing about the same event. Only the icon and accent stay platform-specific
- [x] `notifications/preferences.ts` — Which notification preferences exist, their labels, their display order, and how a row maps onto backend fields. Shared because a row is not always one field: "Seat availability" drives both `notify_on_seat_alert` and `notify_on_sold_out`, and the latter has no row of its own, so a client building the list from field names alone would mean something different by the same label. `buildDeliveryUpdate` is the single write path both clients use
- [ ] `showtimes/day-label.ts` — New 2026-09-20. Which day a screening reads as: "Today", "Tonight", "Tomorrow", or nothing (in which case the caller prints the date). In `shared` because it is wording, like the two entries below it — the app has to say the same thing. The rule worth knowing: **a screening in the small hours belongs to the evening you are standing in.** Between 06:00 and midnight, anything on tomorrow's date before 04:00 is "Tonight", because a 00:30 show is sold and planned as part of tonight and a feed that calls it tomorrow at nine in the evening is telling you to come back another day for something starting in three hours. Before six in the morning the relabel is off: at 02:00 the small hours are today already. The reference day is passed in rather than read from the clock, which is what lets a page label every row against one instant — see `frontend/src/features/showtimes/day-clock.ts`. Web only so far; the app still says "Tomorrow"
- [x] `showtimes/seat-availability-level.ts` — How a busyness level is worded, plus the seat-count and "checked 5m ago" formatting, plus the icon and palette that mark it. Split out of the app's module so the website describes *and marks* the same level the same way — the icons are one progression (a lone silhouette, the room filling, a flame, a closed sign) and a client picking its own broke the rank the shape carries. Only resolving the icon name and palette against a theme stays behind in each client 2026-09-19: added `isBusySeatAvailabilityLevel` (`very_busy` and up, a set like `CALM_LEVELS`) for the web cards' seat badge; mobile still uses `isUrgentSeatAvailabilityLevel`.
- [x] `showtimes/visibility-mode.ts` — What each visibility mode promises, plus the icon and palette that mark it, in one place because it is a privacy promise: an app and a website wording — or marking — the same setting differently is how people share more than they meant to. Each client only resolves the palette name against its own theme
- [x] `showtimes/seat-label.ts` — How a row and a seat join into the label the ticket prints. Shared because the join reads the shape rather than assuming one: "F" + "12" is "F12", but "3" + "12" needs the dash or it is a different seat, and the two clients must not disagree about which they print (moved out of `mobile/utils/seat-label.ts`, which now wraps it)
- [x] `showtimes/showtime-time.ts` — "Has it started" and the "20:30~22:36" range. Shared because showtime datetimes are zone-less Amsterdam wall-clock times and both clients have to compare them in that zone — a phone in another timezone must not decide a 20:30 screening is over (moved out of `mobile/utils/showtime-time.ts`, which now wraps it)
- [x] `showtimes/seat-floor-plan-layout.ts` — Turns the flat seat list into rows and blocks with aisles, which is the whole geometry of the floor plan (moved out of `mobile/components/showtimes/`, which now re-exports it, so the website shares the same logic)
- [x] `showtimes/seat-input.ts` — Parses and validates a typed seat label (row letter + number) against a room's seats (moved out of `mobile/components/showtimes/`, which now re-exports it, so the website shares the same logic)
- [x] `users/avatar-color.ts` — Which tint a person's initial circle gets, and the letter in it. Deliberately dumb (char codes modulo the palette) because it has to be *stable*: the same person keeps the same colour across lists, screens and both clients, which anything keyed on list position could not do. Only the palette keys live here — each client resolves them against the same `shared/theme/colors` entry, so neither can drift (moved out of `mobile/utils/avatar-color.ts`, which now wraps it)
- [x] `friends/friend-watch-kind.ts` — The two Letterboxd relationships a friend can have with a film, and the wording, icon and palette each gets. Shared for the reason the per-client copies failed: the app marked "watchlisted" with a clock and the website with a star, for the same fact on the same film (moved out of `mobile/components/friends/friend-watch-kind.ts`, which now wraps it)
- [x] `movies/language.ts` — ISO-639-1 code to a spoken-language name, for `movie.original_language`. Shared so a film is not "Nederlands" on one client and "Dutch" on the other (moved out of `mobile/utils/language.ts`, which now wraps it)
- [x] `movies/synthetic-movie.ts` — Listings with no TMDB counterpart (currently just the sneak preview, whose film the cinemas keep secret) use negative ids and show their unknown metadata as "???". Mirrors `is_synthetic_movie_id` in the backend; shared because the placeholder is wording, and two clients writing it differently is two products (moved out of `mobile/constants/synthetic-movies.ts`, which now wraps it)
- [x] `cinemas/cinema-color.ts` — Which palette entry a cinema's accent is. `badge_bg_color` is a palette *key*, not a colour, and both clients hold the same palette — resolving it in one place is what keeps a venue the same colour in the app's pill, the website's badge and the header of its own page. The name-hash fallback is shared too, so a cinema with no key configured is not a different colour on each client (moved out of `mobile/utils/cinema-color.ts`, which now wraps it)
- [ ] `cineville/surcharge.ts` — "+€5,00" for a Cineville screening that costs extra on top of the pass (`showtime.cineville_surcharge_cents`, read off the Z-ELITE price list by the seat poller). Shared so both clients' Get ticket rows say it the same way (created 2026-09-25)

---

## Mobile — Components (`mobile/components/`)

Only components created or reworked during the cleanup are listed here; the rest of
`mobile/components/` predates this checklist.

- [x] `ui/ConfirmDialog.tsx` — Reusable themed confirm dialog (fade + scale over a dimmed backdrop); the app-wide replacement for `Alert.alert` whenever the user is asked to decide something
- [x] `badges/SeatAvailabilityBadge.tsx` — The busyness icon on a showtime row. Reads the prefetch cache and never fetches, and renders nothing at all when there is no reading — a row of dashes where a real answer sometimes appears is worse than nothing being there
- [x] `showtimes/seat-availability-level.ts` — Resolves a busyness level's palette name into theme colours. The wording, the icon, the palette and the "31 of 312 seats left" / "Checked 4 minutes ago" formatters all come from `shared/showtimes/seat-availability-level`, which the website reads too. Never recomputes a level — the backend hands one over
- [x] `showtimes/visibility-mode.ts` — Resolves a visibility mode's palette name into theme colours; the wording, icon and palette are `shared/showtimes/visibility-mode`, so the website cannot mark a privacy setting with a different glyph than the app does
- [x] `showtimes/InviteBeforePrivateDialog.tsx` — Checkbox-list variant of `ConfirmDialog`, shown right before a showtime switches to INVITED_ONLY when friends are already going/interested but never pinged; offers to invite them (non-notifying) so they don't silently lose visibility
- [x] `showtimes/SeatSheets.tsx` — Owns which seat map is open (the editable `SeatFloorPlan`, the read-only `SeatFloorPlanPreview`, or neither) and is driven by an imperative ref rather than a prop. Exists so opening or closing one doesn't have to wait behind a re-render of `ShowtimeActionModal`, which is what made both feel slow — the sheet animation was never the delay
- [x] `ui/AnimatedHeight.tsx` — Measures its children and tweens its own height to match, for content that grows and shrinks under the user (search results arriving, an empty state replacing a list). Used instead of `LayoutAnimation` where the change comes from a query resolving rather than a tap, since `configureNext` has to be armed before the update that moves things
- [x] `ui/LoadMoreFooter.tsx` — The spinner at the bottom of a paginated list, tweening its own height and opacity so a loaded page glides in instead of snapping up a whole row the instant the spinner unmounts. Shared by every infinite list (showtimes, movies, friends, cinema/friend/movie detail). Not `AnimatedHeight`: there is nothing to measure, and the fade has to run *with* the collapse rather than after it
- [x] `auth/AuthScreenShell.tsx` — The frame every auth screen sits in (brand mark, title, subtitle, keyboard handling, and the error banner, which is tweened in rather than inserted under the user). Exists so log in / sign up / pick a username / recover password cannot drift apart — this is the first thing anyone sees of the app
- [x] `auth/AuthTextField.tsx` — Labelled auth input: focus recolours a border that is always there (so focusing cannot reflow the form), validation messages are tweened in, and a password field gets a reveal toggle
- [x] `auth/AuthPrimaryButton.tsx` — The one call to action on an auth screen; keeps the label mounted under the spinner so a button that starts working does not resize
- [x] `auth/SocialSignInSection.tsx` — "Continue with Apple / Google" plus the divider above the email form, shared by log in and sign up, which used to carry a verbatim copy of both handlers each. Both buttons commit to a busy state on tap, and the Apple button follows the colour scheme (a black button on the dark theme was a rectangle you could only find by its text)
- [x] `auth/GoogleMark.tsx` — Google's four-colour "G", drawn with `react-native-svg` as their sign-in branding guidelines require
- [x] `friends/UserSearchResults.tsx` — The results under a "find a friend" search box, shared by the intro's friends page and the add-friends tip. Debounced, keeps the previous results up while the next load, and tweens its height, so typing does not kick the invite card below it down the page
- [x] `friends/FriendVisibilityControl.tsx` — Per-friend "Can see your showtimes: Always / Only when invited" segmented control (writes `shares_status`). Its own shape — squared corners on a hairline white track, because it sits on the white of a friend row — over the shared `useSlidingThumb` motion: the tint slides between the two answers and crosses green to amber on the way
- [x] `friends/FriendCard.tsx` — Friends-tab row: initial avatar, relationship-aware primary/ghost actions, remove-friend confirm, inline visibility control
- [x] `friends/InlineFriendRequestButtons.tsx` — Icon-only request controls for invite lists; now shares `useFriendActions` with `FriendCard`
- [x] `friends/FriendListRow.tsx` — One person in a friend list: colored initial avatar, name, optional watch marker, and either a labelled "Invite" button (only the button invites, so a stray tap can't) or static/tappable display (renamed from `FriendInviteRow.tsx`, which invited on a whole-row tap behind a bare `+`)
- [x] `friends/FriendWatchListModal.tsx` — Shared "Watchlisted" / "Watched" popup listing the friends behind the small markers; static on a movie page, with invite buttons when opened from a showtime
- [x] `friends/friend-watch-kind.ts` — Resolves the palette name into theme colours; the wording, icon and palette are `shared/friends/friend-watch-kind`, so the website marks watchlisted/watched identically rather than reaching for its own star and eye
- [x] `utils/avatar-color.ts` — Deterministic avatar tint + initial for a user id, shared by every list that draws a person as a colored circle
- [x] `hooks/useFriendActions.ts` — The five friendship mutations (send/accept/decline/cancel/remove) with shared invalidation + error handling
- [x] `hooks/useFriendStatusSharing.ts` — Debounced, serialized writes for the per-friend visibility setting; local intent wins over query data until the server agrees (the backend rebuilds all effective-visibility rows per write, so overlapping requests collide) **2026-09-23:** now a thin wrapper — the logic moved to `shared/hooks/useFriendStatusSharing.ts`; this only supplies the native error alert
- [x] `tips/FeatureTipsHost.tsx` — Renders at most one feature tip: computes each tip's eligibility and lets `useFirstVisibleTip` pick the winner, so the user never faces a stack of nags. Mounted in `app/(tabs)/_layout.tsx` (moved from the Screenings tab 2026-09-24, so a tip shows on whichever tab is open); gated on the tabs' focus, since the tip is a blocking dialog and the tabs stay mounted under pushed screens
- [x] `tips/FeatureTipModal.tsx` — Shared shell for every feature tip: a blocking dialog with icon/title/optional message, an optional inline control, an optional collapsible help section, the action button, and a quiet "Don't show this again" checkbox (pinned below the action, no section of its own) read on whichever way the dialog closes. `density="compact"` shrinks the header for a tip whose own content needs the room, and exports its padding so a child can run full width
- [x] `tips/LetterboxdUsernameTip.tsx` — Tip shown when no Letterboxd username is set; takes the username inline rather than sending the user to Settings
- [ ] `tips/LetterboxdAvatarTip.tsx` — Tip offering the opt-in to use the Letterboxd profile picture as your avatar (off by default); previews the picture, only eligible once a sync has read one and the switch is still off; also opened (no dismiss-forever) by Settings → Letterboxd after a first-time username save; writes via `shared/hooks/useLetterboxdAvatarPreference.ts` (new, shared with Settings on both clients and the intro)
- [x] `utils/theme-preference.ts` — The theme preference store, and the three beats a change of it is played in: the curtain comes down, the preference is applied under it, and it lifts once the app has settled (`runAfterInteractions` plus a frame, capped at 1.5s, held for a 300ms minimum so a fast switch is not a flicker). The switching flag has its own subscriber list — announcing it must re-render one component and nothing else, or it would cost the same full-app render it exists to hide. Startup (`loadThemePreference`) skips all of it: the splash is still up
- [x] `layout/ThemeSwitchOverlay.tsx` — That curtain: full-screen, painted in the palette being switched *to* so it lifts onto a screen the colour it already was. An `ActivityIndicator` rather than the splash's pulsing logo, because `Animated.loop` over a sequence steps between its halves from JS and would stall for exactly as long as the re-render it is covering
- [x] `utils/feature-tips.ts` — Feature-tip store: master switch + permanent dismissals in SecureStore, session snoozes and their notification-centre reminders in memory; subscriber model copied from `theme-preference.ts`. Holds `FORCED_TIP_ID`, the dev override that pins one tip on screen while it's being designed, and `ALWAYS_SHOW_TIP_IDS`, the tips that are obligations rather than suggestions and so ignore the chance, the cooldown, the master switch and permanent dismissal
- [x] `notifications/NotificationRowLayout.tsx` — Shared visual shell for a notification row (accent icon circle, title/subtitle, relative time, unseen dot, dismiss ✕), extracted from `NotificationRow` so backend feed items and local feature-tip reminders cannot drift apart
- [x] `notifications/FeatureTipNotificationRow.tsx` — A snoozed feature tip in the notification centre; tapping it reopens the tip dialog, so a suggestion the user closed is recoverable rather than gone
- [x] `notifications/NotificationPreferenceList.tsx` — The four notification preferences, one line each, as a single `SegmentedControl` per row (replacing a switch plus a second delivery row), with "Off" carrying the neutral thumb so it does not read as switched on; shared by the Settings screen and the notification-permission tip
- [ ] `hooks/useNotificationPreferences.ts` — Preferences state, delivery channels, OS permission and the optimistic writes behind them. Also exports `useSystemNotificationPermission`, `useCanReceivePush` (OS allowed + account `has_push_token`), `isNotificationDeliverable`, and `openSystemSettings`. A push row on a device that cannot receive one reads as off; picking push asks through `usePushPermissionFlow` (2026-09-24: `wantsPushNotifications` removed, native Alert on refusal replaced by the settings-steps dialog, `pushHelpDialog` rendered by `NotificationPreferenceList`)
- [ ] `hooks/usePushPermissionFlow.tsx` — New 2026-09-24. Turning push on everywhere (intro, notification tips, Settings): OS prompt when askable, otherwise a `ConfirmDialog` with per-OS steps to allow it in system settings; finishes by itself when the user comes back allowed. Success = token registered. Calls `notePushDenied` on refusal
- [ ] `tips/NotificationEventTip.tsx` — New 2026-09-24. Shell for the "you missed something" tips: push (via `usePushPermissionFlow`) or email for one preference; confirms in place, closing after a choice leaves no bell reminder
- [ ] `tips/InviteTip.tsx`, `tips/SoldOutTip.tsx`, `tips/FriendRequestTip.tsx`, `tips/InterestRemindersTip.tsx` — New 2026-09-24, thin wordings over `NotificationEventTip`. The first three are `EVENT_TIP_IDS` (no chance/cooldown, fed by `utils/away-events.ts`); interest reminders is low priority. `InviteTip` says "You got an invite!" (upcoming, row opens the screening) or "You missed an invite!" (screening started before the invite was seen; takes precedence). Always the earliest screening
- [ ] `tips/TipScreeningRow.tsx` — New 2026-09-24. Poster/title/cinema/time row used inside the invite and sold-out tips
- [ ] `tips/CinevillePassTip.tsx` — New 2026-09-24. Low-priority tip to save the Cineville pass number inline; says it is not connected to Cineville and stored on device only
- [ ] `utils/away-events.ts` — New 2026-09-24. Per-account "last active" mark in SecureStore (set on background), and `GET /me/away-events` since it on every foreground, for the event tips
- ~~`tips/NotificationPermissionTip.tsx`~~ — Deleted 2026-09-24, replaced by the event tips above
- [x] `tips/WatchlistDigestTip.tsx` — The quietest tip in the app: offers the watchlist digest to someone who has never turned it on. The only tip whose eligibility the *backend* decides (`show_watchlist_digest_tip` on /me = server switch + confirmed address + never enabled), so the feature can sit unadvertised in Settings until it is ready to be pointed at; longest cooldown and lowest show chance of any tip
- [ ] `settings/WatchlistDigestSourcesSection.tsx` — The "Advanced: sources" body of the digest settings: a user may have several `WatchlistDigestSource` rows, each its own frequency/list/cinema card, plus the shared "add a Letterboxd list" row and per-source delete confirm
- [ ] `settings/CustomCinemaPickerModal.tsx` — One-off cinema multi-select for a digest source (`custom_cinema_ids`); wraps `CinemaPickerList` directly rather than the preset-bound `CinemaFilterModal`, since nothing here is ever saved as a `CinemaPreset`
- [ ] `hooks/useWatchlistDigestSources.ts` — Query + create/update/delete mutations for `/me/watchlist-digest-sources`
- [x] `tips/VerifyEmailTip.tsx` — Tip shown while the account's email address is unconfirmed. Unlike the others it is unfinished business rather than a suggestion: highest priority, exempt from the random chance, the cooldown and the Settings switch (`ALWAYS_SHOW_TIP_IDS`), and no "Don't show this again" — closing it only lasts the session. Spells out the address (a typo is the usual reason nothing arrived) and can re-send the link
- [x] `tips/CinemaPresetTip.tsx` — Tip shown when the user still browses every cinema and has saved no preset; opens straight onto the cinema picker (seeded from the current selection, full-bleed and sized to every pixel the dialog chrome is not using, so the save button and dismissal controls never move off screen), then asks for a name in a second dialog and saves it as the favorite
- [x] `filters/CinemaPickerList.tsx` — Compact grouped cinema picker (per-city sections with a select-all shortcut, wrapping outline checkbox chips), shared by the cinema-preset tip and the `CinemaFilterModal` sheet so cinemas are picked identically in both. Per-city deselecting is opt-in (`onDeselectCinemas`): a dialog gets one global clear, the sheet is long enough to want it per section. Chips are outlines so the list suits either surface; presentational, the caller owns the selection
- [x] `tips/FilterPresetTip.tsx` — Tip shown when the user filters showtimes but has saved no preset; offers their current selection plus a catalogue of ready-made presets, each added with its own labelled button, and its action opens the filters modal (deferred to the dismissal so it does not race the fade)
- [x] `filters/premade-filter-presets.ts` — The five suggested presets and the builder that turns one into a `SavedPresetCreate`. Partial presets ("Hide Watched", "Short") name every other dimension, including each Letterboxd list, as untouched so applying them cannot clear filters they never mentioned
- [x] `ui/NamePromptDialog.tsx` — The input-bearing sibling of `ConfirmDialog`: one short string, same fade/scale timing, hands the caller a trimmed name so no flow has to hold draft text of its own
- [x] `filters/FilterSection.tsx` — The two shapes a Filters-modal section can take, together so their heading metrics cannot drift. `FilterSection` is collapsible: uppercase heading, caret that spins 180° on the native thread, children unmounted while collapsed, every section closed by default so opening the modal costs almost nothing. Only the collapse gets the height tween — expanding mounts the content in the same frame and animating that janks. `FilterInlineRow` is the one-control section (Group By, Marked by Friends as, Language): a caret there would cost a tap and save no space, so label and control share a line, and the label wraps rather than squeezing the control. `FilterSubLabel` is the heading one step down, for a block inside a section (Movie Length, Curated lists), shared with `LetterboxdFilters`. An optional `summary` shows what a collapsed section is filtering on next to its caret `FilterHeadingRow` (2026-09-23) is an always-open section heading with an optional right-hand action — Feed style / Language use it for "Make this the default".
- [ ] ~~`filters/LetterboxdUsernamePrompt.tsx`~~ → `filters/LetterboxdConnectCard.tsx` (2026-09-24) — Takes the place of the watchlist/watched filters when they cannot be used: no Letterboxd username, or no account. Collapsed to one row by default and opened on tap: the username field for a member, "Create an account" / "Log in" for a guest (which used to get the full-size `SignedOutPanel` card here). `onLeave` closes the Filters sheet before navigating to the auth screens. Saving invalidates `currentUser`, which flips `canUseWatchlistFilter` and swaps the card for the real controls
- [ ] `utils/username-live-validation.ts` — New 2026-09-24. `shouldValidateUsernameWhileTyping`: sign-up and pick-username check a username on the keystroke once it holds a character that can never be valid or reaches the minimum length, on top of `mode: 'onTouched'` (blur, then live) — they used to check only on submit
- [ ] `filters/LetterboxdFilters.tsx` — Renamed from `FilterMoviesSection.tsx` 2026-09-23 when the Filters modal was reordered to match the web rail. `LetterboxdWatchFilters` is the Letterboxd section (only-watchlist / hide-seen cards, one direction each — hide-watchlist and only-seen were dropped from the UI as too niche; the username prompt or the guest sign-in card in their place); the default export `LetterboxdListFilters` is the lists block inside "More filters".
- [ ] `hooks/useFeedDefaults.ts` — New 2026-09-23. The device-local feed style + language defaults (storage key `feed_defaults_v1`), written only by the Filters modal's "Make this the default"; applied once at startup by `useSharedTabFilters` (which no longer applies a favourite quick filter), read by every clear-all so language resets to its default, and by `ActiveFilterChips` so a default language gets no chip.
- [x] `hooks/useSaveLetterboxdUsername.ts` — The save-username mutation shared by the feature tip and the filters-modal prompt, so both invalidate the same cache entry and report failure identically
- [x] `ui/use-sliding-thumb.ts` — The motion behind a segmented control, shared by `SegmentedControl` and `FriendVisibilityControl` (which cannot share a component — different shape, different palette — but must share a feel): segments measured rather than assumed equal, one thumb tweened to the one picked, and the label crossfades that track it. Nothing animates until the first measurement lands, so a control is never seen sliding into place on mount. The travel is started by the press (`moveTo`), never by React noticing the value afterwards — on the Friends tab the same tap swaps a SectionList for a FlatList and renders a QR code, and a tween that waited for the effect after that commit started visibly late. The layout-effect pass is only for a value changing from somewhere other than a press, and `appliedRef` makes sending the thumb where it is already going a no-op rather than a restarted tween. An optional `externalProgress` inverts the whole model for a control above a pager: the thumb stops being sent anywhere and becomes a readout of the pages, interpolated across the measured segments by a `useAnimatedReaction` so it tracks the finger and never waits on React — the JS round trip is what made it lag a second behind a swipe on Android
- [x] `ui/SegmentedControl.tsx` — Compact pill track with the selected option as a filled thumb, for an either/or choice that should cost one line rather than a wrapping row of loose pills. Per-option thumb colors let a neutral default ("Any") avoid reading as switched on while the real options keep the app's status colors. A `stretch` + `size="large"` pair widens it into screen-level navigation (the Friends tab's Friends / Find people header) without disturbing the compact settings-row default. The thumb travels: it slides and resizes to the segment picked, crossing colour on the way when the two segments do not share one, and each segment stacks a resting-colour and a selected-colour copy of its content that crossfade with its arrival — text colour is not something Reanimated can drive through `ThemedText`. Every either/or choice in the app now goes through it: Light/Dark/System, Off/Push/Email, Eager/Weekly, Friends/Find people, Group By, Marked by Friends. The travel starts on the tap rather than on the answer (`useOptimisticValue`), because `onChange` can re-theme the app or wait on a save. A control labelling a pager passes that pager's `progress` instead: the thumb then follows the pages on the UI thread — tracking the finger mid-drag — rather than being sent to a segment by a render, which on Android left it frozen on the old page for as long as the commit took
- [x] `utils/expand-animation.ts` — Shared caret-spin duration + `LayoutAnimation` config for disclosure toggles (and Android's `setLayoutAnimationEnabledExperimental` opt-in), so `FilterSection` and `ShowtimeActionModal`'s invite/visibility toggles open and close at the same speed
- [x] `filters/DaysFilterSection.tsx` — The Filters modal's Days section: the three relative days as cards labelled with the date they resolve to, the seven weekdays as one chip row, a summary line naming every selection (with Clear), and the row that opens the calendar. Every token the section can produce is reachable without opening a sheet; selections are canonicalized first so an ISO date for today lights up the Today card instead of hiding in the calendar row
- [x] `filters/SpecificDatesModal.tsx` — Calendar sheet behind that row (six months of month grids, applied on close). Draws today/tomorrow/day-after as selected even though they are relative tokens — and clears the token when such a day is tapped — so no day is shown unselected while it is being filtered on. Weekday tokens are left alone, including by "Clear dates"
- [x] `utils/sign-in-notice.ts` — The one-time thing a sign-in has to tell the user afterwards: currently only "linking Apple/Google removed this account's password" (the unverified-account takeover). Set during the sign-in, read by `SignInNoticeHost` in the root layout, because the screen that learns it unmounts on the navigation that follows. In memory only, like `username-gate.ts`
- [x] `auth/SignInNoticeHost.tsx` — Renders that notice as a single-button `ConfirmDialog`, mounted in the root layout alongside `IntroHost`
- [x] `utils/auth-session.ts` — Whether there is a signed-in session, as the app's navigation sees it. The stored token is read exactly once, at startup; every transition after that is announced synchronously by whoever caused it (`completeLogin` in, logout/401 out). Re-deriving it from SecureStore on every route change made the guard disagree with the navigation it was guarding for a frame or two, which showed up as a login that hung and then flicked through the wrong screen on its way to the right one
- [x] `hooks/useOptimisticValue.ts` — The app-wide "paint on the tap" hook: the returned value flips in the frame the press lands, the real `onChange` is deferred a frame so the visual never waits on a re-filter, a re-theme or a save, and the override drops itself once the incoming value catches up. It also gives up after a second, for the change that is refused outright and silently (Email delivery with no verified address) — nothing else would ever put such a control back. Behind every filter pill, the movie-filter cards and `SegmentedControl`
- [x] `hooks/useDebouncedValue.ts` — Settles on a value once it stops changing, so a search field is one request per word rather than one per keystroke
- [x] `hooks/useLayoutAnimatedValue.ts` — Mirrors a value one frame late so the layout change it causes is tweened, for values arriving as props (a form error, a server error) where `LayoutAnimation.configureNext` cannot be armed in time
- [ ] `hooks/useAnimatedValue.ts` — One `Animated.Value`, built once and kept, replacing `useRef(new Animated.Value(0)).current` everywhere. The ref spelling made the React Compiler skip the whole component — silently, no build warning — and it did so in 27 files, all of them the animated ones: every dropdown, every chip in the filter row, the search bar, the dialogs. A lazy `useState` says the same thing to React without the ref, and skips the per-render `Animated.Value` the old spelling constructed and threw away. The Reanimated equivalent of the same trap is writing `sv.value = x` outside a worklet; use `sv.set(x)`
- [x] `constants/auth.ts` — Email pattern + password minimum, shared by the auth screens that were each carrying their own copy
- [x] `utils/intro.ts` — First-run intro store: the pending flag (set when an account is created, so existing users updating the app never get an intro) in SecureStore, the current phase in memory, same subscriber model as `feature-tips.ts`. Cleared on finish/skip rather than on start, so an app killed halfway through gets another go
- [x] `intro/IntroHost.tsx` — Mounts the walkthrough once the user is in the tabs *and* the loaded account has a username (a social sign-in is authenticated while still picking one, and an intro over that form would cover it — or, when a redirect race put the app in the tabs instead, replace it entirely). Only the start is gated; once running the intro stays put. No longer waits for the splash: the intro is a Modal, so it draws *above* the splash overlay, which is exactly what stops the app being revealed for a beat before the walkthrough covers it back up
- [x] `intro/IntroFlow.tsx` — The five pages, the progress row and the always-reachable "Skip tutorial". One Modal for all of them: opaque for the three that own the screen, see-through for the showtime tour, whose sheet is rendered as a sibling of the Modal so it shows behind it rather than under it. Takes the screen instantly and animates its *contents* in (fading the window itself showed whatever it was covering through the fade); leaving does fade, since by then the app underneath is the point. Pages crossfade into each other rather than hard-cutting
- [x] `intro/IntroPageShell.tsx` — Shared shell for a full-screen intro page (icon, title, message, the page's own content, primary button and a quiet escape hatch), the intro's counterpart to `FeatureTipModal`
- [x] `intro/IntroCinemasPage.tsx` — Page 1: the cinema picker, saved as the favorite preset under a fixed name ("Favorites" — a ten-second-old account should not be asked to name anything) and applied to the session; retires the cinema-preset tip. Opens with every cinema ticked ("0 of 24 selected" on a new account's first screen reads as a filter already hiding everything), and clearing them all is taken as saving nothing at all — no preset, and the tip stays eligible
- [x] `intro/IntroLetterboxdPage.tsx` — Page 2: the username, inline, with "I don't use Letterboxd" — which retires that tip for good, so the app never asks again about a service the user has said they don't have
- [x] `intro/IntroShowtimeSheet.tsx` — Page 3's subject: the real `ShowtimeActionModal` over invented data, mounted outside the intro's Modal on purpose (the sheet portals into the app root, and a Modal is a window above it)
- [x] `intro/IntroShowtimeTour.tsx` — Page 3's script and overlay: three steps walking a spotlight across Interested, Going and Invite friends
- [x] `intro/IntroFriendsPage.tsx` — Page 4: search for someone by name, or show the invite QR code — this being the one moment the user is likely to be sitting next to the friend who told them about the app
- [ ] `intro/IntroNotificationsPage.tsx` — Rewritten 2026-09-24: "How do you want to be notified when friends invite you?" — push or email, with a multi-step opt-out (push refused → try again/email/none; "are you sure?" → invites only via push/email → all off). Saves with `app_notifications_prompted: true`. Also the whole notifications-only intro (`utils/intro.ts` mode `notifications`, started by `IntroHost` for accounts never asked on the app). "Skip tutorial" in the full intro now jumps to this page; it has no skip and back does nothing
- [x] `intro/demo-showtime.ts` — The showtime the tour points at: the first real one the list would show (real film, poster, cinema, time), dressed with invented friends and a sentinel id that matches no real showtime
- [x] `intro/SpotlightOverlay.tsx` — Dim-with-a-hole overlay: four panes around a measured rect, a pulsing ring, and a caption card in whichever gap is bigger. Four panes rather than an SVG mask, so the hole is genuinely untouched and a caller can let the real control be tapped through it (`onPressTarget`)
- [x] `intro/IntroFiltersSpotlight.tsx` — The intro's last step, owned by the showtimes screen rather than the flow because it waits for that screen's list to load, its tab to be focused, and nothing else to be open. The highlighted button is real: tapping it opens the filters. Renders nothing until the button has actually been measured — presenting first and measuring after showed the caption in its unmeasured fallback position and then jumped it down to the button. If the measurement never lands it ends the intro rather than leaving the phase parked forever
- [x] `sheets/SheetBackdrop.tsx` — The dimmed backdrop behind every sheet (`AppBottomSheet` and `ShowtimeActionModal`). A near-copy of gorhom's `BottomSheetBackdrop` that separates the fade from touchability: gorhom drops both at `disappearsOnIndex`, which for a modal must be -1, so the invisible backdrop went on swallowing touches for the whole close animation and the screen behind stayed dead for a beat after the sheet was dismissed. Here it stops taking touches as soon as the sheet leaves its open position, while the fade still runs to -1
- [x] `utils/blocking-overlays.ts` — Live registry of the blocking surfaces the layout-level providers own (showtime sheet, filters/cinema sheets, notification centre), for anything that has to be the *only* thing in front of the user rather than merely on top. A set, not a boolean, so a dialog over a sheet unregisters independently. Each surface registers *how to close itself* as well, so the intro can guarantee a clear screen (`closeAllBlockingOverlays`) instead of waiting and hoping for one — a filters sheet left open before a replay was still sitting there when the walkthrough ended. `useIsFocused` cannot see these: they are windows, not routes
- ~~`filters/DayFilterModal.tsx`~~ — deleted (its shortcut lists moved inline into `DaysFilterSection`, its calendar into `SpecificDatesModal`)
- ~~`tips/FeatureTipCard.tsx`~~ — deleted (the inline card was superseded by the blocking `FeatureTipModal`)
- [x] `hooks/useCurrentUser.ts` — The signed-in account (same `currentUser` query key as `shared/hooks/useAuth`, but enabled from the global auth session rather than that hook's own token read, so it is live on every launch and from the moment a sign-in is announced) plus the predicate the app is gated on: `hasUsername`/`isMissingUsername`, positive checks both ways so an account that has not loaded is never read as either
- [x] `utils/username-gate.ts` — "This session owes a username", known synchronously, raised by a social sign-in in the same block as `markSignedIn` so the route guard has an answer before the account loads. In memory only, unlike `intro.ts`: a stored flag outliving its account would strand a user on the username screen
- [x] `auth/SignInGateProvider.tsx` — One place to ask "does this need an account?", and one dialog to answer it. `requireAccount(feature)` returns true when the caller may proceed and false when it has put the prompt up instead, so an account-only affordance anywhere in the app gates itself in one line. Mounted above the sheet/notification providers so the prompt outlives the surface the tap came from
- [x] `auth/SignInRequiredDialog.tsx` — What a guest sees when they tap something an account is needed for. Not a `ConfirmDialog`: it asks for two different yeses (make an account, or use the one you have), with dismissal as a quiet third option
- [x] `auth/SignedOutPanel.tsx` — What a guest sees instead of a tab that has nothing to show them (Agenda, Friends), and as a card at the top of Settings. Reads the same copy table as the dialog, so the sentence on the Friends tab is the one they read again if they tap "Invite" three screens later
- [x] `auth/account-features.ts` — The copy table both of the above read: for each thing an account is for, an icon, a title naming the *feature* rather than the restriction, and one sentence on what it does
- [x] `auth/BrowseWithoutAccountLink.tsx` — The way past the door, on both auth screens. A quiet link rather than a third button competing with Apple/Google; hidden when the user is already browsing as a guest
- [x] `utils/guest-cinema-selection.ts` — A guest's chosen cinemas, kept on the device (a signed-in user's live on their account). Empty means all cinemas, which is where a guest starts; `claimGuestCinemaSelection` hands them to the account on sign-up so the picking is not repeated
- [x] `utils/cinema-selection.ts` — Re-export of `shared/filters/cinema-selection.ts`, which now owns the resolve/commit/compare rules so the website resolves a selection identically
- [x] `hooks/useCinemaSelection.ts` — The session's cinema selection plus persistence for guests, so every picker in the app saves a guest's choice by doing nothing different from what it already did. **2026-09-23:** `CinemaSelectionScope` context — a page with its own selection (`usePageFilters`) provides one and every picker under it uses it instead of the session's
- [ ] `hooks/usePageFilters.ts` — New 2026-09-23. Page-local filters for the film, friend and cinema pages (friend/cinema used to read and write the tabs' shared filters). Copied from the feed only with `inheritFilters=1`, else empty — cinema ([] = all) and language included — so a page opened from Activity/notifications/friends list always shows the screening clicked. `InheritFiltersContext` + `useInheritFiltersParams`: provided by the Films tab and the showtime sheet (from its `inheritFilters`), read by `CinemaPill`/`FriendBadges`
- [x] `hooks/useSingleFireNavigation.ts` — Wraps a `router.push`/`replace` call so a double-tap only fires it once; `expo-router` navigation is async, so a disabled-on-submit re-render lags behind a fast double-tap and stacks the destination screen twice. Resets on screen focus. Rolled out to every push-on-press site found across the app (movie/friend/cinema cards, showtime long-press, login's sign-up/forgot-password links, the notification centre)
- [x] `hooks/useSwipePager.ts` — The horizontal pager behind the Activity tab's All/You/Friends pages and the Friends tab's Friends/Find people pages: the finger drags the row, a flick or a third of a page turns it, and the settle borrows `THUMB_SLIDE_MS`/`THUMB_SLIDE_EASING`. One shared value, `progress` (counted in pages, fractional mid-drag), is both the row's offset and the segmented control's thumb position, so the two cannot come apart. Everything visible runs on the UI thread by design: committing a page change re-renders a screenful of feeds, and on Android anything waiting on that commit sat frozen for a second or more after the swipe — so the gesture writes `progress` itself and React is told afterwards, and a tap goes through `goTo` before `setState` for the same reason. The index stays the caller's state, so a tap on the control or a `?mode=` deep link is tweened to like any other change. The resting page is a `SharedValue` and emphatically not a ref: a ref captured by a gesture worklet is copied to the UI thread when the gesture is built, so it freezes on the page the pager opened at and every later drag measures from the wrong origin. The pan is horizontal-only (`activeOffsetX`/`failOffsetY`) because every page holds a vertical list with a pull-to-refresh, and a drag that is at all vertical must be theirs
- [x] `hooks/useUserModeration.ts` — Block/unblock/report-user mutations behind one hook, mirroring `useFriendActions`; invalidates `["users"]` since a block changes search, friend-status and the friends list at once
- [x] `friends/ReportUserDialog.tsx` — "Report this person" reason picker (`ConfirmDialog`'s fade/scale chrome, a reason list instead of two buttons); reporting blocks by default, offered from `NonFriendProfile` and `FriendAgendaOptions`
- [x] `friends/NonFriendProfile.tsx` — Gained Block/Report (or Unblock, once blocked) links below the existing friend-request actions — App Store Review guideline 1.2's blocking/reporting mechanism for the one profile screen a stranger is ever shown
- [x] `friends/FriendAgendaOptions.tsx` — Gained the same Block/Report pair below the visibility control; blocking a friend removes the friendship, same as Remove
- `app/blocked-users.tsx` — "Blocked accounts" screen off Settings → Privacy: the one place a blocked account is still visible, since search/friends/invites all filter them out; unblock-only, does not restore what blocking removed
- [x] `constants/tablet-layout.ts` — `tabletCappedContentStyle` (maxWidth 640 + centered), the cheap fix for `RESPONSIVE_AUDIT.md` finding 16; applied to the four main-feed list containers, deliberately not to `FiltersModal`/`AppBottomSheet` (would mismatch their full-width pinned footers)
- ~~`app/modal.tsx`~~ — deleted (leftover Expo Router template screen — "This is a modal" — that was never navigated to; flagged as `RESPONSIVE_AUDIT.md` finding 22)
- [x] `utils/install-referrer.ts` — Android deferred deep link: reads the Play install referrer the web install panel attached to the store URL, so someone who tapped a shared link with no app lands on that link after installing. Claimed once ever (the referrer is fixed at install time and a reinstall would otherwise replay the old one) but cached as a promise for the process, because the effect that reads it re-runs when the session appears. Path shapes are whitelisted — a referrer is attacker-supplied and this ends in a navigation. iOS has no equivalent and is told to reopen the link instead
- [x] `utils/pending-deep-link.ts` — Read/write for the deep-link path held across a sign-in, with a 7-day bound so an abandoned link is not resumed on an unrelated login weeks later. Written by the route guard and the install referrer, followed in one place: the root layout, once the account is real *and* the intro is over. `completeLogin` used to follow it directly and dropped it entirely for new accounts, which lost exactly the invite-then-sign-up case this exists for
- [x] `utils/sentry.ts` — Crash reporting init plus `reportError()`, for failures the app deliberately swallows and Sentry's automatic handlers therefore never see. Added because a Play Services error dialog on the store build was unreproducible on any internal build (those are plain APKs, the store one is an app bundle) and there was no telemetry to tell which native call raised it. Every entry point no-ops without a `SENTRY_DSN`, so local and fork builds are unaffected
- [ ] `constants/api.ts` — New 2026-09-24. The backend URL, resolved once: `EXPO_PUBLIC_API_URL` if the EAS profile baked one in (`staging-device` does), else staging in dev and production in release. `IS_STAGING_BUILD` (release build on staging) drives the badge below and Sentry's `staging` environment. Replaces the inline `__DEV__` ternary that used to set `OpenAPI.BASE` in the root layout
- [ ] `layout/StagingBadge.tsx` — New 2026-09-24. Small orange STAGING tag top-right, touch-transparent, mounted last in the root layout; renders nothing unless `IS_STAGING_BUILD`. Exists because the staging build shares the store app's bundle id and would otherwise be indistinguishable on a test phone
- [x] ~~`utils/code128.ts`~~ → `shared/cineville/code128.ts` (moved 2026-09-23 so the website's Settings can draw the pass too) — Code 128 encoder (symbol table, code-set-B-with-C-for-digit-runs encoding, modulo-103 checksum), written rather than pulled in as a dependency because the app needs exactly one barcode. Verified bit-for-bit against a reference implementation
- [x] `utils/cineville-card.ts` — The device-local Cineville card number. Gained a small subscription store (`useCinevilleCardDigits`) so the agenda shortcut appears the moment the number is saved in Settings — secure storage cannot be watched, so writes announce themselves
- [x] `components/cineville/CinevilleBarcode.tsx` — Draws an encoded value as SVG bars, sized to its container in whole pixels per module and with the quiet zones a scanner needs. Deliberately unthemed: black on white in dark mode too
- [x] `components/cineville/CinevilleCardModal.tsx` — The Cineville pass for the door, on an `AppBottomSheet` so it can be swiped away one-handed the moment the scanner beeps: white card, human-readable number, and the screen eased up to full brightness while it is open (walked back down on close) via `useFullBrightness`
- [x] `hooks/useFullBrightness.ts` — Holds the screen at full brightness while a flag is true, ramping in over 400ms and back out over 300ms a frame at a time rather than stepping — a dark foyer to full brightness in one write is a flash in the face. Progress comes off the clock, so a slow device drops frames instead of running long, and an interrupted ramp (sheet reopened mid-descent) continues from the brightness actually on screen. iOS restores the exact value read beforehand; Android only aims the descent at that reading — it is on a different scale — and ends the boost by dropping the window override, the one thing that leaves adaptive brightness as the user had it
- [x] `components/cineville/CinevilleCardButton.tsx` — The floating shortcut into the above, at the bottom of the agenda and showtimes feeds where a thumb actually reaches. Rendered for guests too since the card is stored on the device and needs no account; hidden until a number is saved, and on any feed its Settings switch is off
- [x] `utils/cineville-shortcuts.ts` — Per-feed on/off for that shortcut, stored on the device beside the card number. Both default to on (a button that must be switched on before it can be found never is), and only an explicit "off" is persisted
- [x] `ui/AppSwitch.tsx` — The app's on/off switch, wrapping RN's `Switch` because iOS and Android want opposite props from it: `trackColor.false` is only an *outline* on iOS, so a white `thumbColor` on a near-white card left the off state a pale empty capsule. iOS gets `ios_backgroundColor` and the system thumb, Android keeps the explicit colors, and both get the selection haptic every other control in the app fires
- [ ] `ui/LetterboxdNotFoundWarning.tsx` — New 2026-09-23. Red "There is no Letterboxd account called …" box under the username field in Settings → Letterboxd and the intro's Letterboxd page, when `letterboxd_account_not_found` (definite 404 only). Mirrors the web's `Settings/LetterboxdNotFoundWarning.tsx`
- [ ] `app/verify-email.tsx` — New 2026-09-23. The app's end of the "Confirm your email" link (`mikino.nl/verify-email?token=`, caught by app links): same states and wording as the web page. Not in any route-guard list on purpose — the token is the proof, so it works signed in, as a guest and signed out
- [x] `app/default-visibility.tsx` — The account-level "default status visibility" screen: which of the 3 modes a newly selected showtime starts in. Reached from the per-showtime visibility dropdown, and on save asks once whether the new default should also take over the showtimes already selected (the screen skips that prompt when the account has none)
- [x] `friends/FriendOfFriendPopup.tsx` — The "+" on a friend-of-friend badge: send or accept a friend request without leaving the showtime sheet. Deliberately lighter than `NonFriendProfile.tsx` — Block/Report stay on the full profile
- [x] `utils/cineville-auto-copy.ts` — Whether opening a Cineville cinema's ticket link also copies the saved card code to the clipboard. Same shape as `utils/cineville-shortcuts.ts`: defaults to on, only an explicit "off" is persisted

---

## Mobile — Docs (`mobile/`)

- [x] `RESPONSIVE_AUDIT.md` — Screen-by-screen responsive/layout audit against a device matrix (SE → tablet). Records every layout risk and inconsistency found, which were fixed, and which are left as recommendations because they need a product decision or a screen restructure. Findings 15-22 are the open items.

---

## Root — Docs

- [x] `WEBSITE_PARITY_REPORT.md` — Feature-by-feature audit of the Expo app against the web frontend: what exists on each, what the gap costs, and a phased build order. Written 2026-09-06, when the web was ~2.5 months behind the app and calling 28 of the ~88 non-admin SDK methods. Its §2 records the load-bearing fact for any web work: the filter/showtime *logic* is already pure TypeScript and hoistable into `shared/`, so the remaining work is UI

---

## CI/CD (`.github/workflows/`)

- [ ] `test-backend.yml` — When does this run? What does it test?
- [ ] `lint-backend.yml` — Ruff + mypy linting
- [ ] `generate-client.yml` — When is the TS client regenerated?
- [ ] `deploy-staging.yml` — Staging deployment trigger and process
- [ ] `deploy-production.yml` — Production deployment (manual trigger)
- [ ] `test-docker-compose.yml` — Full-stack compose smoke test
- [ ] `smokeshow.yml` — Coverage report upload
- [ ] `latest-changes.yml` — Changelog generation
- [ ] `issue-manager.yml` — Issue triage automation

---

## Configuration & Infrastructure

- [ ] `docker-compose.yml` — Full local stack (backend, frontend, db, adminer)
- [ ] `docker-compose.traefik.yml` — Production stack with reverse proxy
- [ ] `backend/Dockerfile` — Backend image (multi-stage, uv, uvicorn)
- [ ] `frontend/Dockerfile` — Frontend image (Vite build + Nginx)
- [ ] `.env` structure — What variables are required? What are the defaults?
- [ ] `alembic/versions/` — 114 migrations: understand the schema evolution
- [ ] `.pre-commit-config.yaml` — What hooks run on commit?
- [x] `scripts/deploy-staging.sh` — Rsyncs the *local working tree* to the self-hosted runner's checkout on mi-ki and rebuilds the staging stack over SSH, so staging can be tried without pushing to dev. The prod→staging data reseed the real workflow does on every push is opt-in here (`--with-reseed`): it is slow and has taken staging down before
- [x] `scripts/server-maintenance.sh` — Cron job on mi-ki: prunes the Docker containers/images/build cache repeated deploys leave behind, then emails an alert if root disk usage is still over 85%. Reuses the SMTP creds already in the staging `.env`

---

## Cross-cutting Concerns (review after individual files)

- [ ] Error handling: are all services using domain exceptions consistently?
- [ ] Logging: is Loguru used consistently throughout?
- [x] Auth: refresh tokens implemented (30 min access / 90 day refresh); both web and mobile now auto-refresh via `shared/authRefresh.ts`
- [ ] Test coverage: reach 80%+ on services and CRUD
- [ ] Frontend tests: set up Vitest + React Testing Library
- [ ] API rate limiting: evaluate adding slowapi
- [ ] Duplicate code in scrapers: strengthen base_cinema_scraper.py
- [x] `filters/FiltersButton.tsx` — The button that opens the Filters sheet, in one place on every screen that has one: to the left of the search field, in `SearchBar`'s `leftSlot`. It used to sit on a row of its own on the sub-pages (in a `FiltersButtonRow` since deleted), which cost a whole band of vertical space to hold one button and made the same screen chrome read differently on the feeds and the sub-pages. It used to be pinned to the left of the preset row, which left that row's caption heading only half a row and forced the two halves to be height-matched; beside the search field it stretches to the field's height and the presets get a row of their own
- [x] `filters/PresetsRow.tsx` — (was `FiltersRow.tsx`) The saved-preset buttons under an uppercase "Presets" caption. The caption is the only thing that can say what these are: a preset's label is a name the user chose, so it carries no signal of its own — squared corners then separate them from the fully-rounded pills and chips that hold state. Absent entirely for a guest rather than empty
- [x] `inputs/SearchFieldFallback.tsx` — Sits under an empty result on either feed when the search ran against something other than the title: names the field that was searched and offers the same query back against titles. The field is set once from a dropdown and then only visible in the placeholder, so a later search for a film by name comes back empty with nothing on screen explaining why. Renders nothing for a title search or an empty query; reached through `ShowtimesScreen`'s new `emptyExtra` slot on the showtimes feed, and inline in both movie-grid empty states
- [x] `filters/PresetButton.tsx` — One saved-preset button, owning its own tap animation: a small scale pop, a brief lift in the neutral the chips flash, and a fade to the disabled look, all one closed 250ms animation started at t=0 and interruptible by nothing. Reanimated rather than RN's `Animated`, and that decides the file: `Animated.sequence`/`delay`/`spring` hand over between stages *on the JS thread* even under `useNativeDriver`, and a press fires an apply that holds that thread for the whole of the animation — which came out on a mid-range Android as pop, shrink, stall, pop again, stall, flash, snap. The press also claims its own outcome (a preset just applied has nothing left to apply) and reconciles with the row once at the end, rather than letting the row's verdict — which arrives in pieces, mid-press — drive the animation. Per button rather than per row so two presets tapped in quick succession run on their own timelines. `onApply` is called in a `requestAnimationFrame` so the apply's render cannot get ahead of the button lighting up
- [x] `filters/filter-change-animation.ts` — How the filter rows answer a change and move while they do, as two beats: everything that leaves goes first (with the cinema pill resizing on the same clock, `PHASE_ONE_MS`), then everything that arrives. Sequencing them is what stops an arriving chip being laid out in the row's new shape while the pill is still travelling towards it. A chip's flash is *part of its entrance* — one `EntryExitAnimationFunction` whose `initialValues` carry the flashed colours and whose animations time them back to resting — not a second animation on a shared clock, which is what it was: the chip only started reading that clock once a `setState` told it to, a commit later, so the growth finished before the colour began. Every wait inside a beat is a Reanimated delay on the UI thread, never a `setTimeout`: applying a preset re-renders the whole feed, and a JS-thread boundary drifts hundreds of milliseconds behind the animations it is meant to be following. The flash is a neutral step away from the pill's resting colour, not an accent — green read as "this is on", a claim about state, and every chip in the row is already on. `FILTER_ROW_SETTLE_MS` is exported for the screens *around* the row: mounting a feed's worth of cards is UI-thread work and stalls a running animation just as a blocked JS thread stalls a timer
- [x] `filters/ActiveFilterChip.tsx` — One active-filter chip, extracted from `ActiveFilterChips` so it can carry its own entering/exiting/layout animations. Nothing keeps a removed chip alive by hand — Reanimated holds it on screen for its exit, which is why the row can simply stop rendering it
- [x] `filters/MorphingChipLabel.tsx` — A chip label that changes without snapping: text cannot morph, so the outgoing label slides out sideways and fades while the incoming one slides in behind it, over roughly the time the chip's width transition takes to catch up. The direction follows the width — a longer label arrives from the right as the chip opens up for it, a shorter one from the left as it closes in — so the text and the box travel the same way at the same moment instead of reading as two animations. The outgoing copy is absolutely positioned so only the incoming text decides how wide the chip wants to be, and it is left mounted at zero opacity rather than cleared on a timer. The swap is taken during render, not in an effect: deferring it by a frame put the chip's resize in a commit of its own, after the row had stopped holding movement back for chips still animating away, and the two collided. Used by the cinema pill, whose label is a count one moment and a preset's name the next; any chip with a changing label can take it
- [x] `feeds/feed-paging.ts` — How both infinite feeds ask for and render rows, in one place because they are the same list with a different card. Sizes the *first* page to a screenful plus two from the row height each card states (`SHOWTIME_ROW_HEIGHT`, `MOVIE_ROW_HEIGHT`), leaving later pages full-size: the first page is the only one always fetched, and a filtered feed is very often scrolled nowhere, so the rest of a twenty-row page is query, payload, parse and two batched prefetches spent on rows nobody sees. Two things have to come with it or it saves nothing — `getNextPageParam` must sum real page lengths instead of assuming uniform pages, and `onEndReached` must wait for an actual drag (`useScrollTriggeredLoadMore`), since `FlatList` otherwise calls it on mount for any content shorter than the threshold and pulls the next page unasked. Also caps the render window: the defaults (10 initial rows, 21 viewports) mount a whole page of native views on the UI thread while whatever caused the fetch is still animating
- [x] `filters/ActiveFilterChips.tsx` — The row, and the conductor for its own motion: the only thing that can see both halves of a change, so it decides where each beat starts. It never drives an animation or owns one — every decision is made in the commit the change lands in and handed to a chip as two booleans read at mount, because anything the row still had to say afterwards would arrive too late to be part of the movement. Removals go the same way round: a tapped chip is dropped from the row's own list first (`dismiss`) and the filter written a frame later, so the exit starts in a commit that rebuilds nothing — most filter setters write the session cache synchronously, and the feed rebuild that follows was holding the exit up long enough to read as the row ignoring the tap
- [x] `tab-bar.tsx` — (was `haptic-tab.tsx`) The bottom bar's button, icon and label. All three exist because the default bar derives everything it shows about the selected tab from navigation state, and navigation state changes in the *same commit* that renders the screen being navigated to — so a tab press showed nothing at all until that screen had built, then the bar and the screen arrived together: a stall followed by a jump. Here one module-level shared value says which tab is lit, set on press *before* the navigator is called, and the highlight, the icon colour and the label colour all follow it on the UI thread. Navigation still decides what is really selected, but the correction is watched as an edge, never asserted every render — between the press and the navigator catching up the bar still believes the old tab is selected, and asserting would snap the highlight back. Colours cross-fade rather than tween, because an icon's colour is a prop and not a style: each is drawn twice, once in each colour, and the copies are faded past each other. The press haptic fires on touch-down with the flash, and on **both** platforms via `triggerTabPressHaptic` — the Expo tab template this began as fired on iOS only, reasoning that Android "already has native ripple feedback", which confuses something you see for something you feel
- [x] `layout/TabScreenSkeleton.tsx` — What a tab shows between being pressed and being built. A tab screen is mounted the first time it is opened and React renders the whole thing before committing anything, so the tab you pressed *away from* stayed on screen for as long as the new one took to build — the press looked ignored. Each tab now gates its content behind `useDeferredMount` and puts this up instead: the same top bar the screen will have, and `ListLoadingLogo` under it. Rows of bones were what it used to draw, and they could only ever be right for one tab — feed cards, friend rows and settings groups are three different shapes, and being wrong about the shape made the content arriving read as a correction; the panel promises no layout at all and is the same wait the rest of the app shows. The gate is a wrapper component rather than an early return, so every hook the screen owns sits behind it — an early return inside one component would defer only the render, not the queries and subscriptions that set it up. The wait is whatever `tabContentHoldMs()` still owes the tab bar's press flash: the mount takes the UI thread, and without the wait it lands in the middle of the movement that is supposed to be answering the tap. The *navigation* is no longer held back for it — that cost every press ~170ms of showing the tab it was leaving, and an already-built tab has no mount to protect the flash from
- [x] `layout/LoadingLogo.tsx` — The one panel every wait in the app puts up: splash logo, spinner, one line of text. Extracted from `ThemeSwitchOverlay`, which now renders it, so a wait inside a sheet and a wait covering the whole screen are visibly the same app waiting. Takes its colours as props rather than reading the theme hook, because the theme curtain is painted in the palette being switched *to*, which the hook cannot know yet. The spinner stays an `ActivityIndicator` for the reason the curtain always had one — what it covers is a heavy render, so anything stepped from JS would stall exactly when it is meant to be saying "still working" — and the optional `fadeIn` exists for callers that put it up on *every* open, where most opens finish before the fade does and so show nothing rather than a blink
- [x] `layout/ListLoadingLogo.tsx` — `LoadingLogo` sized and posed for a feed, and the app's replacement for every list skeleton (`SkeletonRows` is gone). No spinner — every caller has a `ThemedRefreshControl` pinned above it and the two would spin side by side — and it sits a little above centre, since dead centre reads as low once the search bar and filter row above it are counted. Callers mount it as an **absolute overlay** rather than as a list's `ListEmptyComponent`: list content scrolls, and RefreshControl's pull drags it, which read as the logo drifting down the screen. `pointerEvents="none"` so it never eats the pull-to-refresh gesture underneath it. The movie page is the deliberate exception: its list is headed by the movie's *already loaded* description, which can run long, so a viewport-centred overlay printed the logo over good text — there it goes in the empty slot instead, under the description at the top of the showtimes section, in a fixed-height parent. That only holds because the panel there is never up for a refresh. The hard rule is in its doc comment: it must never come up for a pull-to-refresh at all, which is RefreshControl's own spinner's job
- [x] `hooks/useDelayedTrue.ts` + `constants/loading-logo.ts` — When the loading panel is allowed to appear, which is the whole difficulty with it. True only once its input has held for `LOADING_LOGO_DELAY_MS`, so a filter or preset that resolves from cache never flashes it; false *synchronously*, gated on the input rather than on the effect that resets it, because the render where real content arrives is exactly the render the panel has to be gone in and an effect lands a frame or more later (visible on Android, where a big list commits slowly). `LOADING_LOGO_COOLDOWN_MS` additionally holds back the *next* appearance after a hide, so tapping through several filters shows the panel at most once per window instead of once per tap. A screen's own deliberate hold (a preset's `FILTER_ROW_SETTLE_MS` settle window) gets its own clock with the delay but **no** cooldown — a cooldown left ticking by the previous preset outlasts the next one's hold and swallows its panel entirely
- [x] `ui/FeedItemEntrance.tsx` — Wraps one feed row so it fades and lifts into place instead of popping in, staggered ~45ms per row and capped at the eighth, so a page reads as filling in rather than snapping in as a block. Past the first screenful the stagger would only delay rows nobody is looking at. On every paginated feed in the app: the showtimes/movies tabs, cinema and friend showtimes (via `ShowtimesListContent`), the movie page's showtimes and both Friends-tab lists. In a `SectionList` the `index` it is given is the row's place within its *section*, so each group cascades from its own header
- [x] `sheets/use-sheet-content-ready.ts` — Why every sheet opens on the tap. gorhom mounts a sheet's children the moment `present()` is called and only starts the rise once that mount has committed, so a heavy body — `CinemaFilterModal`'s ~80 chips, `ShowtimeActionModal`'s 3400 lines — was being built *before* the sheet moved. The body is gated on this flag with a `SheetLoadingPanel` in its place, so the mount costs the open nothing. **Nothing may be added between the tap and the rise**: six attempts at avoiding the panel are listed in `AppBottomSheet`, the last of which built the body first and rose second and took tap→rise on a current iPhone from 98ms to 320ms. The reset on close is load-bearing, not tidy-up — the body renders from whatever the sheet was last given and @gorhom/portal commits sheet content a render late, so a body kept across opens is the *previous* item on screen for a frame or two. A timer and not `InteractionManager`: gorhom animates under Reanimated, which registers no interaction handle, so `runAfterInteractions` resolves immediately and defers nothing (learned on the seat-plan sheet). Also exports `useSheetPanelReady`, the one thing allowed between tap and rise: that same portal lateness means a panel rendered in the commit that calls `present()` is not on screen when the rise begins, so the sheet came up empty and caught up mid-animation. The panel's own `onLayout` is the signal rather than a count of frames — it fires when the views exist and have been measured, and is as fast as the device is instead of as slow as the worst device guessed at — with a 64ms timeout so a panel that never reports makes the sheet late rather than stuck. `ShowtimeActionModal` deliberately uses **none** of this — it builds its content and rises with it, waiting only for the portal commit (`CommittedShowtimeReporter`); the panel was tried there and rejected, because that sheet's whole subject is what a logo would be hiding
- [x] `sheets/sheet-timing.ts` — The one clock every sheet runs on: `SHEET_OPEN_DURATION_MS` (the rise, and the sink), `SHEET_CONTENT_MOUNT_DELAY_MS` (the content gate, on top of it) and `SHEET_CONTENT_FADE_MS` (the cross-fade out of the panel). Its own module because `AppBottomSheet`, `use-sheet-content-ready`, `SheetContentFade` and `SheetLoadingPanel` all need it, and importing it from any one of them closes a cycle through the others — which for a module-scope constant means whichever side loads first reads `undefined`
- [x] `sheets/sheet-warm-up.ts` — **Deleted 2026-09-24.** The startup warm-ups caused more trouble than their ~325ms first-open saving on Android (a sheet bouncing up after the intro, an orphaned sheet eating every tap on the Android login screen). Every sheet is now built on its first open and kept. Replaced for stacking by `sheets/use-stack-above-key.ts`: the one pair that has to stack (Cinemas over Filters, in `FiltersModalProvider`, `app/movie/[id].tsx` and `app/friend-showtimes/[id].tsx`) rebuilds Cinemas once, via a new `key`, the first time it opens over Filters after having registered before it.
- [x] `sheets/SheetLoadingPanel.tsx` — What a sheet shows in place of content it does not have yet, and deliberately rare: sheets build their body before they rise, so one whose data is in hand comes up full and never puts this up at all. What is left is a sheet genuinely waiting on the network — a showtime opened by id from a link or a notification, an uncached cinema list — where rising on this beats making the tap wait out a fetch. Whole sheet, never part of one: a sheet that shows some of itself while the rest loads shows whatever it was given *last* in that part, since the body renders from the previous props until the new ones commit. Five attempts at making it appear less often are listed in `AppBottomSheet`; each traded the flash for something users noticed more
- [x] `sheets/SheetContentFade.tsx` — Cross-fades a sheet's content in over the loading panel it replaces, because the swap is otherwise a hard cut on the frame the body commits and the sheet is already still by then, so the change is the only thing moving. Costs the open nothing: it starts *after* the content has committed, so the content lands at the same moment either way, and runs on the native driver so the commit it follows cannot stall it. Started two `requestAnimationFrame`s deep, not one — the commit that mounts it is hundreds of native views, and an animation started inside that spends its first frames blocked and arrives part-way through. The panel is faded out underneath rather than dropped, since content fading up from an empty sheet is the thing being avoided; `panelHeader` carries anything the loading state drew above it (`ShowtimeActionModal`'s own grab handle) so the layer fading out is what was actually on screen
- [x] `utils/android-back.ts` — Who gets Android's back press, decided by what opened last instead of by React. RN calls its `hardwareBackPress` subscribers in reverse registration order *and re-registering jumps the queue*, so with the cinema sheet open on top of the Filters sheet the press went to Filters: opening the cinema sheet re-rendered `FiltersModalProvider`, `FiltersModal` re-subscribed in that same commit, and back closed the sheet behind the one the user was looking at. Now every surface is stamped with a sequence number when it opens and keeps it, one RN subscription fans the press out highest-first, and the handler is read through a ref so a re-render can never reorder anything. The subscription is dropped when the last surface closes, so it is always re-added after react-navigation's and an open surface still beats navigation. `AppBottomSheet`, `ShowtimeActionModal` and `SearchBar`'s `AndroidBackClear` all go through it — nothing subscribes to `BackHandler` directly any more
- [x] `utils/use-settled-focus.ts` — Focus, reported once the tab switch has been drawn instead of while it is being computed. `useIsFocused` subscribes to navigation state, so every screen calling it re-renders *inside* the commit that switches tabs — and the switch is only on screen once that commit is done. On the showtimes, activity and movies tabs that meant the whole feed re-rendering plus every focus-gated query dispatching a refetch and notifying every other mounted observer, all of it between the tap and the tab appearing, with the *previous* tab still on display. None of that work belongs in the switch: the tab being opened is already mounted and already holds its last render, and what focus actually gates is whether its queries may run, which nothing sees a frame later. Waits on `InteractionManager` with a 200ms backstop, because a leaked interaction handle anywhere in the app would otherwise leave a focused tab's queries switched off for good. Use `useIsFocused` wherever focus changes what is *drawn* — this would show the wrong thing for a frame
- [x] `app/_layout.tsx` → `RouteGuard` — the auth/route redirect rules pulled out of `RootLayourContent` into a component that renders `null`. Not tidiness: `useSegments`/`usePathname` subscribe to expo-router's store, so whatever calls them re-renders on **every** navigation — and `RootLayourContent` renders the whole shell (the stack navigator and all of its descriptors, the intro and notice hosts, the theme curtain). All of that was rebuilding inside the commit that switches tabs, and the switch is only on screen once that commit lands. It reads its own auth state rather than taking props, because a prop would put the subscription back in the parent
- [x] `hooks/useAppIconBadge.ts` — The number on the home-screen app icon, which is deliberately a *third* sum and not either badge already in the app. The bell counts unseen events and zeroes the moment the centre is opened; the Friends tab badge counts friend requests still outstanding. Off the home screen there is only one number, so it carries both — a friend request badged nothing until you happened to open the app, which is precisely the notification users reported missing. Opening the app therefore clears the event half and leaves the friend-request half lit until the request is accepted or declined. Local feature tips are left out although they count towards the bell: they are the app's own nudges, not something that happened to the user. Rides on the two queries the tab layout and TopBar already poll, so it costs no requests, and invalidates them on foreground because React Query's `refetchOnWindowFocus` never fires in React Native — nothing wires its focus manager to `AppState`, and a badge fifteen seconds stale is stale exactly when it is being read. The backend computes the same sum in `push_notifications.badge_count` and puts it on every push, so the icon is already right when the app is opened from one
- [ ] `backend/app/services/me.py` `_refresh_letterboxd_avatar` + `scraping/letterboxd/watchlist.py` `get_avatar_url` — New 2026-09-23. Turning `use_letterboxd_avatar` on (or changing the username while it is on) fetches the picture from the watchlist's first page in the request. `mobile/components/intro/IntroLetterboxdPage.tsx`: the switch became a second step (`AvatarQuestion`) after the username saves.
- [ ] `shared/users/avatar-sources.ts` — New 2026-09-23. Avatars drawn ≥64px try Letterboxd's 300px crop first, falling back to the stored URL; used by both `PersonAvatar`s (which also no longer stay on the initial when a URL arrives after mount).
- [ ] `frontend/src/components/Tips/VerifyEmailTip.tsx` + `tips.css` — New 2026-09-23. Web port of the app's always-show "Confirm your email" tip: signed in + `email_verified` false + no intro pending; closing holds for the browser session (sessionStorage). Mounted in `_layout`.
- [x] `mobile/components/tips/FriendRequestTip.tsx` — **Deleted 2026-09-24.** Friend-request notifications are reasonable to have off, so no tip nudges them on; removed from `FeatureTipId`, `EVENT_TIP_IDS`, the host and the notification-centre row map.
- [x] `mobile/hooks/useMarkShowtimeInvitesSeen.ts` — New 2026-09-24. Opening a screening you were invited to marks just that screening's invites seen (`POST /me/pings/showtime/{id}/mark-seen`), which takes it off both the Activity tab badge and the bell, since both count `ShowtimePing.seen_at`. It also clears the new red "unseen invite" dot on `ShowtimeCard` (from `viewer.has_unseen_invite`) in the cached feeds straight away. Called from `ShowtimeModalProvider`, so it covers every place the sheet opens. The Activity page no longer marks every invite seen when "You" is shown.
