/**
 * The user's manual ordering of their cinema presets.
 *
 * Moved to `shared/filters/cinema-preset-order.ts` so the website shares the exact same logic;
 * re-exported here to keep existing `@/components/filters/cinema-preset-order` imports working.
 */
export * from "shared/filters/cinema-preset-order";
