/**
 * Canonicalising a page's filter state into preset-comparable form.
 *
 * Moved to `shared/filters/filter-preset-utils.ts` so the website shares the exact same logic;
 * re-exported here to keep existing `@/components/filters/filter-preset-utils` imports working.
 */
export * from "shared/filters/filter-preset-utils";
