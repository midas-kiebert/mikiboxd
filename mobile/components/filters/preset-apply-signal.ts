/**
 * The cross-component signal fired when a preset is applied.
 *
 * Moved to `shared/filters/preset-apply-signal.ts` so the website shares the exact same logic;
 * re-exported here to keep existing `@/components/filters/preset-apply-signal` imports working.
 */
export * from "shared/filters/preset-apply-signal";
