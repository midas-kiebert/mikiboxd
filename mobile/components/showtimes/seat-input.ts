/**
 * Parsing and validating a typed seat label.
 *
 * Moved to `shared/showtimes/seat-input.ts` so the website shares the exact same logic;
 * re-exported here to keep existing `@/components/showtimes/seat-input` imports working.
 */
export * from "shared/showtimes/seat-input";
