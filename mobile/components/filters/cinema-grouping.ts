/**
 * Grouping cinemas by city, and naming a cinema selection's scope.
 *
 * Moved to `shared/filters/cinema-grouping.ts` so the website shares the exact same logic;
 * re-exported here to keep existing `@/components/filters/cinema-grouping` imports working.
 */
export * from "shared/filters/cinema-grouping";
